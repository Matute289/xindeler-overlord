# === Node / JS ===
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.tsbuildinfo

# === Expo ===
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts

# === Native project dirs ===
# Generated by `expo prebuild` / CNG. Regenerate them; never hand-edit and never commit.
# If a config plugin can't express something, that's a signal to write a plugin, not to
# check these in.
/ios/
/android/

# === Signing material — NEVER commit any of this ===
*.p8
*.p12
*.pem
*.key
*.mobileprovision
*.provisionprofile
*.keystore
*.jks
google-services.json
GoogleService-Info.plist
credentials.json

# === Secrets / config ===
.env
.env.*
!.env.example

# === macOS / editors ===
.DS_Store
*.swp
.idea/
.vscode/
!.vscode/extensions.json

# === Local scratch ===
/tmp/
/scratch/
*.local.md

# === Claude Code ===
.claude/settings.local.json
.claude/worktrees/
