# Backlog — Xindeler Ops Console

**Convention:** `OC-N` rows, appended, never renumbered. One PR per row, branch `ocN/<slug>`.
Update the row's status in the same PR that does the work.

**Status legend:** ⬜ not started · 🔄 in progress · ✅ done · 🅿️ parked · ❌ dropped

**Phases mirror NH-75's build order** (`xindeler-new-horizon/docs/design/specs/2026-08-09-nh75-ops-console-oracle-design.md`
§7), re-cut for a native client. Two things differ from NH-75's plan and both matter:

1. **NH-75's P0 is VPS work, not app work.** WireGuard peer, `xindeler-ops` user, the systemctl
   wrapper, `ui_api_secret` in `settings.ron` — all of that belongs to the *gateway*. This backlog
   starts at the app's own P0: toolchain + store accounts + a scaffold that runs on three targets.
2. **The gateway does not exist yet.** OC-13 (mock gateway) is therefore not optional polish — it
   is what unblocks every screen. Build it before the screens, not after.

---

## Decisions resolved

The six decisions listed in `docs/specs/2026-08-09-client-architecture-design.md` §9, answered by
Matías 2026-08-09 via the fill-in-worksheet convention:

| # | Decision | Resolution |
|---|---|---|
| 1 | Framework: Expo or the Capacitor off-ramp | Already settled before the worksheet — Expo, per Matías's original ask. Not reopened. |
| 2 | Repo visibility | **Stays public.** No secrets ever live here (enforced by `ops-safety-reviewer`); the real access boundary is the gateway's WireGuard tunnel + TOTP step-up, not client-source secrecy. Public also keeps branch protection and CI minutes free on a personal GitHub account — private would cost money for the same protections Matías wants to keep. |
| 3 | Bundle id / app name / icon | **Bundle id `com.xindeler.overlord`, display name "Overlord"** (supersedes the spec's placeholder `dev.xindeler.opsconsole` / "Xindeler Ops"). Icon: `~/MyXindeler/imagenes-assets/Overlord/overlord_app-icon.png`, copied into `assets/`. |
| 4 | Gateway repo shape | Confirmed: **`xindeler-ops-gateway` is a separate, private repo**, not a monorepo package. Matches everything already assumed in this repo's docs. |
| 5 | Who else gets the app | Just Matías for now; **~2 more testers possible later**, not urgent. TestFlight *internal* track (100 testers) stays the right shape; per-operator audit attribution can stay Phase 6 (OC-28 already covers durable audit rows; per-operator attribution is cheap to add if/when a second operator actually joins — revisit then rather than now). |

NH-75's own open questions Q1–Q8 (exposure posture, etc.) are **not** resolved by this — they are a
separate, larger design question for a `xindeler-ops-gateway` design session, and directly determine
OC-12/OC-22.

---

## Phase 0 — Toolchain, accounts, scaffold

Goal: `npx expo run:ios`, `run:android` and `expo start --web` all render the same "hello" screen,
and a signed build has reached both TestFlight and Play internal testing **before any real feature
exists**. Proving the pipeline first is deliberate — the store pipeline is where the multi-day
surprises live, not the UI.

| ID | Item | Notes | Status |
|---|---|---|---|
| OC-1 | Install **Xcode 26** + iOS 26 simulator runtimes | ✅ Done 2026-08-09. Xcode 26.6 installed, selected (`xcode-select -s /Applications/Xcode.app`) and licensed. iOS 26.5 simulator runtime installed via `xcodebuild -downloadPlatform iOS`; simulators available (iPhone 17/17 Pro/17 Pro Max/17e/Air, iPad Pro 13"/11" M5, iPad mini A17 Pro, iPad Air 13"/11" M4, iPad A16). ⚠️ **Xcode 26 / iOS 26 SDK has been mandatory for every App Store Connect upload since 2026-04-28** — this is not optional even if EAS builds in the cloud. | ✅ |
| OC-2 | Install Android Studio + SDK **API 36** + an emulator image | ✅ Done 2026-08-09 via `brew install --cask android-commandlinetools` (no Android Studio GUI needed — cmdline-tools only, `sdkmanager`/`avdmanager` on PATH). Installed: `platform-tools` 37.0.1, `platforms;android-36`, `build-tools;36.0.0`, `emulator`, `system-images;android-36;google_apis;arm64-v8a`. AVD `xindeler-ops-test` (Pixel 7) created. **JDK 17 (Temurin) installed alongside the system JDK 26** — pin `JAVA_HOME=$(/usr/libexec/java_home -v 17)` per-project, do not change the system default. SDK root: `/opt/homebrew/share/android-commandlinetools`. Play requires target API 36 for all uploads from **2026-08-31** — already covered. | ✅ |
| OC-3 | Scaffold the Expo app | Done 2026-08-09. **SDK 57.0.11** (RN 0.86.2, React 19.2.3), TypeScript strict, Expo Router, NativeWind 4.2.6 (pinned to tailwindcss 3.4.19 — v4 is not yet supported, see `ops-run` §0 gotchas), `app.config.ts` (bundle id `com.xindeler.overlord`, name "Overlord"). Three-target smoke run passed: web (`expo start --web`, HTTP 200), Android (`expo run:android` on `xindeler-ops-test`, screenshot confirmed) and iOS (`expo run:ios` on iPhone 17 Pro, screenshot confirmed). | ✅ |
| OC-4 | Repo hygiene | Done 2026-08-09. ESLint (flat config, `eslint-config-expo` + `eslint-config-prettier`), Prettier (scoped to code — docs keep their own hand-formatted conventions), `.nvmrc` pinned to `26.3.0` (already installed, satisfies RN 0.86's engines range; no separate LTS install needed). `.editorconfig` already existed from bootstrap, unchanged. ⚠️ ESLint **10.x** hits a real bug in `eslint-config-expo`'s bundled `eslint-plugin-react@7.37.5` (`context.getFilename is not a function`) — pinned to ESLint **9.39.5** until that's fixed upstream, see `ops-run` SKILL.md. GitHub Actions CI (running these on every PR) is OC-5, not this row. | ✅ |
| OC-5 | GitHub Actions CI | Done 2026-08-09. `.github/workflows/ci.yml` runs `typecheck` + `lint` + `format:check` (the same three OC-4 commands from `CLAUDE.md`'s pre-PR line) on every PR and on push to `main`/`development`. No `test` job yet — no test runner exists. `ubuntu-latest`, Node pinned via `.nvmrc`, `npm ci`. Public repo = free unlimited Actions minutes. **No store credentials or secrets used** — nothing beyond `npm ci` and the three checks. | ✅ |
| OC-6 | Apple: bundle id + App Store Connect record + TestFlight internal group | Done 2026-08-10: App ID `com.xindeler.overlord` registered (no capabilities selected — none needed yet, trivial to add later since EAS manages signing/profiles), app record created in App Store Connect, Test Information filled (feedback email `overlord-feedback@xindeler.com`, beta description), internal testing group "OverTesters" created with Matías as the sole tester. Never submitted for App Review — stays in "Prepare for Submission" indefinitely, as intended. First build installed and confirmed working via OC-9. | ✅ |
| OC-7 | Google: Play Console app entry + internal testing track + tester list | ✅ Done 2026-08-11. App entry "Overlord" (`com.xindeler.overlord`) created in Play Console once Google's identity verification cleared. Tester list "OverTesters" created with `maticgrinberg@gmail.com`. Internal testing track's API-access requirement resolved via **Users and permissions** (Google retired the old dedicated "API access" page — the current flow is: create a service account in Google Cloud Console with the Play Android Developer API enabled, then invite its `...iam.gserviceaccount.com` email as a Play Console user with the "Lanzar aplicaciones en canales de pruebas" app permission). Service account `overlord-eas-submit@xindeler-overlord.iam.gserviceaccount.com` invited and scoped to Overlord. | ✅ |
| OC-8 | EAS project + build profiles | Done 2026-08-10. EAS project created under the `xindeler-team` account/org (Matías created it during `eas login`), linked via `extra.eas.projectId` + `owner: 'xindeler-team'` in `app.config.ts` (dynamic config — `eas init` can't write to it automatically, added by hand both times it asked). `eas.json` has all three profiles: `development` (dev client, internal), `preview` (internal), `production` (autoIncrement). No `credentialsSource` override — EAS manages signing credentials by default, as planned. Free tier: 30 builds/month (15 iOS + 15 Android), 45-min timeout; Starter USD 19/mo for 2 h. Decide free-vs-paid after the first few builds. | ✅ |
| OC-9 | **First round-trip build to both stores** | ✅ Done 2026-08-11, both platforms. **iOS** (2026-08-10): build → submit to App Store Connect (API key, **App Manager** role — least privilege, not Admin) → processed by Apple → manually attached to the "OverTesters" group (builds don't auto-attach to a group, that's a manual step every time) → installed via the TestFlight app on Matías's own iPhone. `usesNonExemptEncryption: false` meant no export-compliance prompt. **Android** (2026-08-11): `eas submit --platform android --latest` run interactively by Matías once the OC-7 service account key was ready — completed end-to-end, version 0.1.0 is Active/"Disponible para testers internos" in the internal testing track, no manual review-and-confirm step needed. Phase-0 exit criterion **met for both platforms**. | ✅ |

---

## Phase 1 — Shell, auth, read-only console

Maps to NH-75 P1. **Zero write capability.** This alone delivers "see the server from my phone",
which is the most-used half of the ask.

| ID | Item | Notes | Status |
|---|---|---|---|
| OC-10 | Theme + design tokens | ✅ Done 2026-08-10, restyled 2026-08-10 (final-review fix wave). `src/ui/theme.ts` now exports only `colors`/`fonts`/`useTheme()` — spacing and typography moved to NativeWind `className` (Tailwind's default scale + `tailwind.config.js` color tokens), per `ops-ui` SKILL.md's "NativeWind, no inline styles" rule. `useTheme()` is kept only for real JS values a component prop needs (Ionicons `color`, active/inactive className pick) — `fonts` stays a JS value too since RN `Text.fontFamily` must exactly match a loaded font name. `tailwind.config.js` carries both palettes: light hex as the default utilities, dark hex under a parallel `night` namespace selected via `dark:` (NativeWind's actual `media`-driven mechanism — see the file's header comment for why light is "default" there despite this app being dark-first). Two primitives shipped so far — `Screen`, `Empty` — the other ~13 from spec §3 are deliberately deferred to the screens that need them (Phase 1+). App icon, splash icon, Android adaptive icon and web favicon replaced with the corrected full-bleed Overlord crest (verified in the production web export's generated `favicon.ico`, not just the source PNG). The O-mark loading animation is deliberately deferred, no screen needs it yet. Known minor gaps, parked (none blocking, none load-bearing for later tasks): on web only, the sidebar's top padding is silently overridden to 0 by `SafeAreaView`'s web style handling (native unaffected, cosmetic ~32px offset — fix by moving the padding to an inner `View`); `useTheme()`'s unknown-color-scheme tie-break (defaults to dark) technically disagrees with NativeWind's own tie-break (defaults to light) — one-word fix (`scheme === 'dark'`) if it ever matters in practice; sidebar nav items lack explicit `accessibilityRole`/`accessibilityState`; a few unused theme/CSS tokens left in place as part of the full palette. | ✅ |
| OC-11 | Navigation skeleton | ✅ Done 2026-08-10. `app/(tabs)/_layout.tsx`: one route tree, 5 destinations (Status·Jugadores·Logs·ORACLE·Más), bottom tabs under 768 pt width, persistent left sidebar at/above it (`useBreakpoint` hook, `useWindowDimensions`-driven, live-reactive with no reload). Manual verification pass (Task 8) on iPhone sim, iPad sim, and web found and fixed one real bug: the bottom tab bar was rendering with React Navigation's untouched default theme (white bar, iOS-blue active tint) instead of the app's dark palette — fixed by wiring `tabBarStyle`/`tabBarActiveTintColor`/`tabBarInactiveTintColor` from `colors.*`, matching the sidebar. Sidebar's active-item highlight (`usePathname() === '/'` for Status at the root route) is now runtime-verified — Task 8's own environment couldn't drive the iPad simulator or the automated browser past the 768pt threshold (iPadOS 26's windowed-app/Stage Manager default on the M5 iPad sim, and a fixed/unresponsive viewport in the browser automation tool), so this was confirmed separately by temporarily forcing the breakpoint threshold to 0 (never committed), screenshotting the live sidebar (Status highlighted at `/`, highlight correctly moves to Jugadores after navigating to `/players`), then reverting. Sidebar container now wrapped in `SafeAreaView` (`edges: left/top/bottom`) — landscape on any current iPhone crosses the 768pt sidebar threshold, so the notch/Dynamic Island can sit over the sidebar's left edge otherwise. ⚠️ `eslint.config.js`'s `import/no-unresolved`/`import/namespace` override (originally scoped to `app/**`) is now repo-wide (`**/*.tsx`/`**/*.ts`) — `eslint-import-resolver-typescript@3.10.1` fails on `@/*` alias imports under `typescript@6.0.3` anywhere, not just in `app/`; `tsc --noEmit` is the compensating check, same as the OC-4 ESLint pin gotcha above. | ✅ |
| OC-12 | Environment/profile switcher | ✅ Done 2026-08-11. Two profiles for now — `mock` (`http://localhost:4000`, provisional until OC-13 exists) and `wireguard` (`http://10.77.0.1:19260`, confirmed) — `public` deliberately excluded, no hostname/posture decided for `xindeler-zuul` yet. `src/config/environments.ts` + `EnvironmentContext.tsx` (persisted via `@react-native-async-storage/async-storage` — non-secret preference, not a session token, so not `expo-secure-store`'s job; default `mock`, the safe choice). Persistent "which server am I on" indicator lives in `app/(tabs)/_layout.tsx` (rendered once, above both the tabs and sidebar branches) rather than inside `Screen` — `ui/` may only import the theme. Full switcher replaces the "Más" tab's placeholder (`src/features/environment/`, the first real `features/` module). Verified on web: badge updates on selection, persists across a full reload. Not verified on iOS/Android simulators this round (web-only pass). **Post-merge review (2026-08-11) found and fixed 3 real bugs**: `setEnvironment()`'s `AsyncStorage.setItem` was unawaited/uncaught — a persistence failure was silent while the UI already showed the new selection, so a later reload would silently revert to `mock`; the context started on the default environment before the persisted value loaded, briefly showing the wrong one on cold start (now gated — nothing renders until the read resolves, matching the font-loading gate in `app/_layout.tsx`); `Screen`'s default-edges `SafeAreaView` double-applied the top safe-area inset on every screen since `EnvironmentBadge` already accounts for it once, above the whole nav shell (fixed the same way the sidebar's own top inset was already fixed). | ✅ |
| OC-13 | **Mock gateway** | ✅ Done 2026-08-13. `tools/mock-gateway/` (plain JS Express, no TypeScript), run via `npm run mock-gateway`. §2 auth: login → TOTP challenge → bearer token, refresh, logout. §3 read surface: status, players, logs, chat, chronicle, audit, plus the §3.1 SSE stream (`status`/`log`/`chat`/`lifecycle`/`audit` events, periodic heartbeats). Scenario engine backs all of it — 6 modes (`normal`, `down`, `draining`, `log_flood`, `auth_expiry`, `stream_drop`) with per-scenario params, exactly one scenario active globally, switching tears down the previous scenario's timers first — driven by `POST`/`GET /mock/scenario` (no auth on that control surface, it's the mock's own knob, not part of the app contract) and started at `normal` on boot so the log generator and `lifecycle` state are live immediately. Every error is `{error:{code,message}}` with a real HTTP status, verified end-to-end incl. `stream_drop` actually dropping an open SSE connection on its own. **§4/§5/§6 landed the same day in a follow-up pass, closing this item completely — no more deferred scope.** §4 lifecycle: `POST /api/v1/server/{start,stop,restart,cancel_shutdown,disconnect_all}` + `POST /api/v1/broadcast`; step-up (`X-Ops-Totp` header, reusing the same fixed TOTP code as login) gates all of §4 except `/broadcast`, which is rate-limited (5s) instead of step-up'd. The draining-countdown engine that originally only backed `POST /mock/scenario`'s `draining` mode was refactored into a shared, parameterized `beginGracefulStop` so the scenario switch and the real lifecycle endpoints run through one engine, not two copies. §5 ORACLE: `GET /oracle/events`, `GET /oracle/presets`, `POST /oracle/stage` + `DELETE /oracle/stage/:id`, `POST /oracle/trigger`, `POST /oracle/enabled` (kill switch) — step-up on every write, reads open. Staging simulates the real "write the file, poll until loaded" round-trip with a ~1.5s delay; sanitization (clamping `intensity`/`radius` when present) is a deliberately generic stand-in, not a real `DmEvent` schema — that schema lives in the game engine, not this mock. §6 ORACLE chat: `POST /oracle/chat` (SSE token stream + terminal `draft` event, no step-up — chat only ever produces a draft, never applies anything) and `GET /oracle/budget` (fixed fake ledger); no LLM behind it, canned reply with rotated fixture drafts. `GET /api/v1/audit` now returns real rows (was `[]` through §2+§3) — every §4/§5 mutation writes one and broadcasts it on the SSE `audit` event, closing the gap the §2+§3 pass left open. **Unblocks OC-14…OC-31** — every route the app's screens need, including the step-up (OC-23) and lifecycle/ORACLE (OC-25, OC-31) UI work, now has a real endpoint to build against. | ✅ |
| OC-14 | Typed API client | ✅ Done 2026-08-13. `src/api/`: `httpClient.ts` (shared request pipeline — auth header injection via an injected `getAuthHeader`, `Idempotency-Key` on every mutation via an injected `generateIdempotencyKey`, 10s timeout via `AbortController`, GET-only retries with backoff (300ms/900ms) on `network_error`/`timeout`/5xx, the `{error:{code,message}}` envelope parsed into `ApiError`), `errors.ts`/`schemas.ts` (zod schema per endpoint), `authApi.ts` (login/totp/refresh/logout) and `readApi.ts` (status/players/logs/chat/chronicle/audit) built on top of `httpClient.ts`, `apiClient.ts` as the composition root (`createApiClient(baseUrl)`, the only file that imports `expo-crypto`/`../auth/sessionStorage`), `index.ts` as the public barrel (`createApiClient`, `ApiClient`, `ApiError`, the read-surface row/response types). Scope is §2+§3 only, matching what OC-13's mock gateway implements. Dependency-injection design: `httpClient.ts` and everything built on it (`errors.ts`, `schemas.ts`, `authApi.ts`, `readApi.ts`) has zero Expo/native imports by construction, so it was verified end-to-end against a running mock gateway via `npx tsx` rather than needing the Expo runtime; only `apiClient.ts` itself — the composition root — needed an in-app smoke check (`npx expo start --web`, clean bundle, no errors mentioning `src/api`). | ✅ |
| OC-15 | Secure session storage | ✅ Done 2026-08-11. `src/auth/`: `SessionStorage` interface (`save`/`read`/`clear`/`getAuthHeader`) with two backends — `SecureSessionStorage.native.ts` (`expo-secure-store`, persists the bearer token + operator/expiresAt) and `.web.ts` (persists only operator/expiresAt to `localStorage` as a non-secret UX marker; the token is deliberately discarded — the real credential is the gateway's `HttpOnly` cookie, invisible to JS by design). `getAuthHeader()` returns `{Authorization: Bearer ...}` on native, `undefined` on web (OC-14's API client will need `credentials: 'include'` for web instead). Metro resolves the platform file automatically; added `moduleSuffixes` to `tsconfig.json` so `tsc` does too (first platform-split file pair in the repo — the gap didn't exist before). Login/TOTP flow itself is OC-16, not built yet — nothing consumes this interface until then. **Post-merge review (2026-08-11) found and fixed 1 real bug**: the native backend wrote the token and the operator/expiresAt metadata as two separate `SecureStore` calls — an interruption between them could leave `read()` (says no session) and `getAuthHeader()` (still returns a Bearer header from the orphaned token) disagreeing; now a single atomic write. | ✅ |
| OC-16 | Login + TOTP screens | ✅ Done 2026-08-13. Two-step flow: `app/(auth)/login.tsx` (username/password → `POST /api/v1/auth/login`, navigates to `/totp` with the returned `challengeId`) → `app/(auth)/totp.tsx` (6-digit code → `POST .../totp`, no manual navigation on success — `AuthContext`'s `status` flip to `'authenticated'` is what `Stack.Protected` in `app/_layout.tsx` reacts to). `AuthContext` (`src/auth/AuthContext.tsx`): boot-time session restore via the `sessionStorage` module (web backend persists a non-secret `operator`/`expiresAt` marker to `localStorage['overlord.session.metadata']`, per OC-15), `login`/`totp`/`logout`, and `handleAuthError(error)` — the mechanism a future data screen calls on a `session_expired`/`unauthorized` `ApiError` to flip the guard back to unauthenticated; no such screen existed yet at the time this row was written (that's OC-18+). **Now wired**: OC-18's `useStatusQuery` calls it on the bootstrap query's error (final-review fix wave, 2026-08-13) — see OC-18's row for the live verification. Route guard: `app/_layout.tsx`'s `RootNavigator` renders `<Stack.Protected guard={status === 'authenticated'}><Stack.Screen name="(tabs)" /></Stack.Protected>` alongside the inverse-guarded `(auth)` branch. Two new `src/ui/` primitives: `TextField` (label + themed `TextInput`) and `Button` (label/onPress/loading/disabled, themed `Pressable`+`ActivityIndicator`). Every error shown is `ApiError.message` verbatim (fixed Spanish fallback only on network failure), matching the mock gateway's `Código TOTP inválido` exactly. Full end-to-end pass on web against `mock-gateway`: wrong TOTP code shows the mock's exact error and stays on `/totp`; correct code (`000000`) flips to `(tabs)` with zero manual navigation; a full browser reload (confirmed via `performance.getEntriesByType('navigation')[0].type === 'navigate'`, not SPA routing) preserves the session with no login screen. ✅ **The "returns you to where you were" property is now independently confirmed** (OC-18's final-review fix wave, 2026-08-13): with OC-18's Status screen shipped as a real data screen, a live browser pass against `mock-gateway` did the full round-trip — logged in, landed on the Status tab with real data showing, opened `/more` and tapped `Cerrar sesión` (dropping to `/login`), logged back in — and confirmed the app lands back on the Status tab (not reset to `/more` or any other default) with live data re-populating correctly (a freshly re-fetched `Iniciado`/`Entidades`/`Chunks` set, not stale leftover values). This closes the item that was left open here. **Dead-end `/totp` bug (found in this same pass) — fixed, not a follow-up**: reachable via a natural session-TTL expiry, a bookmarked/restored deep link straight to `/totp`, or a full reload while on that URL, all with no `challengeId` param — the screen rendered anyway with no way back to `/login` short of manually editing the URL (`Volver` threw "The action 'GO_BACK' was not handled by any navigator"). First attempt tried `(auth)/_layout.tsx`'s `initialRouteName="login"`; A/B tested live and it does **not** fix this — `initialRouteName` only picks the *initial* screen for a fresh stack mount, it doesn't intercept an explicit deep-linked route request for a different screen, so a direct/restored `/totp` load still renders `/totp` first. Actual fix: `app/(auth)/totp.tsx` renders `<Redirect href="/login" />` when `challengeId` is falsy, before any other hook/effect runs. Verified two ways: (1) live browser A/B — loading `/totp` directly with no params now bounces straight to `/login`, while the legitimate `login → totp` navigation (which always carries `challengeId`) is unaffected; (2) independently at the `expo-router` params-context source level, to rule out a flash-redirect on the legitimate path (i.e. confirm `useLocalSearchParams` reads the already-resolved route params synchronously on first render, not a later effect, so a real `challengeId` is never transiently seen as missing). The now-dead `initialRouteName="login"` prop left over from the disproven first attempt was removed in the same fix wave (`app/(auth)/_layout.tsx`). | ✅ |
| OC-17 | SSE transport | ✅ Done 2026-08-13. `src/stream/`: `sseParser.ts` (pure reader-in/events-out generator, zero Expo/native imports, chunk-boundary-safe, skips the mock's `: ping\n\n` keep-alive), `StreamClient.ts` (`createStreamClient(url, {getAuthHeader, fetchImpl})`, `fetchImpl` DI'd the same way `httpClient.ts` already does; exponential backoff `[1s, 2s, 4s, 8s, 16s, 30s]`; a generation counter invalidates any in-flight connect/retry from a previous `start()`/`stop()` cycle so a stale promise can never resurrect a dead connection; idempotent `start()`/`stop()`; `src/api/schemas.ts` gained `LifecycleEventSchema` to type the new `lifecycle` stream event), `StreamContext.tsx` (`StreamProvider`, wrapping the app in `app/_layout.tsx`; `useStreamEvent`/`useStreamStatus` hooks; start/stop gated on `AuthContext`'s `status === 'authenticated'`; resume-on-foreground via `AppState.addEventListener('change', ...)` calling `client.reconnectNow()` on `'active'`). Plus `src/features/connectivity/StreamStatusBanner.tsx` — an always-mounted red banner ("Reconectando con el gateway…") within the authenticated `(tabs)` tree (mounted in `app/(tabs)/_layout.tsx` next to `EnvironmentBadge`, not global to the whole app — the unauthenticated login screen doesn't render it), shown only on `status === 'reconnecting'` (never the ordinary few-hundred-ms `'connecting'` every login passes through). **Post-Task-3 live-verification bug, found and fixed, not a follow-up**: `expo/fetch`'s web export is a bare `globalThis.fetch` reference; calling it detached — exactly the call shape this DI'd client uses (`fetchImpl(url, init)`) — throws `TypeError: Failed to execute 'fetch' on 'Window': Illegal invocation` in Chrome, a `this`-binding requirement invisible to `tsc`, ESLint, and diff-only review since the type signature and call shape are both fully valid; it only surfaced once the app was actually driven in a browser and the console/Network tab were read. Fixed with `expoFetch.bind(globalThis)` passed as `fetchImpl` in `StreamContext.tsx`. **Task 4 live verification (this task)**: full browser pass against `npm run mock-gateway` — logged in (`matias`/`mock`, TOTP `000000`), confirmed the banner absent on `normal` for 13s (spans the periodic unconditional 5s `status` broadcast); reading `tools/mock-gateway/src/routes/stream.js` showed `stream_drop`'s drop timer attaches only to connections established *while* the scenario is already active, so setting the scenario after the page's connection is already open has no effect — the correct test is to arm `stream_drop` **before** the page opens its stream, not after, which the first attempt got wrong and had to be redone; done correctly, the red "RECONECTANDO CON EL GATEWAY…" banner was captured appearing in a screenshot taken ~1s into the reconnect window and gone in the next screenshot once the retry succeeded — a real, observed capture (Chrome screenshots plus console/network reads), not inferred from code reading alone. Zero console errors across the whole pass. ⚠️ Three gaps carried over from the design spec's own Testing section (the third added during the final whole-branch review), still open, all honestly-unverified rather than known-broken: **(1)** foreground resume's code path (`reconnectNow()` clearing a pending backoff timer on `AppState`'s `'active'` transition) is exercised and correct on web (tab visibility fires `'active'` reliably), but whether a real iOS/Android device's socket actually needs this after a genuine background suspension can't be confirmed without a device pass. **(2)** `TextDecoder`'s native availability is assumed, not device-verified — Hermes builds it in as an intrinsic and RN has no JS-level polyfill for it, so it's correct by construction on the current toolchain but not independently confirmed on-device. **(3)** the stream's native Bearer-header auth path (`getAuthHeader()`'s non-empty-header branch, used when there's no cookie to rely on) has never actually been exercised in any verification pass so far — every browser pass to date only ever exercises the empty-header/cookie-credentials path, so the header-injection branch is correct by code reading only, not independently confirmed live. | ✅ |
| OC-18 | Status screen | ✅ Done 2026-08-13. Two parts, per `docs/specs/2026-08-13-status-screen-design.md`: **(1) The TanStack Query foundation** — `src/api/queryClient.ts` (`QueryClient` singleton + a `queryKeys` naming convention covering every read endpoint, not just `status`, so OC-19–21/28 reuse it rather than inventing their own), `src/api/QueryProvider.tsx` (TanStack's own documented React Native recipe: `focusManager` via `AppState`, `onlineManager` via `expo-network`), `src/api/ApiContext.tsx` (`useApi()` — the authenticated `ApiClient` instance, rebuilt on environment switch, mirroring `StreamContext.tsx`'s `useMemo` shape). **Key architecture decision, confirmed explicitly by Matías rather than an autonomous call**: adopt the TanStack Query data layer the original client-architecture spec (§4) called for now, rather than continuing OC-13–17's hand-rolled Context pattern ("tenemos tiempo, y conviene construir algo robusto y fuerte desde el principio" — confirmed a second time after the trade-off was laid out: "vayamos con la segunda opción"). This is genuinely the first point in the codebase where gateway-owned data is read at all — `EnvironmentContext`/`AuthContext`/`StreamContext` are all client-side session/config state, not "data the gateway owns." **(2) The Status screen** (`app/(tabs)/index.tsx` + `src/features/status/`) — bootstraps via one REST `GET /api/v1/status` (`useQuery`), then stays live via the stream's `status` event writing straight into the query cache (`queryClient.setQueryData`) instead of triggering a refetch, making "never a 1 Hz full refresh" true by construction. Up/down header (colored dot + `service`/`health`-derived label), `pending_shutdown` banner, and stat rows (uptime, version, players online, tick time, entity/chunk counts, started-at). ⚠️ **For a future reader**: `queryKeys`/`ApiContext`/`QueryProvider` are foundation infrastructure built for every future data screen (OC-19 players, OC-20 logs, OC-21 chat, OC-28 audit) to reuse — they are not status-specific, even though this is the row that introduced them. **Final-review fix wave (2026-08-13)** found and fixed 9 issues before merge, none requiring a design change: (1) the bootstrap query's error never reached `AuthContext.handleAuthError` — a REST 401 (session expired/unauthorized) left the operator stuck inside `(tabs)` showing error text instead of dropping to `/login`; now routed via a `useEffect` on `query.error`. (2) TanStack's default `retry: 3` stacked on top of `httpClient`'s own `requestWithRetry` (already up to 3 attempts × 10s timeout), compounding to ~2 minutes of undifferentiated "Cargando…" on an unreachable gateway, and blindly retrying errors `httpClient` had already classified as non-retryable (401/403/404); fixed via `defaultOptions.queries.retry: false` + `staleTime: 30_000` (the stream already covers live updates, so window-focus refetching adds little). (3) `ApiContext.tsx`'s destructive `queryClient.clear()` effect was keyed on `[api]`, a `useMemo`-derived object identity React only documents as a performance hint, not a stable-reference guarantee — repointed to `[environment.baseUrl]`, the actual primitive that can never change spuriously. (4) this row and OC-16's carried-over verification gap (below) were closed. (5) a documented, accepted, self-healing race between the bootstrap fetch and a stream push (no revision-number scheme needed — self-heals within one broadcast interval). (6) the health-status dot was missing `dark:` variants (`dark:bg-night-accent-cyan`/`dark:bg-night-danger`), inconsistent with every other color-conditional className in the codebase — fixed in both the component and the design spec, which had the same gap. (7) `formatStartedAt` threw a `RangeError` on a malformed `started_at` (the schema only validates `z.string().nullable()`, nothing about date-ness) — with no `ErrorBoundary` anywhere in the app, this would have unmounted the whole app; now guards `Number.isNaN(date.getTime())` and returns `'—'`, plus hoists the `Intl.DateTimeFormat` instance to module scope instead of rebuilding it every render. (8) `queryClient.setQueryData(queryKeys.status, ...)` type-checked against `unknown` since `queryKeys.status` is a plain untyped array; migrated `status` specifically to TanStack v5's typed `queryOptions()` helper (deliberately not extended to `players`/`logs`/etc. — premature for endpoints with no consumer yet). (9) a one-line comment noting `ApiContext.tsx` and `useStatusQuery.ts` only reach the same `queryClient` instance today because it's a module-level singleton and `ApiProvider` sits above `QueryProvider` — a future non-singleton `QueryProvider` would silently break the environment-switch cache clear otherwise. **Live verification**: full browser pass against `npm run mock-gateway` (logged in `matias`/mock TOTP `000000`, confirmed real Status data render, no console errors) plus the OC-16 round-trip check (see that row) plus three independent session-invalidation passes (mock-gateway process restarted mid-session to wipe its in-memory session map while the client's locally-stored session still believed itself valid, isolating the REST-401 code path from `AuthContext`'s own boot-time local-expiry check) — all three runs correctly dropped the app to `/login` rather than leaving it stuck, confirming finding (1) above end-to-end. This exercises `handleAuthError`'s `unauthorized` branch specifically (a token the mock no longer recognizes at all); the `session_expired` branch (a token the mock still recognizes but whose TTL has passed, e.g. via the mock's own `auth_expiry` scenario) shares the exact same `handleAuthError` code path and wasn't separately exercised in this pass — a triviality given both codes hit the identical `||` check, not a real gap, but named precisely here rather than implying both were independently verified. One caveat: the browser automation tool's network-request log proved unreliable across full-page navigations in this environment (repeatedly reporting a stale cached `200` for the bootstrap request even after a confirmed session wipe), so the exact HTTP status code of that specific request could not be independently read off the Network tab — the 401 is confirmed structurally instead, by reading `tools/mock-gateway/src/middleware/auth.js` (a missing/invalid session token always yields `401 unauthorized`) combined with the consistently-observed, correct end-to-end behavior across all three passes. `npx tsc --noEmit`, `npm run lint` (0 errors — pre-existing `import/no-duplicates` resolver warnings only, unrelated to this change), and `npm run format:check` all pass clean. | ✅ |
| OC-19 | Players screen | ✅ Done 2026-08-13. Per `docs/specs/2026-08-13-players-screen-design.md`. **Task 1**: extracted `useAuthErrorRouting` (`src/auth/useAuthErrorRouting.ts`, commit `d1925b8`) out of OC-18's inline `useEffect`-on-`query.error` pattern into a reusable hook — see that commit's own message for the extraction details, not repeated here. **Task 2** (this row's screen): `src/features/players/usePlayersQuery.ts` (`useQuery` on `queryKeys.players` against `api.read.getPlayers()`, wired through `useAuthErrorRouting` exactly like `useStatusQuery`), `PlayerRow.tsx` (alias + 8-char uuid prefix), `PlayersScreen.tsx` (header showing `Jugadores (${count})`, `FlatList` with a `RefreshControl` wired to `query.refetch`, an `Empty` state while `query.data` is `undefined`, and a `ListEmptyComponent` for a genuinely empty roster) — wired into the Jugadores tab via `app/(tabs)/players.tsx`. **Not in scope, by design, not oversight**: `players` has no SSE stream event — the contract's stream event table (§3.1) lists exactly `status`/`log`/`chat`/`lifecycle`/`audit`, no `players` — so this screen is REST-plus-pull-to-refresh only, which is *why* the backlog line asks for pull-to-refresh here specifically (OC-18's Status screen has none, precisely because it doesn't need one); a player joining or leaving mid-session simply won't appear until the operator pulls to refresh. `npx tsc --noEmit`, `npm run lint` (0 errors — the 3 new warnings are the same pre-existing `import/no-duplicates` resolver warning every other file in this repo already has, not new problems), and `npm run format:check` all pass clean. **Live verification**: full browser pass against `npm run mock-gateway` confirmed the Jugadores tab renders `Jugadores (5)` with all 5 fixture players (Kaelith, Voss, Ember, Doran, Nyx) each showing the correct 8-character uuid prefix, and the Network tab showed exactly one `GET /api/v1/players` firing on page load. ⚠️ **Pull-to-refresh gesture itself is unconfirmed — corrected root cause (2026-08-13)**: three independent techniques (mouse-wheel scroll at the list's top boundary, a simulated touch-drag via `left_click_drag`, and synthetic `TouchEvent` dispatch via JS) all failed to trigger a second `GET /api/v1/players`. This was first written up as a browser-automation tooling limitation; that explanation was wrong and has been corrected after reading `react-native-web`'s actual source. `RefreshControl`'s web implementation (`node_modules/react-native-web/src/exports/RefreshControl/index.js`) destructures out `onRefresh`/`refreshing`/`tintColor` entirely and renders a bare `<View>` — there is no gesture-responder path on web at all, so **no technique, automated or a real human hand on a real trackpad, can ever fire a refetch via pull-to-refresh in a browser build of this app**. This is a platform limitation, not an automation gap: web has no explicit refresh affordance; the only refresh paths are incidental (tab blur/focus, or navigating away and back at the wide breakpoint, both gated on the 30s staleTime). `onRefresh={query.refetch}` is still the correct, proven `useQuery`/`refetch` wiring for native, where `RefreshControl` is a real native component — that half remains open pending a device/simulator pass, which is the only way this specific screen's pull-to-refresh can be verified at all. Revisit whether web needs its own visible refresh affordance (a button) as a deliberate follow-up, not a verification gap. **Session-invalidation pass — actually performed (final whole-branch review fix wave, 2026-08-13)**: the design spec's Testing section named this as required but it had only been reasoned through by code inspection, not exercised. Live-verified against a running `npm run mock-gateway`, using the same technique as OC-18's own final review: logged in, landed on Status with real data, killed and restarted the mock-gateway process (wiping its in-memory session map while the client's locally-held session still believes itself valid). First pass navigated Status→Jugadores before the query fired, which ended up exercising `usePlayersQuery`'s routing instead of the intended target; corrected by repeating the kill/restart and, this time, reloading the page while still on the Status route (no intervening navigation) so `useStatusQuery`'s own bootstrap fetch is what hits the wiped session. Both passes dropped the app cleanly to `/login` with a normal, unstuck login form. ⚠️ **This end-to-end result doesn't cleanly isolate which code path caused it, and the re-review that caught this is right not to let that slide**: `/api/v1/stream` is behind the same `requireAuth` the REST reads are, and `StreamContext`'s own `onUnauthorized` → `handleAuthError` wiring (OC-17) fires on a stream 401 too — so a wiped session flips auth status via *either* `useStatusQuery`'s routing *or* the stream client's, and a clean drop to `/login` can't tell you which one actually fired first. The live pass confirms the end-to-end outcome is correct; it does not, on its own, isolate `useAuthErrorRouting`'s extraction as the specific cause, the way the sentence above originally overclaimed. The stronger evidence that the extraction is behavior-preserving is the direct code comparison from Task 2's own review (`useStatusQuery.ts`'s retrofitted effect is byte-identical to its pre-extraction form: same guard, same dependency array) — that comparison is what actually rules out a regression here, not this specific live pass. | ✅ |
| OC-20 | Logs screen | ✅ Done 2026-08-13. Three files, per `.superpowers/sdd/2026-08-13-logs-screen-implementation/`. **What actually ships — final state after a full whole-branch review fix wave, not the intermediate attempts below**: **(1) `src/features/logs/useLogsQuery.ts`** — bootstraps via `GET /api/v1/logs?limit=200` (`useQuery`), then buffers incoming `log` stream events in a `useRef` array and flushes them into the query cache on a fixed 150ms `setInterval`, one `setQueryData` per flush rather than one per event — a `log_flood` can push up to 20 events/sec, and a per-event `setQueryData` (the pattern `status`/`players` use) would mean 20 array replacements/sec, exactly the "not smooth under a flood" failure this row calls out — plus a flush-on-unmount so the last (up to) 150ms of buffered lines aren't silently dropped when the screen unmounts. Every line, bootstrap and stream alike, is stamped with a monotonic client-side `_seq` the moment it enters the buffer (bootstrap in `queryFn`, stream at push time via a shared `useRef` counter), and `LogsScreen.tsx`'s `keyExtractor` keys on `_seq` instead of array index — index-based keys shifted on every flush once the 500-entry cap started dropping the oldest lines, remounting every row instead of `LogRow`'s `memo` bailing out. That cap (`MAX_BUFFERED_LOGS`, `.slice(-500)`, matching the mock's own `state.logBuffer` cap) is enforced both in the flush's own `setQueryData` and via a `select` on the `useQuery` call, so the bootstrap path is capped too — previously only the flush path was. The query disables `refetchOnWindowFocus`/`refetchOnMount` for itself specifically (not the global `QueryClient` defaults): this cache entry is stream-owned after its initial fetch, and an incidental window-focus/remount refetch would otherwise silently replace up to 500 accumulated stream-appended lines with just the 200-line REST snapshot. The one refetch this hook *does* trigger on purpose: on `useStreamStatus()`'s specific `!== 'open' -> 'open'` transition (a stream reconnect after a drop), it calls `queryClient.invalidateQueries` to backfill recent history across the outage gap — a deliberate, rare replace, not the incidental kind suppressed above. New `warning` color token added to `src/ui/theme.ts` (amber, for the WARN level badge) — the theme previously only had `danger` for ERROR. **(2)** `LogRow.tsx` (memoized, level-colored badge — INFO neutral/WARN amber/ERROR red/DEBUG muted — long-press copies the formatted line via `expo-clipboard`) and `LevelFilter.tsx` (chip row: `Todos` + one chip per level, `Set<string> | null` selection model where `null` means "all"; exports `KNOWN_LEVELS` so the screen can special-case a level with no chip). **(3)** `LogsScreen.tsx` — follow-tail state machine (`followTail` boolean, `scrollToEnd` on new *filtered* data while following) plus `FlatList` flood-tuned performance props (`maxToRenderPerBatch={20}`, `windowSize={10}`, `removeClippedSubviews` on native only) — wired into `app/(tabs)/logs.tsx` as `LogsRoute`. The level filter treats an unrecognized level (the schema's `level` is a bare string, not an enum, per OC-14's own deliberate design) as always-visible regardless of which chips are toggled off — `!KNOWN_LEVELS.has(line.level) \|\| selectedLevels.has(line.level)` — since there's no chip an operator could use to bring an unanticipated level back. <br><br>**Scroll-disengage detection: three attempts, only the third holds under a live flood.** Ships **direction-based**: `handleScroll` compares each event's `contentOffset.y` to the previous one (a `useRef`) and only disengages `followTail` when the list moved *up* past the 50px bottom threshold — a programmatic `scrollToEnd` only ever increases `contentOffset.y`, so a genuine user scroll-up is structurally distinguishable with no timing assumption at all. Kept briefly here for institutional memory, not because either still ships: **attempt 1** (Task 3's own live verification, originally logged as "Done 2026-08-14" before the date was corrected to match every commit) used a boolean `isAutoScrollingRef` cleared by an independent 400ms `setTimeout` per call — under sustained flood, `scrollToEnd` fires roughly every 150ms while `followTail` is true, and each call scheduled its own timer without clearing the previous one, so the ref could flip back to `false` mid-flood and reopen the original disengage race intermittently. **Attempt 2** (a same-day fix round) replaced the boolean with a timestamp watermark (time-since-last-auto-scroll < 400ms) — this closed attempt 1's race, but introduced a worse one only this pass's live re-verification caught: the 150ms flush interval is *faster* than the 400ms settle window, so under any sustained flood the watermark never went stale — the guard was permanently "on," and a real user scrolling up during a flood could not disengage follow-tail *at all*, defeating the exact interaction the design spec names as the reason auto-disengage exists. Direction-based detection (attempt 3) has no timing dependency, so it has no equivalent failure mode. <br><br>**A fourth bug, found only by combining two of this wave's own fixes and then actually driving them together in a browser**: adding `filteredLines` to the auto-scroll effect's dependencies (so a filter change re-snaps to the bottom immediately, correct and required on its own) interacts with a level-filter toggle's wholesale `FlatList` `data`-array swap — RN Web's scroll container can transiently report an offset near the top before settling back down, which the new direction-based check reads as a user scroll-up and incorrectly disengages `followTail`, even though the view is (and stays) pinned to the live bottom. Reproduced live, intermittently — roughly half of single-chip toggles during an active flood, and reliably under a rapid multi-chip toggle burst. Fixed with a second, narrowly-scoped guard (`suppressScrollCheckRef`): armed only when the auto-scroll effect fires *without* `lines` itself having changed (a filter or re-engage toggle — a discrete, once-per-user-action event, never the 150ms flush cadence), and cleared once a scroll event shows the list has genuinely settled back near the bottom (not on the first non-up tick, which an initial version of this guard used and which a rapid multi-chip-toggle burst could still slip past via more than one stacked transient dip). Scoping the guard to filter/toggle changes only — never armed on a plain stream-driven append — is what keeps it from reopening the exact "guard permanently on during flood" failure attempt 2 above had: it is armed once per discrete user action, not once per flush. <br><br>**Live verification (this pass)**, specifically targeting the interaction the two prior passes never tested: full browser pass against `npm run mock-gateway`. Confirmed normal rendering under `normal`. Drove `log_flood` (`{"logsPerSec":20}`, the mock's own default) and **manually scrolled up while the flood was actively running** — `followTail` correctly disengaged ("Siguiendo" → "Seguir") and, critically, *stayed* disengaged under continued flood pressure rather than being fought back on, closing the exact gap attempt 2's "~40 seconds, zero user interaction" pass had left uncovered by construction. Tapped the toggle back on mid-flood — snapped to the bottom and stayed engaged through continued live ticks. Toggled level filters mid-flood repeatedly — single chips, a full reset via `Todos`, and a rapid three-chip burst — confirming `followTail` no longer spuriously disengages in any of those cases (the fourth bug above, caught during this exact pass), with the list narrowing/resetting correctly and no perceptible jank in every case. Zero console errors throughout. Reset to `normal` afterward. `npx tsc --noEmit`, `npm run lint` (0 errors — the same pre-existing `import/no-duplicates` resolver warnings every file in this repo has), and `npm run format:check` all pass clean. <br><br>**Fifth round (2026-08-13, human-authorized extra pass)**: a re-review after the fourth round's fix wave surfaced 3 more residual issues, all fixed. (1) `seqRef` (a `useRef(0)` scoped to the `useLogsQuery` hook instance) restarted at 0 on every `LogsScreen` remount, while the TanStack query cache — a module-level singleton, cleared only on an environment switch, never on logout or remount — kept its previously-stamped `_seq` values; a remount within the cache's `gcTime` (5 min) with `refetchOnMount: false` in effect (so `queryFn` never re-runs to reseed the counter) collided new stream-pushed `_seq`s with the still-cached ones, reproducing the exact duplicate-key bug `_seq` exists to prevent. Fixed by hoisting the counter to a module-level `let nextLogSeq = 0`, tied to the JS module's lifetime rather than any component mount. (2) The `suppressScrollCheckRef` filter-change guard inferred "was this a filter change" indirectly from whether the `lines` reference changed across the effect re-run, which both wrongly armed on mount (the ref's initial value equals the first render's `lines`, reading as "unchanged") and could fail to arm when a stream flush landed in the same tick as a filter toggle. Replaced with a `prevSelectedLevelsRef` that tracks `selectedLevels` directly, so the guard arms exactly when a filter selection genuinely changes — simpler than the code it replaced, not just more correct, since it removes the mount-inference edge case by construction rather than special-casing it. (3) `select: (rows) => rows.slice(-MAX_BUFFERED_LOGS)` was a fresh arrow function every render, defeating TanStack's reference-based `select` memoization and re-walking up to 500 objects every render under flood's ~7 renders/sec; hoisted to a module-level named function `capBufferedLogs`. All three hand-traced and live-verified: logged out and back in with cached log lines present (cache persisted through logout as expected, no duplicate-key console warning, sequence continued rather than restarting); drove `log_flood` and rapidly toggled level-filter chips while the flood was active (`followTail` stayed engaged throughout) and confirmed a genuine manual scroll-up still disengages it correctly. `npx tsc --noEmit`, `npm run lint`, and `npm run format:check` all pass clean. | ✅ |
| OC-21 | In-game chat viewer | ✅ Done 2026-08-14. Three tasks, per `docs/superpowers/plans/2026-08-13-chat-viewer-implementation.md` and `docs/specs/2026-08-13-chat-viewer-design.md`. **(1) `src/features/chat/useChatQuery.ts`** — bootstraps via `GET /api/v1/chat` (`useQuery`), then appends each incoming `chat` stream event straight into the query cache with a plain `queryClient.setQueryData(queryKey, (old) => [...(old ?? []), message])`, one array replacement per event. Deliberately *not* OC-20's buffer-and-flush pattern: the gateway API contract has no chat-flood scenario (the mock pushes at most one message every 15s, versus logs' up to 20/sec), so a per-event append is the right scope — reaching for `log_flood`'s batching machinery here would be solving a problem this screen doesn't have. Same task also extracted **`src/ui/FollowTailToggle.tsx`** (the `{followTail, onToggle}` pill button — "Siguiendo"/"Seguir" — OC-20's `LogsScreen.tsx` had built inline) and retrofitted `LogsScreen.tsx` to consume the shared component instead, so both screens' follow-tail affordance stay pixel- and behavior-identical by construction rather than by convention. **(2)** `ChatMessageRow.tsx` (memoized; author/timestamp header line + message body, no level badge or long-press-copy — chat has neither concept) and `ChatScreen.tsx` — a simpler follow-tail than Logs': no flood-tuned `FlatList` props, no filter-change suppression guard, since chat has no filters and no flood to fight; keeps OC-20's direction-based scroll-disengage detection (`contentOffset.y` decreasing vs. the previous event, `useRef`-tracked) since that fix's underlying rationale — a programmatic `scrollToEnd` only ever increases offset, so a genuine user scroll-up is structurally distinguishable with no timing assumption — applies identically here even without a flood to stress it. **(3)** Route wiring: `app/(tabs)/chat.tsx` (`ChatRoute` — `Screen` + `ChatScreen`, the same one-line pattern every other tab route follows) and a new `chat` entry in `app/(tabs)/_layout.tsx`'s shared `DESTINATIONS` array (`chatbubbles-outline` icon, label "Chat"), positioned between Logs and ORACLE rather than buried in "Más": the client-architecture spec (`docs/specs/2026-08-09-client-architecture-design.md`, "the log tail and the ORACLE chat — the two things this app *is*") names live log tailing and ORACLE chat as this app's core purpose; in-game chat is close kin to both (live, read-only, high-attention during play) and earns a primary slot on the same basis, not a secondary one. `Destination`'s `href`/`routeName` union types gained exactly the `'/chat'`/`'chat'` members the new array entry uses; `TabsLayout`/`SidebarLayout` map over `DESTINATIONS` generically, so no other `_layout.tsx` change was needed for the tab to appear on both the phone tab bar and the wide sidebar. **Live verification**: full browser pass against `npm run mock-gateway`, logged in `matias`/mock TOTP `000000`. First pass — against a mock-gateway process and browser tab both left running continuously since Tasks 1–2's own verification — showed the Chat tab, icon, and position correctly, but the message list appeared frozen across multiple 20+ second observation windows despite `GET /api/v1/chat` fetched directly confirming the server's `chatHistory` was actively growing. Root-caused with a temporary diagnostic log in the mock's `broadcast()` (`tools/mock-gateway/src/sse.js`, reverted before commit — `git diff` confirms no trace left) plus a raw low-level SSE read bypassing the app entirely: the long-lived browser tab's stream connection was a stale artifact of that earlier, hours-old dev session and simply wasn't receiving `chat` broadcasts, while newer connections on the same server were — not a bug in this branch's code. Restarted the mock gateway and re-logged in for a clean session: confirmed the message list grew live with zero manual interaction (5 → 9 → 13 messages across three ~20s waits, follow-tail engaged throughout, no page interaction), the Chat tab renders between Logs and ORACLE with the chat-bubble icon in both the phone-width tab bar and the wide sidebar, and — demonstrated repeatedly during the initial (stale-connection) pass on this same unmodified code, before the root-cause digression — that a manual scroll-up disengages follow-tail ("Siguiendo" → "Seguir") and stays disengaged, and that tapping the toggle re-engages it and snaps back toward the live tail. `npx tsc --noEmit`, `npm run lint` (0 errors — the same pre-existing `import/no-duplicates` resolver warnings every file in this repo has), and `npm run format:check` all pass clean. | ✅ |
| OC-22 | Connectivity UX | ✅ Done 2026-08-14. Per `docs/specs/2026-08-14-connectivity-ux-design.md` / `docs/superpowers/plans/2026-08-14-connectivity-ux-implementation.md`, 4 tasks. **(1) Detection + Android action** (`src/features/connectivity/`): `gatewayErrorMessage.ts` — `isLikelyVpnDown(environmentId, error)` is true only when `environmentId === 'wireguard'` *and* the error is an `ApiError` with code `network_error`/`timeout` (`VPN_SUSPECT_CODES`); `gatewayErrorMessage()` swaps in the literal `VPN_DOWN_MESSAGE` ("No llego al gateway — ¿está la VPN prendida?") on a match, else passes `error.message` through unchanged. This is deliberately the only signal available: per §5.4's own framing (`docs/specs/2026-08-09-client-architecture-design.md`), the WireGuard-only Phase-1 posture means a dead tunnel fails at the network layer with no gateway response to inspect — environment profile plus the two REST-layer failure codes `httpClient.ts` already classifies is the entire reliable signal that exists, no richer network-layer probe was available to build against. `openVpnSettings.ts` — `canOpenVpnSettings()` is `Platform.OS === 'android'` only; `openVpnSettings()` calls `Linking.sendIntent('android.settings.VPN_SETTINGS')`. iOS gets no deep link: researched during design, there is no reliable/documented iOS URL scheme to jump to VPN settings (unlike Android's real, documented intent), so this stays Android-only rather than shipping a link that silently fails on iOS. `VpnSettingsButton.tsx` renders `null` when `canOpenVpnSettings()` is false. `GatewayErrorEmpty.tsx` composes all of it into one reusable block (`Empty` + the VPN-conditional button) for screens with a full empty-state slot. **(2)–(3) Six call sites**: the four data screens' first-load empty/error state — Status, Players, Logs, Chat (`GatewayErrorEmpty` swapped in for the old generic error empty state) — plus login and TOTP (`app/(auth)/login.tsx`, `app/(auth)/totp.tsx`, same `gatewayErrorMessage`/`isLikelyVpnDown`/`VpnSettingsButton` trio inlined since neither screen uses `Empty`); Chat's background-refetch error banner is a seventh surface reusing `gatewayErrorMessage` (no button, same thin-strip shape as the stream banner). **(4) `StreamStatusBanner.tsx`** — the always-mounted top-of-app reconnect strip now checks `environment.id === 'wireguard'` directly (not `isLikelyVpnDown`, since `StreamClient` exposes no error code to check) and swaps its text to the same `VPN_DOWN_MESSAGE` constant on that profile; no button here, this is a thin strip with no room for one. Every surface reuses the one `VPN_DOWN_MESSAGE` string constant — never retyped. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing `import/no-duplicates` resolver warnings every file in this repo has), `npm run format:check` all pass clean across all 4 tasks. **Live verification**: a pass run by the controller after Task 4 against a real (temporarily reachable, then killed) second gateway instance on the `wireguard` profile confirmed the VPN message on all four data screens (Status/Jugadores/Logs/Chat), the login screen, and the stream-reconnect banner, with the `mock` profile confirmed unaffected throughout; the TOTP screen (code-identical to login) was not independently live-tested, only reviewed. | ✅ |

---

## Phase 2 — Lifecycle control (first write capability)

Maps to NH-75 P2. **Blocked on the gateway shipping its lifecycle routes**, which is in turn
blocked on the small additive engine PR in `xindeler-new-horizon`.

| ID | Item | Notes | Status |
|---|---|---|---|
| OC-23 | Step-up auth flow | ✅ Done 2026-08-14. Two tasks, per `.superpowers/sdd/2026-08-14-step-up-auth-implementation/`. **(1)** `src/api/httpClient.ts`'s `RequestOptions` gained an opt-in `stepUpCode?: string` field that, when present, attaches an `X-Ops-Totp` header to the request — additive only, no existing call site touched. **(2)** `src/auth/StepUpContext.tsx` (`StepUpProvider`/`useStepUpAuth()`) and `src/auth/StepUpPrompt.tsx` (the TOTP-entry modal, matching `app/(auth)/totp.tsx`'s login TOTP-screen styling). `requestStepUp(options?: { forceFresh?: boolean })` returns a cached code immediately if one was obtained within the last 90 seconds, otherwise shows the modal and resolves/rejects every concurrent caller together off one shared waiter queue. 90s, not an arbitrary longer "a few minutes": a client-side cache outliving a real TOTP code's own validity (~30s step + clock-skew tolerance) narrows, but does not eliminate, the risk of the cached value having expired server-side by the time it's reused — see `docs/specs/2026-08-14-step-up-auth-design.md`. Mounted via `StepUpProvider` in `app/(tabs)/_layout.tsx` only (not the root layout) — step-up is exclusively an authenticated-session concern, and `(tabs)` is where every future destructive-action screen will live. **No real destructive-action screen consumes this yet** — OC-24 through OC-27 will; this ticket ships only the mechanism itself. The retry-on-403 path — catching `403 invalid_totp`/`step_up_required` and calling `requestStepUp({ forceFresh: true })` before retrying the mutation — is load-bearing for this design, not optional polish, since no client-side cache window can be guaranteed to align with a real TOTP implementation's server-side validity; whoever implements OC-24+ must build that retry, not just call `requestStepUp()` once. **Live verification**: a temporary test trigger (button + handler + its imports) was added to `app/(tabs)/more.tsx`, exercised against the mock gateway's already-implemented, real step-up-gated `POST /api/v1/server/disconnect_all`, then fully removed before commit (`git diff` on that file shows zero changes). All 5 checks passed: (1) tapping the trigger shows the modal (title, 6-digit field, Confirmar disabled until 6 digits, Cancelar link); (2) entering the real mock TOTP `000000` closes the modal, logs `200`, and a new `warn`-level "Todos los jugadores fueron desconectados" line appears in Logs — proof the header reached and was accepted by the real server, not a client-side stub; (3) tapping the trigger again immediately reuses the cached code — no modal, still `200`, a second Logs line; (4) tapping the trigger and choosing Cancelar fires no `fetch` at all (no new console line, no new Logs entry) — the promise rejected before reaching the request; (5) forcing a fresh prompt (`forceFresh: true`) with the wrong code `111111` logs `403` — proof a bad code is rejected server-side, not silently accepted client-side. `npx tsc --noEmit`, `npm run lint` (0 errors, the same pre-existing `import/no-duplicates` resolver warnings every file in this repo has), and `npm run format:check` all pass clean. | ✅ |
| OC-24 | Confirm-by-typing sheet | ✅ Done 2026-08-14. Per `.superpowers/sdd/2026-08-14-confirm-by-typing-implementation/`, one task: `src/ui/ConfirmByTypingSheet.tsx` — `ConfirmByTypingSheet({visible, word, description, onConfirm, onCancel})`, a bottom-sheet `Modal` (`Button`/`TextField` composed exactly as they already exist) requiring the operator to type `word` into a `TextField` before `Button`'s `disabled` styling lifts off "Confirmar". Match is exact string equality (`typed !== word`) — case-sensitive and no trim, deliberately: the whole point of typing the verb is to force a conscious, precise action, not a case-insensitive "close enough" that a hurried operator (§ "phones in pockets") could half-type past. Both `onConfirm` and `onCancel` (including the backdrop `Pressable` and the `Modal`'s own `onRequestClose`) clear `typed` back to `''` before firing, so re-opening never shows a stale prior entry. Plain controlled component, not a hook: unlike OC-23's `useStepUpAuth` (which caches a code across time and multiplexes concurrent async callers onto one waiter queue), there is no async result to cache and no concurrent-caller problem to solve here — a promise-returning wrapper would be generalizing from zero real call sites, since no destructive-action screen exists yet to prove out that shape. **No real destructive-action screen consumes this yet** — OC-25/26 will. **Live verification**: a temporary test trigger (button + local `visible` state + handlers logging which callback fired + their imports) was added to `app/(tabs)/more.tsx`, driven live against `npx expo start --web` (no mock-gateway needed, the component makes no network calls), then fully removed before commit (`git diff` on that file shows zero changes). All 6 checks passed: (1) tapping the trigger shows the sheet sliding up with the description and a disabled/dimmed "Confirmar"; (2) typing `resT` (partial, mixed case) leaves "Confirmar" disabled; (3) typing the exact `RESTART` enables "Confirmar", tapping it logs the confirm case and closes the sheet; (4) re-opening shows the field empty, not still `RESTART`; (5) typing `RESTART` again and tapping "Cancelar" logs the cancel case (not confirm) and closes the sheet; (6) re-opening, typing `RESTART`, and tapping the backdrop above the sheet logs the cancel case again. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing `import/no-duplicates` resolver warnings every file in this repo has), `npm run format:check` all pass clean. | ✅ |
| OC-25 | Lifecycle state machine UI | ✅ Done 2026-08-14. Per `docs/specs/2026-08-14-server-lifecycle-design.md`, `src/features/status/useLifecycleState.ts`: `LifecycleState = 'running' \| 'draining' \| 'stopped' \| 'starting'`. There's no `GET /lifecycle` bootstrap endpoint (only the `lifecycle` SSE event) — before any real event has arrived, `deriveFromStatus()` computes a best-effort initial state from the `Status` snapshot `StatusScreen` already fetches (`pending_shutdown` → `'draining'` with its `seconds_left`; `service: 'active'` → `'running'`; else `'stopped'`, folding `service: 'failed'` in with no dedicated lifecycle state for it). One known, accepted ambiguity: a derived guess can never distinguish `'stopped'` from `'starting'` — `status` reads `'inactive'` for both — self-corrects the moment a real `lifecycle` event lands, since once any live event has arrived it wins outright over the derived guess for the rest of the session. Extends `StatusScreen` rather than a new tab — the lifecycle indicator reuses the existing `pending_shutdown` danger-box treatment, now driven by this hook instead of reading `status.pending_shutdown` directly, behavior-preserving for an operator who never touches OC-26's new buttons. **Cancel stays reachable through the whole drain**: `'draining'` renders only Cancelar (plus Desconectar a todos), never obscured by a confirm sheet or disabled while the countdown runs — live-verified (OC-26's Step 3, check 2) by cancelling mid-countdown and confirming an immediate revert to `'running'`. **Final-review fix wave, same day:** `useLifecycleState` now *reconciles* `live` against `status` instead of trusting a real `lifecycle` event forever — `status` is pushed on every change plus a 5s heartbeat, and a dropped `lifecycle` event (e.g. mid stream-reconnect) could otherwise strand `live` at `'draining'` after the drain actually ended, or at `'starting'`/`'stopped'` while `status` shows an active service. `live` is cleared (falling back to `deriveFromStatus`) only on an active contradiction between the two; a non-contradicting `live` still wins, unchanged. The header's primary status text is now driven by this reconciled `state` via a `lifecycleLabel()` helper covering all four values, not `status.service` directly — the `'draining'` danger-box (with its `reason` text) stays as a second, richer indicator alongside it. `Detener` is now also visible during `'starting'` (previously no action button existed for it) so a stalled start has an abort path. Both `useLifecycleState.ts` and `StatusScreen.tsx` adjust state during render, guarded against a stored previous value, rather than in a `useEffect` that calls `setState` unconditionally — the latter is flagged as an error by this repo's React Compiler ESLint config (`react-hooks/set-state-in-effect`). **Second-round safety review, same day (2026-08-14) — one BLOCKING finding, fixed:** the reconciliation added in the first fix wave (above) cleared a `'draining'` `live` on a single contradicting `status` snapshot (`pending_shutdown === null`), exactly like it cleared `'stopped'`/`'starting'` — but that symmetry was wrong. `status` and `lifecycle` are two independently-timed data paths (gateway contract §3.1: "the gateway polls the game server on **one** internal timer" and fans it out as `status`, separate from the `lifecycle` push) — against a real gateway, `status`'s view of `pending_shutdown` can lag a genuine drain by up to a poll cycle, or never populate at all for a gateway-orchestrated drain, and a single lagging snapshot clearing `'draining'` would delete the Cancelar button for as long as the lag lasts, directly violating invariant 11. Invisible in mock testing because `tools/mock-gateway/src/scenarios.js` broadcasts `lifecycle` and `status` atomically on the same tick, so the two sources can never actually disagree there. Fixed by removing the `'draining'`-clearing branch from `contradicts()` entirely — `lifecycle` is now the sole source of truth for both entering and leaving `'draining'`; the `'stopped'`/`'starting'` clearing branch is unchanged since that direction only ever adds actions back, never removes an abort path. Verified by code tracing only (the mock cannot reproduce the disagreement), not a live pass — documented as such rather than overclaimed. Asymmetry documented in both `useLifecycleState.ts` and `docs/specs/2026-08-14-server-lifecycle-design.md`'s reconciliation section. | ✅ |
| OC-26 | Start / stop / restart / disconnect-all | ✅ Done 2026-08-14. Per `.superpowers/sdd/2026-08-14-server-lifecycle-implementation/`, three tasks landing together as this ticket's other half. **Write API** (`src/api/writeApi.ts`, wired into `apiClient.ts`): `startServer`, `stopServer({mode, seconds, reason?})`, `restartServer({seconds, reason?})`, `cancelShutdown`, `disconnectAll` — all POST `/api/v1/server/*`, all take a step-up code. Stop/restart always call with `mode: 'graceful'`/`seconds: 30`, no reason — no immediate-mode toggle, seconds picker, or reason field this ticket (YAGNI; revisit if a real incident-response need shows up). ⚠️ Restart is orchestrated gateway-side (`Restart=on-failure` means a graceful stop stays stopped, NH-75 §1.3) — the app only triggers it and renders OC-25's state machine through the automatic `draining → stopped → starting → running` sequence, never faking it with a stop+start of its own; live-verified (check 5) with no button tap at the `stopped`/`starting` midpoints. **`useDestructiveAction<T>`** (`src/features/status/useDestructiveAction.ts`): wraps a call in `requestStepUp()` (OC-23) → the call → on a `403 invalid_totp`/`step_up_required`, one retry via `requestStepUp({forceFresh: true})` → the call again; a cancelled step-up (`isStepUpCancelled`) clears silently with no error state, any other failure sets `error` for inline display. Returns `{run, pending, error}`; no internal re-entrancy lock — `Button`'s own `isDisabled = disabled \|\| loading` (fed `loading={xAction.pending}` on every action button) is what actually blocks a double-tap from firing two concurrent mutations, confirmed live via a rapid double-tap on Desconectar a todos landing exactly one `POST /disconnect_all` on the wire. ~~Confirm-by-typing gate applied to Reiniciar (`RESTART`) and Detener (`STOP`) only~~ — **superseded, see final-review fix wave below**; this original scope was a literal reading of OC-24's backlog line that under-covered invariant 5/9 for Desconectar a todos. Errors render inline via OC-22's `gatewayErrorMessage`/`isLikelyVpnDown`/`VpnSettingsButton`. **Live verification** (all 8 checks from the plan, against `npm run mock-gateway` + `npx expo start --web`): (1) Stop → `ConfirmByTypingSheet(word="STOP")` → step-up → draining countdown with only Cancelar/Desconectar visible; (2) Cancel mid-drain reverts to `'running'` immediately; (3) a full countdown reaches `'stopped'` (only Iniciar); (4) Start skips the typing sheet, passes through `'starting'`, lands on `'running'`; (5) Restart (`RESTART`) runs the full automatic sequence with no manual taps at the midpoints; (6) Disconnect-all skips the typing sheet, a new `warn` "Todos los jugadores fueron desconectados" line lands in Logs; (7) a wrong step-up code (`111111`) re-shows the prompt via `forceFresh`, the correct code then succeeds; (8) cancelling the step-up prompt returns the button to idle with no error shown anywhere. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing `import/no-duplicates` resolver warnings every file in this repo has), `npm run format:check` all pass clean. **Final-review fix wave, same day (per `docs/specs/2026-08-14-server-lifecycle-design.md`'s "Confirm-by-typing gate" section):** CRITICAL finding — Desconectar a todos fired a real mutation from a single tap (no confirm-by-typing sheet), which combined with `StepUpContext`'s 90s warm step-up cache meant one tap within that window could disconnect every player with zero typed confirmation and no visible modal, violating invariant 5/9. Fixed: Desconectar a todos is now gated (`word="DISCONNECT"`). Iniciar is now also gated (`word="START"`) for uniformity with invariant 5/9's "every one of these" framing. Cancelar remains deliberately, permanently ungated — the one intentional exception, justified by invariant 11/9's own requirement that Cancel stay reachable for the whole draining window, which argues against adding typing friction to the one abort path; documented explicitly in the spec rather than left as an unstated omission. Every gated action's confirm handler now re-checks its lifecycle-state precondition before firing (e.g. Stop/Restart require `'running'`) and silently closes without firing if it no longer holds — closes a race where a second operator changes state while a sheet is open mid-typing. The sheet's displayed `word`/`description` now track the last non-null confirm action (via state adjusted during render, not a ref read during render — the latter is a React Compiler ESLint error in this repo) so the copy can't flip to the wrong action while the sheet is closing. Disconnect-all now shows a brief "Desconectados" confirmation after a successful call, since it produces no lifecycle change and previously gave zero feedback; `useDestructiveAction.run()` now returns a `boolean` success indicator to support this cleanly. A stale `cancelAction.error` from an earlier drain is now suppressed once the lifecycle state leaves `'draining'`, un-suppressed the moment a fresh Cancel attempt actually fails again, so it can't silently reappear under Cancelar in a later, unrelated drain. **Second-round safety review, same day (2026-08-14) — 5 more findings fixed, plus one bonus fix:** (finding 3) `writeApi.ts`'s `OkResponseSchema` was `z.object({ ok: z.boolean() })`, which validated a `200 { ok: false }` response just as happily as `{ ok: true }` — now `z.object({ ok: z.literal(true) })`, so a real rejection throws an `invalid_response` `ApiError` instead of silently reading as success. (finding 4) Desconectar a todos no longer shows during `'draining'` — confirming it opens a full-screen `Modal` that covers Cancelar for as long as the operator is typing "DISCONNECT", leaving the one abort path unreachable (not just "obscured") for that window, which violates invariant 11 the same as removing the button would; now `'running'`-only, and its confirm-time precondition check narrowed to match. (finding 5, defense-in-depth) `ConfirmByTypingSheet`'s Confirmar was `disabled={typed !== word}` — an empty `word` prop evaluates that `false` (enabled) before any typing, since `'' !== ''` is `false`; not reachable via any current call site, but hardened to `disabled={word === '' || typed !== word}` since invariant 9 rests entirely on this one component. (finding 6) `useDestructiveAction`'s retry-on-403 flow called `call()` twice, and `httpClient.ts` minted a fresh `Idempotency-Key` inside every `request()` call independently, so the two attempts of one logical operator action got two different keys — the gateway would see a retried `start` as two starts, a real risk if the first is already mid-orchestration. `call`'s signature is now `(stepUpCode, idempotencyKey) => Promise<T>`; `run()` generates one `Crypto.randomUUID()` per invocation and passes it to both attempts; `httpClient.ts`'s `RequestOptions` gained an optional `idempotencyKey` that overrides the auto-generated one when present; `writeApi.ts`'s five methods and all 5 `StatusScreen.tsx` call sites thread it through. (finding 2, on `StatusScreen.tsx` directly) after bootstrap, `status`/`lifecycle` only update via the stream, with no re-fetch of their own — while `useStreamStatus()` reads `'reconnecting'`, every value on screen is a snapshot of unknown age, but every destructive button stayed fully armed; now Reiniciar/Detener/Iniciar/Desconectar a todos are `disabled` during a reconnect, plus a small inline note near the lifecycle indicator ("Datos posiblemente desactualizados — reconectando…"), on top of the existing global banner. Cancelar deliberately excluded — disabling the one abort path during exactly the kind of stream gap finding 1 (OC-25's row) already flags as risky would be a worse regression than the staleness this warns about. **Bonus fix (not a numbered finding, "also" in the review):** Detener issued from `'starting'` (the stalled-start escape hatch) now sends `{ mode: 'immediate' }` instead of the `'running'`-Detener's `{ mode: 'graceful', seconds: 30 }` — a 30s graceful drain aborting a start that never finished starting up may itself just hang. `StatusScreen.tsx` captures which mode a Detener press means at press time (`stopMode` state) so the confirm sheet's copy and the eventual request body always agree. **Live verification**: draining hid Desconectar a todos correctly (only Cancelar shown) and Cancel reverted to `'running'` instantly on a single tap; Confirmar's disabled-when-empty state observed directly; stream-staleness gating driven via the mock's `stream_drop` scenario armed *before* opening the page's connection (per OC-17's own note on that scenario's drop-timer attachment) — the reconnect banner, the new inline staleness note, and all three gated buttons dimmed together, then all cleared together once the scenario reset and the stream reconnected. Finding 1's reconciliation asymmetry (see OC-25's row) could not be live-verified for the same mock-atomicity reason noted there. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing warnings), `npm run format:check` all pass clean. | ✅ |
| OC-27 | Broadcast a message to players | ✅ Done 2026-08-14. Per `.superpowers/sdd/2026-08-15-broadcast-implementation/`, one task. `src/api/writeApi.ts` gained `broadcastMessage(message)` → `POST /api/v1/broadcast` — the one `write` method with no `stepUpCode`, matching the gateway contract's explicit "not step-up" carve-out for this endpoint (unlike every OC-26 lifecycle action). `src/features/chat/BroadcastComposer.tsx`, mounted as the last child of `ChatScreen.tsx`'s outer view, below the existing `FlatList`: a `TextInput` (200-char `maxLength`, client-invented — no server-verified limit) plus a live `N/200` counter, a "Así lo van a ver los jugadores" preview reusing OC-21's own `ChatMessageRow` unmodified against a synthesized `{author: '[Sistema]', message: trimmed, ts: now}` so the preview can never visually drift from the real row, and an Enviar button disabled on empty/whitespace-only input or mid-send. Deliberately does **not** guess the gateway's rate-limit window with a client-side countdown — the mock enforces a flat 5s window server-side and returns `429 rate_limited` with its own message, which just renders through the existing `gatewayErrorMessage` danger-text path like any other write error; inventing a client countdown would risk disagreeing with a real gateway's actual window. **Live verification**, all 5 checks against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock TOTP `000000`): (1) typing renders the preview card above the compose box, `[Sistema]`-authored, styled identically to a real chat row; (2) the counter tracks length live and typing past 200 chars is silently blocked by `TextInput`'s own `maxLength` (confirmed by typing 264 characters and observing the field stop accepting input at 200); (3) tapping Enviar clears the field and the message lands for real in the chat feed above via the live `chat` SSE event (confirmed via `read_network_requests`: `POST /api/v1/broadcast` → `200`); (4) an immediate second send (within the mock's 5s window) surfaces the mock's exact `429 rate_limited` body — "Esperá unos segundos antes de enviar otro mensaje" — through the danger-text error style, not a client-invented countdown (confirmed both visually and via the network log showing the second `POST` returning `429`); the field's typed text is retained on this failure path, not cleared, since `setMessage('')` only runs on the success branch; (5) clearing the field, or filling it with spaces only, keeps Enviar disabled (verified counter shows non-zero for spaces-only input while Enviar stays dimmed, since `canSend` gates on `trimmed.length`, not raw `message.length`). Zero console errors across the whole pass. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing `import/no-duplicates` resolver warnings every file in this repo has), `npm run format:check` all pass clean. | ✅ |
| OC-28 | Audit log screen | ✅ Done 2026-08-14. Per `.superpowers/sdd/2026-08-15-audit-log-implementation/`, one task. `src/features/audit/useAuditQuery.ts` bootstraps via `GET /api/v1/audit?limit=50` (`queryKeys.audit(50)`, the same `useApi`/`useAuthErrorRouting` shape as every other screen) then stays live via the stream's `audit` event — **prepending, not appending**, unlike OC-20 Logs/OC-21 Chat: this is a newest-first review screen (who did what, most recent first), not a live-tail-follow screen, so there's no cap and no batching either — audit rows are one per human write-action (start/stop/restart/cancel/disconnect/broadcast), the same low-frequency profile as Chat, never a flood. `src/features/audit/AuditRow.tsx` exports the row component as **`AuditLogRow`**, not `AuditRow` — the design spec's own self-review caught that the obvious name collides with the `AuditRow` *type* already exported from `@/api/schemas` and consumed by the same files; outcome badge (cyan `ok` / red `error`), action, `formatTime(row.ts)` (OC-22's shared helper, not a new copy), operator, JSON-stringified payload when non-empty, and `detail` in red when present. `src/features/audit/AuditScreen.tsx` — `Empty`/`GatewayErrorEmpty` loading/error states matching every other screen, `FlatList` over the rows, "Sin actividad todavía." empty state. `app/(tabs)/audit.tsx` registers the route but **not** as a 7th tab — reachable instead via a new `Pressable` row (chevron via `Ionicons`, `colors.textMuted` from `useTheme()`, reusing the app's existing muted-text/inactive-tint token convention) added to the top of `app/(tabs)/more.tsx`, above `EnvironmentSwitcher`. This is the first sub-navigation `more.tsx` has ever had — establishes the pattern (route lives under `(tabs)` so it inherits the nav shell/sidebar, just isn't in the tab bar) for any future secondary screen that doesn't warrant primary-tab real estate. **Live verification**, full pass against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock TOTP `000000`): (1) "Auditoría" row renders above the environment switcher on Más, tapping it navigates to `/audit`; (2) fresh mock session correctly showed the "Sin actividad todavía." empty state before any write action; (3) and (4) verified together with a stricter two-tab test than the plan called for — one tab left mounted on Auditoría the entire time while a second tab triggered a `server.restart` (Status, full TOTP step-up flow) and then a `broadcast` (Chat composer): both rows appeared at the **top** of the first tab's list live, in the correct order, with zero navigation or refresh on that tab — confirms the prepend-not-append stream handler end-to-end, not just via bootstrap re-fetch. (5) **not independently verified live**: the only reachable error-outcome path is tapping Cancelar in the documented self-healing race from OC-18/OC-25 (a drain that finishes server-side while the client's `lifecycle` view still lags by one broadcast interval, letting a stray Cancelar tap land after the drain already ended and get back `400 no_pending_shutdown`) — three attempts to time a tap inside that window across ~30s countdowns all missed, since the self-heal window is sub-second and browser-automation round-trips aren't fine-grained enough to reliably hit it. Verified by code trace instead: `tools/mock-gateway/src/routes/server.js`'s `/cancel_shutdown` handler calls `recordAudit({ ..., outcome: 'error', detail: err.message })` on exactly this path, and `AuditLogRow`'s `row.outcome === 'error'` / `row.detail` branches (red badge, red detail text) are the same literal code as every other conditional in the component — not a separately-untested branch, just not exercised live this pass. Zero console errors across the whole pass. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing `import/no-duplicates` resolver warnings every file in this repo has), `npm run format:check` all pass clean. | ✅ |

---

## Phase 3 — ORACLE manual control

Maps to NH-75 P3. Highest-risk surface in the app; every safety affordance below is load-bearing,
not polish. Read NH-75 §4.3 and §5.2 before starting.

| ID | Item | Notes | Status |
|---|---|---|---|
| OC-29 | Loaded events / templates browser | ✅ Done 2026-08-14. **Phase 3's first ticket** — deliberately the lowest-risk one: read-only, no staging/firing/composer (those are OC-30..34). `OracleEventsResponseSchema`/`EntityTemplateSchema` (`src/api/schemas.ts`), `api.read.getOracleEvents()` (`GET /api/v1/oracle/events`), `queryKeys.oracleEvents` — same `useApi`/`useAuthErrorRouting` shape as every other read screen, no `refetchOnWindowFocus`/`refetchOnMount` overrides since (unlike Logs/Chat/Audit) nothing streams into this cache, so TanStack's default refetch-on-focus/remount is the right behavior, not something to suppress. `src/features/oracle/OracleEventsScreen.tsx` — three sections (Cargados/En etapa/Templates disponibles), pull-to-refresh, and an honesty note shown only while something is staged: **no invented "failed" status** — the mock (and, per NH-75, the real gateway) only distinguishes `staging`/`loaded`, a parse failure is only ever a server-side `warn!` today, so the note says outright that a stuck staged event might be in-flight or might have failed silently, rather than the UI pretending to know which; **no invented polling** — nothing in this ticket needed a live-update mechanism (no stream event backs ORACLE events yet), so this stays a plain bootstrap query, not a fabricated interval. `app/(tabs)/oracle.tsx` now renders `OracleEventsScreen` in place of the Phase-3 placeholder. **Live verification**, full pass against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock TOTP `000000`): (1) fresh session showed all three sections correctly — "Sin eventos cargados.", "Nada en etapa.", and the three fixture templates ("Manada de lobos", "Campamento de bandidos", "Elemental de tormenta") populated from the always-on fixture. (2) populated real data via a temporary trigger (`POST /api/v1/oracle/stage` with `X-Ops-Totp: 000000`, reverted before commit — confirmed via `git diff` showing zero changes to the file used): `test_event` appeared under "En etapa" with the honesty note visible immediately after staging, then moved to "Cargados" (note gone) after the mock's ~1.5s staging delay elapsed and the query refetched past its 30s `staleTime` — confirming the staged→loaded transition end-to-end, not just the empty state. (3) loading state verified by code trace only, not caught live — the local mock responds fast enough that the "Cargando…" frame never landed inside a screenshot's round-trip window (same class of limitation OC-18 hit); the branch is the identical `query.data === undefined && !query.error` pattern already live-verified on every other screen. Error state **was** exercised live (stronger than the brief's fallback): killed the mock-gateway process mid-session, reloaded, and confirmed `GatewayErrorEmpty` rendered "No se pudo conectar con el gateway" with the "RECONECTANDO CON EL GATEWAY…" banner; restarting the mock gateway (which wipes its in-memory session map, same as OC-18's precedent) correctly dropped the app to `/login` rather than getting stuck, and a fresh login showed ORACLE recovered cleanly. Zero console errors across the whole pass. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing `import/no-duplicates` resolver warnings every file has), `npm run format:check` all pass clean. **What's still ahead in Phase 3**: OC-30 preset library, OC-31 `DmEvent` composer, OC-32 target picker, OC-33 dry-run preview, OC-34 fire + kill switch — this ticket built none of them, only the read surface they'll eventually sit alongside. | ✅ |
| OC-30 | Preset library | ✅ Done 2026-08-15, combined with OC-31 per `docs/specs/2026-08-15-oracle-preset-library-composer-design.md`'s "Why one ticket" reasoning: OC-30's own text — "Browse, search, clone into **the composer**" — names OC-31 as its destination, so a preset library with nowhere to clone into isn't the feature described (same call as OC-25+26). `GET /api/v1/oracle/presets` (three fixture presets — "Emboscada de lobos", "Tormenta mágica", "Asalto de bandidos") rendered as a client-side-filtered searchable list (substring match on `name`, no server-side search endpoint exists or was asked for) inside `OracleComposerScreen.tsx`. Each row's "Usar" button clones that preset's `dm_event` into the composer form below — still fully editable before staging, "clone" not "fire immediately" — including a fresh slugified `id` derived from the preset id + timestamp so two clones never collide. | ✅ |
| OC-31 | `DmEvent` composer form | ✅ Done 2026-08-15. `src/features/oracle/OracleComposerScreen.tsx` + `app/(tabs)/oracle-composer.tsx`, linked from `OracleEventsScreen.tsx` via a "Componer evento" row (same bordered-`Pressable`-with-chevron pattern as `more.tsx`'s "Auditoría" link). **Builds against the mock's actual flat `DmEvent` shape, not the full real-engine schema**: the private NH-75 design's real `DmEvent` has additional nested structs beyond `dimension_config`/`atmosphere` (the two already public via the gateway API contract) that this ticket does not scope in — this repo is public, so the private schema's internal field names are referenced by doc path, not restated here; the mock (`tools/mock-gateway/src/fixtures.js`/`oracleSanitizer.js`) implements only `{ kind, template_id?, intensity, radius, dimension_config?, atmosphere? }` and the gateway API contract doc doesn't specify inner detail either — per this session's established practice, the mock is the concrete build target, matching OC-29's identical resolution for the step-up-on-reads question. `kind` (`spawn`/`weather`) and `template_id` (populated from OC-29's `useOracleEventsQuery().entity_templates`, shown only when `kind === 'spawn'`) are `ChipPicker` allowlists rather than free text — the two `kind` values actually observed in the mock's fixtures. `intensity` (0–10) and `radius` (1–100) are bounded numeric fields with inline "Tiene que estar entre…" validation, bounds sourced directly from `sanitizeDmEvent()`'s own clamp values — the mock's own bounds, not invented ones, and the closest thing to the backlog's "`bounds::` constants" this schema has. ⚠️ `atmosphere.weather_effect` and `dimension_config.biome_profile` (both optional single-string fields, a deliberately minimal stand-in for the real engine's nested structs) render inside a section carrying the exact badge text **"Guardado, no aplicado al mundo en vivo"** — invariant 10 (NH-75 §9); staging's response `sanitized` object was confirmed via direct API call to still carry both fields unchanged, proving the mock's `{ ...dmEvent }` pass-through spread actually works, not just that the badge renders. Staging goes through `useDestructiveAction` (OC-25/26) — the second real consumer of that hook outside the Status screen — and is deliberately **step-up-only, not confirm-by-typing**: per NH-75's two-step model, staging only writes a file (nothing spawns, nothing is player-visible, trivially reversible by deleting the file), so `ConfirmByTypingSheet` stays reserved for the actually dangerous action, OC-34's fire. "Guardar en etapa" stays disabled until `id` is non-empty (post-slugify), `kind` is chosen, `template_id` is set when required, and both bounds are satisfied. On success, navigates back to `/oracle` (OC-29's screen) rather than polling — matching OC-29's own no-invented-polling reasoning. **Live verification**, full pass against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock, TOTP `000000`), all 8 planned checks: (1) "Componer evento" renders on ORACLE and navigates to `/oracle-composer`. (2) all three preset fixtures render; typing "lobos" filters the list down to "Emboscada de lobos" only. (3) "Usar" on that preset pre-filled every field correctly — slugified id, "Aparición" selected, "Template" section appeared with "Manada de lobos" selected, intensity `6`/radius `20` (the fixture's exact values). (4) clearing the id field visibly disabled "Guardar en etapa"; retyping a non-empty id re-enabled it. (5) staging prompted the step-up TOTP dialog directly (no confirm-by-typing sheet, confirming the deliberate design choice); `000000` succeeded, navigated back to `/oracle`, and the new id appeared under "Cargados" (the mock's ~1.5s staging delay had already elapsed by the time the screen re-rendered). (6) a fresh `weather`-kind composition correctly hid the "Template" section. (7) filling "Bioma"/"Efecto climático" showed the exact badge text above them; staged, then confirmed via a direct authenticated `curl` against `POST /api/v1/oracle/stage` that the response's `sanitized.dimension_config`/`sanitized.atmosphere` carried the submitted values unchanged. (8) intensity `15` triggered the inline "Tiene que estar entre 0 y 10." error and kept the button disabled even with every other field valid. Zero console errors across the whole pass. ⚠️ **Correction (final-review fix wave, 2026-08-14):** this row originally logged the dropped-underscore behavior in the id field as an incidental artifact of "very fast scripted typing… not reproducible at human speed" — **that diagnosis was wrong.** The field ran the full `slugify()` on every keystroke, and `slugify()` strips a *trailing* separator run, so the `_` was deleted the instant it was typed and the next character concatenated onto the previous word (`tormenta magica test` → `tormentamagicatest`). It reproduced 100% of the time at any typing speed — nothing to do with speed. Fixed by splitting the two behaviors: `slugifyPartial()` (on-type — strips the leading run and collapses internal repeats, keeps a trailing one) drives the field, and the full `slugify()` is applied once when the request body is built at stage time. Preset cloning was never affected because it sets the id programmatically in one shot, which is why that live check didn't surface it. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing `import/no-duplicates` resolver warnings every file has), `npm run format:check` all pass clean. | ✅ |
| OC-32 | Target picker | ✅ Done 2026-08-15, combined with OC-33 per `docs/specs/2026-08-15-oracle-target-picker-dryrun-design.md`'s "Why one ticket" reasoning: OC-32's target picker has no reason to exist on its own — a `target` only means anything as the input to `POST /api/v1/oracle/trigger`, and the only reachable call to that endpoint in this ticket's scope is `dry_run: true` (OC-33's own preview card; real firing is OC-34, kept separate). Shipping OC-32 alone would produce an orphan component with nothing consuming it and no way to live-verify it against the real gateway flow — same reasoning as OC-25+26 and OC-30+31 before it. Two-task ticket. **Task 1** (`3df5259`): `OracleTargetSchema`/`OracleTriggerResponseSchema` (`src/api/schemas.ts`), `api.write.triggerOracleEvent(eventId, target, dryRun, stepUpCode, idempotencyKey?)` (`src/api/writeApi.ts`, `POST /api/v1/oracle/trigger`), and the mock's new server-side enforcement (`tools/mock-gateway/src/routes/oracleTrigger.js`): a `target.type === 'player'` whose alias isn't in the (scenario-aware) online roster now returns `404 target_player_offline` before any other validation — a missing player is an error, never a silent fallback to the origin. **Task 2** (this row's screen): `src/features/oracle/OracleDryRunScreen.tsx` — player picker is primary, reusing OC-30/31's `ChipPicker<string>` unchanged against `usePlayersQuery()`'s fixture aliases; manual X/Y/Z coordinates sit behind a `mode` disclosure toggle ("Usar coordenadas manuales" / "Usar jugador conectado"), each field free-text (no `keyboardType` override — signed decimals need the plain keyboard on both platforms). `buildTarget()` re-checks the selected player is still in `playersQuery.data` at build time (not just at selection time) before ever constructing an `OracleTarget` — the client-side half of the "missing player is an error" invariant, mirroring the server-side check Task 1 added; a player who disconnects between selection and tap makes the button go back to disabled with a visible "Este jugador ya no está conectado." note, not a request that reaches the server naming a stale alias. Toggling modes preserves whichever selection/fields were already entered (confirmed live, check 4) rather than clearing state, since nothing about switching modes invalidates the other mode's in-progress input. Route: `app/(tabs)/oracle-trigger.tsx` (`?id=` param via `useLocalSearchParams`), suppressed from the tab bar the same way `audit`/`oracle-composer` already are (`app/(tabs)/_layout.tsx`). Entry point: `OracleEventsScreen.tsx`'s "Cargados" section gained an optional `onItemPress` on its shared `Section` component (unused by "En etapa"/"Templates disponibles", which stay plain text) rendering a "Probar disparo" row per loaded event, navigating to `/oracle-trigger?id=<event>`. **Live verification**, all 8 planned checks, against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock, TOTP `000000`): (1) tapping a loaded event's "Probar disparo" row navigated to `/oracle-trigger?id=tormenta_magica_test` with the id in the screen's title. (2)–(8): see OC-33's row, since every check exercises this ticket's one screen — not repeated twice. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing `import/no-duplicates` resolver warnings every file in this repo has), `npm run format:check` all pass clean. ⚠️ **Correction + additions (final-review fix wave, 2026-08-14):** (a) the `404 target_player_offline` path was originally logged here as "a real, tested error" — **it wasn't tested at the time.** The client-side `buildTarget()` guard makes that server response structurally unreachable through the UI (an offline player can't be selected, and with the mock in `down` the picker is empty and the button disabled), so the original 8-check live pass could only ever observe the client-side half. It has now been observed directly, by authenticated `curl` against `POST /api/v1/oracle/trigger` (`X-Ops-Totp: 000000`, `dry_run: true`, against a genuinely loaded event): an unknown alias under `scenario=normal` returned `HTTP/1.1 404` + `{"error":{"code":"target_player_offline","message":"El jugador 'Fantasma' no está conectado"}}`, and the fixture player `Voss` under `scenario=down` returned the same code for `'Voss'`. Server-side enforcement confirmed by execution, not by reading the route. (b) **`dry_run` is now a required boolean in the mock, not an optional one.** The route previously allowed it to be omitted and `if (!dryRun)` then took the **live-fire** branch (log line + `oracle.trigger` audit row) for a request that simply forgot the field — a fail-open default on the most dangerous parameter in the API, inherited from OC-13 and left in place right before OC-34 builds Fire on this same route. An absent or non-boolean `dry_run` is now `400 invalid_body` (`curl`-confirmed: `{"error":{"code":"invalid_body","message":"dry_run es requerido y debe ser boolean"}}`), and an unrecognized `target.type` — which previously skipped every check and got echoed back as `resolved_pos` — is now `400 invalid_body` too. `docs/reference/gateway-api-contract.md` §5 states the requirement explicitly rather than leaving the default implicit. (c) `triggerOracleEvent`'s `dryRun` parameter is now the TypeScript literal type `true`, not `boolean`, so no future call site can flip it to `false` without a deliberate, reviewable signature widening in OC-34. | ✅ |
| OC-33 | Dry-run preview card | ✅ Done 2026-08-15, part of the same ticket as OC-32 above (see that row for the "why combined" reasoning and the target-picker half of this screen). `OracleDryRunScreen.tsx`'s result card renders all four fields the mock's `OracleTriggerResponseSchema` returns: `would_spawn` ("Se generarían: N"), `bodies` (comma-joined, "Criaturas: …"), a resolved position ("Posición resuelta: …" — formatted `(x, y, z)` when the response shape matches, else a raw `JSON.stringify` fallback, exercised live since the mock's `resolved_pos` for a player target is the raw `{type,alias}` target object, not `{x,y,z}` — confirmed the fallback branch renders correctly, not just the happy-path one), and `nearest_player_dist` ("Distancia al jugador más cercano: N"). **Deliberately no trigger-time diff field**: the design spec named a `sanitize()` diff as the card's main safety affordance, but `POST /api/v1/oracle/trigger`'s actual mock response (`OracleTriggerResponseSchema`) carries no `diff` — nothing is sanitized at trigger time, only at stage time. That requirement is already satisfied one screen earlier, by OC-31's composer/staging flow (`StageOracleEventResponseSchema`'s `diff: DmEventDiffEntrySchema[]`, live-verified there against a real `sanitizeDmEvent()` clamp) — restating a diff here would either fabricate one client-side or misrepresent what the gateway actually does at this step. **`dry_run: true` is hardcoded with no path to `false`**: `OracleDryRunScreen.tsx` calls `api.write.triggerOracleEvent(eventId, target, true, code, idempotencyKey)` — literally `true`, no toggle, no button, no branch that could send `false`; grepping the diff for `dry_run` and `dryRun` confirms zero occurrences of the string `false` anywhere near it. Sending `false` (an actual, irreversible spawn) is reserved for OC-34's Fire button, not built here. **Live verification**, all 8 planned checks, against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock, TOTP `000000`): (1) navigation from a loaded event's "Probar disparo" row landed on `/oracle-trigger?id=tormenta_magica_test` with the right id in the title (see OC-32's row). (2) the picker listed all 5 fixture players (Kaelith, Voss, Ember, Doran, Nyx); selecting Voss enabled the button (dimmed → full-opacity cyan). (3) "Usar coordenadas manuales" swapped the picker for X/Y/Z fields, button back to disabled; filling all three (including a signed decimal, `-12.5`) re-enabled it. (4) toggling back to "Usar jugador conectado" showed Voss still selected — state preserved across the toggle, button correctly reflected the (valid) player-mode state. (5) tapping "Probar disparo" with Voss selected opened the step-up TOTP modal; `000000` closed it and the result card rendered all four fields (`Se generarían: 1`, `Criaturas: wolf`, `Posición resuelta: {"type":"player","alias":"Voss"}` — the `JSON.stringify` fallback path, confirmed exercised — `Distancia al jugador más cercano: 25`). (6) switching the mock to the offline scenario (`curl -X POST http://localhost:4000/mock/scenario -d '{"scenario":"down"}'` — the mock's actual endpoint is `/mock/scenario`, not the bare `/mock` the plan sketched) and reloading the screen showed "Sin jugadores conectados." with "Usar coordenadas manuales" still offered as the only path. (7) switching back to `normal` and reloading repopulated all 5 players. (8) grepped the diff for `dry_run`/`dryRun` — the only value that appears anywhere is the hardcoded `true` in the one `triggerOracleEvent` call site. Zero console errors across the whole pass. `npx tsc --noEmit`, `npm run lint`, `npm run format:check` all pass clean (see OC-32's row for the exact same clean results — one verification pass covers both rows, they're one ticket). ⚠️ **Additions (final-review fix wave, 2026-08-14):** (a) **the result card is now invalidated whenever the target changes.** As originally shipped, `result` was set on success and cleared by nothing — dry-running against one player and then selecting a different one (or toggling to coordinates and retyping them) left the previous target's card on screen with nothing marking it stale, and a failed re-run rendered the old success card directly above the fresh `ActionError`. `setResult(null)` now runs at the start of `handleTrigger`, in the mode toggle, in the picker's `onSelect`, and in all three coordinate `onChangeText` handlers — matching the `setStageResult(null)` precedent OC-30/31's composer already set. Misleading today; a genuine firing hazard the moment OC-34 hangs a Fire button off this card ("card describes target X, form is set to target Y"). Re-verified live: after a successful dry run against Voss, selecting Ember cleared the card; after a run against Ember, toggling to manual coordinates cleared it; after a coords run at `(10, 20, -12.5)`, editing Y cleared it. (b) the card now states **"Simulación: no se generó nada en el mundo todavía."** outright — previously the conditional mood of "Se generarían" was the only thing distinguishing a preview from a real outcome, immediately after the same TOTP step-up modal the destructive lifecycle actions use. (c) the `buildTarget()`-returned-`null` throw inside the `useDestructiveAction` callback (reachable only if the player disconnects *during* the step-up wait) now throws the operator-facing `'Este jugador ya no está conectado.'` — the same Spanish text the screen's own offline banner uses — instead of the raw internal `'invalid target state'`, which rendered verbatim through `ActionError` in an otherwise fully-Spanish UI. (d) `formatResolvedPos` returns `JSON.stringify(pos) ?? '—'`: `JSON.stringify(undefined)` is the value `undefined`, not a string, and would have interpolated as the literal text "undefined" if the gateway ever omits `resolved_pos` (it's `z.unknown()` precisely because that shape isn't ratified). Re-verification also exercised the `(x, y, z)` formatting branch for the first time — a coords-mode dry run rendered `Posición resuelta: (10, 20, -12.5)`, complementing the original pass's `JSON.stringify` fallback observation. Zero console errors across the re-verification pass. | ✅ |
| OC-34 | Fire + kill switch | ✅ Done 2026-08-15, per `docs/specs/2026-08-15-oracle-fire-kill-switch-implementation` plan. **Highest-risk ticket in the app — the first that can cause a real (mock-simulated) irreversible spawn.** Three tasks. **Task 1** (`d8fc41f`): `OracleEnabledResponseSchema` (`src/api/schemas.ts`); `OracleEventsResponseSchema` widened with `oracle_enabled: z.boolean()` — no dedicated GET for the flag, read via `GET /oracle/events` by design, documented in `docs/reference/gateway-api-contract.md`; `api.write.fireOracleEvent(eventId, target, stepUpCode, idempotencyKey?)` (`src/api/writeApi.ts`) is a **new, separate** method hardcoding `dry_run: false`, not a widening of OC-32/33's `triggerOracleEvent` (whose `dryRun: true` literal type stays exactly as that ticket's final review narrowed it) — the only place `dry_run: false` appears anywhere in client code, so grepping `fireOracleEvent` finds every real-fire call site; `api.write.setOracleEnabled(enabled, stepUpCode, idempotencyKey?)` for the kill switch. **Task 2** (`dc63bd3`, fixed by `736d807`): the kill switch lives on `OracleEventsScreen.tsx` (the ORACLE tab), directly below the title and above "Componer evento" — a bordered box showing "ORACLE: Activo"/"ORACLE: Desactivado" plus a red "ORACLE está deshabilitado — el staging y el disparo van a fallar hasta reactivarlo." note when off. **Deliberately asymmetric friction**: disabling (`handleDisable` → `disableAction.run()`) is step-up-only, no typing sheet — lower friction, since disabling is safety-increasing; re-enabling opens `ConfirmByTypingSheet` requiring the literal word `ENABLE` before its own step-up — full friction, since it restores real Fire capability. Fix commit `736d807` corrected a layout defect from the brief's own code: `Button` (`src/ui/Button.tsx`, hardcoded `w-full`, left untouched as a shared component) inside a `flex-row` alongside the status label overflowed past the box edge on web; fixed by stacking the label and button on their own rows instead of sharing a row — the same "never put `Button` in a `flex-row` with a sibling it needs to share width with" rule Task 3 followed for the same reason. **Task 3** (this row's own commit, below): Fire lives on `OracleDryRunScreen.tsx`'s existing result card, gated on a fresh `result`. `result`'s shape widened to `{ response: OracleTriggerResponse; target: OracleTarget; fired: boolean }` — **the frozen target**: `handleTrigger` now captures `buildTarget(onlinePlayers)` into `result.target` at the moment the dry-run succeeds, and `fireAction` sends exactly `result.target`, never a fresh `buildTarget()` read, so Fire can never silently target something other than what the card shows. The one thing that can drift invisibly between dry-run and Fire is the target player's online status, so `fireAction`'s callback re-runs the same `playersRef`-based online check the dry-run path uses, immediately before sending — the client-side half of the "missing player is an error" invariant, with Task 1's server-side `target_player_offline` check as the authoritative backstop either way (live-verified below: the client-side re-check only catches the case where `playersRef` has already refetched; when it hasn't, the server backstop still refuses the fire, and no audit row is written either way). "No hay forma de deshacer esto." renders directly above the "Disparar" button, inside the result card, only while `!result.fired`; on fire success the card text switches to "¡Disparado! Esto ya ocurrió en el mundo en vivo." and the button/no-undo text disappear (can't fire the same preview twice). Firing goes through a new `ConfirmByTypingSheet` requiring the literal word `FIRE`, description "No hay forma de deshacer esto. Se va a generar el evento en el mundo en vivo, ahora." — full friction (typing + step-up), unlike OC-31's staging (step-up-only, since staging is trivially reversible) and matching the kill switch's own re-enable friction level. **Live verification**, both this task's own checks and a re-verification of Task 2's, full pass against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock, TOTP `000000`; both processes already running, `mock-gateway`'s process start time confirmed later than Task 1's commit so it wasn't serving stale pre-`oracle_enabled` code): (1) a fresh dry-run against a connected player rendered "Simulación: no se generó nada en el mundo todavía.", "Disparar", and "No hay forma de deshacer esto." (2) changing the target (different player) made the entire result card — including Fire — disappear, inheriting OC-32/33's `clearResult()` precedent. (3)–(4) a fresh dry-run, "Disparar" opened the typing sheet requiring `FIRE` (Confirmar disabled until typed exactly), confirming it then completing step-up (`000000`) updated the card to "¡Disparado! Esto ya ocurrió en el mundo en vivo." with different `would_spawn`/`bodies`/`resolved_pos`/`nearest_player_dist` values than the dry-run, Disparar/no-undo gone. (5) the audit log (`/audit`) showed a matching `oracle.trigger` row with `"dry_run":false` and the exact fired target. (6) switched the mock to the offline scenario mid-flow (`curl -X POST http://localhost:4000/mock/scenario -d '{"scenario":"down"}'`) after a dry-run but before firing: the fire was refused and no `oracle.trigger` audit row was added — the refusal actually surfaced via the **server's** `target_player_offline` message (`playersRef` hadn't refetched in the ~1s between the scenario switch and the tap, so the client-side re-check didn't catch it first), independently confirmed by a direct authenticated in-page `fetch` against `POST /api/v1/oracle/trigger` with `dry_run:false` returning `404 target_player_offline` — matching OC-32/33's two-layer evidence standard; switched back to `normal` afterward. (7) with ORACLE disabled (via an in-page `fetch` to `setOracleEnabled(false)` so the dry-run screen's local state stayed mounted), both a fresh "Probar disparo" tap and a "Disparar" tap on an already-rendered stale preview failed with "ORACLE está deshabilitado" surfaced through `ActionError`, not a generic failure. Re-verified Task 2's kill switch end-to-end in the same pass: disable is step-up-only (no typing sheet), re-enable requires typing `ENABLE` then step-up, and the box renders with no layout overlap. ORACLE left enabled at the end of the pass. Zero console errors observed. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing repo-wide `import/no-duplicates` resolver warnings), `npm run format:check` all pass clean. **This closes out Phase 3.** ⚠️ **Correction + additions (final-review fix wave, 2026-08-14):** (a) **the documentation paths cited at the top of this row were wrong** — `docs/specs/2026-08-15-oracle-fire-kill-switch-implementation` does not exist. The two real documents are `docs/specs/2026-08-15-oracle-fire-kill-switch-design.md` (design) and `docs/superpowers/plans/2026-08-15-oracle-fire-kill-switch-implementation.md` (plan). (b) **the offline re-check description above is stale.** It describes the pre-`2acd56b` behavior — the client-side re-check catching the drift only when `playersRef` happened to have already refetched, with the server as the backstop otherwise. What actually shipped `await`s `playersQuery.refetch()` inside `fireAction`'s callback and reads ITS resolved `data` (`?? playersRef.current` only as a refetch-failure fallback), so the client-side half now holds in fact, not just on the lucky path. **This is now observed, not just claimed** — see check (3) below, the one live observation this ticket's plan required and that no previous round ever wrote down. (c) **`forceFreshStepUp` was never documented here.** `useDestructiveAction` gained an additive, opt-in `{ forceFreshStepUp?: boolean }` option, and Fire is its only user: it clears `StepUpContext`'s 90-second code cache before requesting, so Fire ALWAYS demands a freshly typed TOTP — unlike every other destructive action in this app, which reuse a cached code. Without it, the dry-run that immediately precedes almost every fire would have just populated that cache and Fire would have silently skipped its step-up, collapsing the intended "step-up AND typing FIRE" double gate down to just the typing. (d) **`reset()` was never documented here either.** `useDestructiveAction` also gained an additive `reset()` (clears only `error`); both additions were verified against every existing call site — no existing caller passes the option or calls `reset()`, so both are strictly additive. (e) **audit-row attribution:** the `oracle.enabled` audit row is NOT this ticket's addition — `tools/mock-gateway/src/routes/oracleEnabled.js` (route + `recordAudit`) pre-existed this branch untouched (`git diff 6673c1b..HEAD -- tools/mock-gateway/src/routes/oracleEnabled.js` is empty). This branch's only mock change is `oracleEvents.js` gaining the `oracle_enabled` read field. (f) **this fix wave's own code changes** (final-review findings 1, 2, 4, 5): **(f1)** an inconclusive fire no longer renders as "nothing happened". `result` gained a third state, `fireOutcomeUnknown`, alongside `fired`. `OracleDryRunScreen.tsx` now classifies every fire failure: **definite no-spawn** (the client's own pre-flight throws, plus the gateway codes that provably return before the spawn/audit block — `unauthorized`, `session_expired`, `step_up_required`, `invalid_totp`, `oracle_disabled`, `invalid_body`, `missing_target`, `target_player_offline`, `event_not_found`) keeps today's behavior (error shown, Fire still offered); **anything else** — `network_error`, `timeout`, `invalid_response`, `unknown_error`, any 5xx, and any unrecognized future code (allowlist direction, so unknown failures fail toward "we don't know") — sets the unknown state, which replaces "Simulación: no se generó nada en el mundo todavía." with "No pudimos confirmar el resultado — puede que el evento se haya disparado igual. Revisá la auditoría antes de reintentar." and **withdraws the Disparar button**. This matters because `useDestructiveAction` mints a fresh idempotency key per `run()`, so a retry after an unconfirmed fire is a NEW operation to the gateway, not a dedupable retry — re-offering the app's one irreversible action under a false "nothing happened yet" claim was a real double-spawn path. **(f2)** the fired card no longer describes a completed real spawn in the conditional mood: "Se generarían: N" is now "Se generaron: N" once `fired` is true (the unknown state keeps "generarían", since those numbers are still only the dry-run's projection). **(f3)** `reset()` is now wired to all four call sites with the identical stale-error shape, not just `fireAction`: `clearResult()` also calls `triggerAction.reset()`, and `OracleEventsScreen.tsx`'s kill switch cross-clears (`handleConfirmEnable` → `disableAction.reset()`, `handleDisable` → `enableAction.reset()`) so a failed flip's error can't linger under a status label that now says the opposite. **(f4)** `docs/reference/gateway-api-contract.md`'s invariant 5 no longer asserts the per-event cap as an existing mitigation — it's marked not implemented (the mock enforces none; deliberately scoped out rather than faking client-side rate limiting) and flagged as a real-gateway concern. **Live re-verification of this fix wave**, against `npm run mock-gateway` + `npx expo start --web` (operator `matias`, TOTP `000000`, event `verify_oc34_reenable`), five checks, all observed directly: **(1) indeterminate fire.** With a valid preview against `Kaelith` on screen, the real fire's `POST /api/v1/oracle/trigger` (`dry_run: false` only — the dry-run path was left untouched) was made to fail at the network level in the browser, producing the same `ApiError('network_error', …)` `httpClient.ts` raises for a genuinely unreachable gateway. The card switched to the red "No pudimos confirmar el resultado — puede que el evento se haya disparado igual. Revisá la auditoría antes de reintentar.", the "Simulación…" line was gone, and **"No hay forma de deshacer esto." + the Disparar button were both gone**; the underlying "No se pudo conectar con el gateway" stayed visible underneath as context. Selecting a different player restored a normal preview (the intended way out of the state). **(2) successful fire.** A fresh dry-run against `Doran` read "Se generarían: 2"; after confirming with `FIRE` + a fresh TOTP the card read **"¡Disparado! Esto ya ocurrió en el mundo en vivo."** and **"Se generaron: 4"** — past tense, and a different value, i.e. the fire response's own numbers, not the preview's. The audit log gained exactly one new row: `oracle.trigger` / `{"event_id":"verify_oc34_reenable","target":{"type":"player","alias":"Doran"},"dry_run":false}` / `ok` at 20:56:24Z — and, correctly, NO row for the indeterminate attempt in check (1), whose request never left the client. **(3) OFFLINE-PLAYER-DURING-FIRE, client-side half — the check this ticket's plan required and had never recorded.** With a valid preview against `Ember` on screen, the mock was switched to `scenario=down` (`curl -X POST http://localhost:4000/mock/scenario -d '{"scenario":"down"}'`, `GET /api/v1/players` then returning `[]`), then Fire was confirmed with `FIRE` + TOTP. The refusal that rendered was **the client's own Spanish message, "Este jugador ya no está conectado."** — not the server's `target_player_offline` text (which reads "El jugador 'Ember' no está conectado"). Proven client-side by the request log: the last network call of the whole attempt was the re-check's own `GET /api/v1/players`, and **no `POST /api/v1/oracle/trigger` with `dry_run:false` was ever sent**; the audit log gained no row. The card correctly stayed in the "Simulación…" state with Fire still offered — the definite-no-spawn classification behaving as designed. Scenario restored to `normal` afterward. **(4) forced-fresh step-up, re-confirmed after this wave's changes.** Twice: a dry-run against `Kaelith` completed WITHOUT a TOTP prompt (reusing the cached code, proving the 90s cache was live), and the fire tapped ~15 seconds later still opened the "Confirmá tu identidad" prompt. Same again for `Doran` in check (2). **(5) stale-error reset, both halves.** *Kill switch:* a disable was made to fail client-side AFTER the server had already applied it (the honest form of this state), leaving "No se pudo conectar con el gateway" under "ORACLE: Activo"; once the query refetched to "ORACLE: Desactivado", re-enabling (typing `ENABLE` + step-up) flipped the label to "ORACLE: Activo" **and the stale disable error was gone** — without `disableAction.reset()` it would have survived, since `useDestructiveAction` only clears `error` inside its own `run()`. *Dry-run:* a failed dry-run against `Voss` left "No se pudo conectar con el gateway" under "Probar disparo"; selecting `Ember` cleared it immediately (`triggerAction.reset()` inside `clearResult()`), and the subsequent dry-run rendered a clean card. Zero console errors across the pass; mock left at `scenario=normal` with `oracle_enabled: true`. `npx tsc --noEmit` clean, `npm run lint` 0 errors / 50 warnings (identical to the pre-change baseline — all the same pre-existing `import/no-duplicates` resolver warnings), `npm run format:check` clean. | ✅ |

---

## Phase 4 — Hardening & the real second platform

| ID | Item | Notes | Status |
|---|---|---|---|
| OC-35 | Tablet / iPad layouts | Split into 3 separate tickets/PRs per Matías (chat, 2026-08-21) — the original scope bundled three genuinely independent pieces investigated together and found to have very different states: `useBreakpoint`/`SidebarLayout` (`app/(tabs)/_layout.tsx`, from OC-10/11) is chrome-switching only, NOT master-detail; keyboard shortcuts were fully greenfield (zero existing infra); orientation handling was a real, previously-flagged gap (`app.config.ts`'s `orientation: 'default'`, `docs/specs/2026-08-10-navigable-shell-design.md:90-94` already noted "no screens are landscape-optimized yet"). **Sub-part 1, keyboard shortcuts on web: ✅ Done 2026-08-21**, `docs/specs/2026-08-21-keyboard-shortcuts-web-design.md` / `docs/superpowers/plans/2026-08-21-keyboard-shortcuts-web-implementation.md`. Scope confirmed by Matías as "Básico": number keys 1-6 navigate the six main tabs (`DESTINATIONS` array position + 1, no separate mapping to desync), `Escape` closes any open confirmation modal, `?` opens a help overlay listing the shortcuts — explicitly no destructive action is ever reachable via a shortcut. Two small hooks, both `Platform.OS === 'web'`-gated, zero native behavior change: `useEscapeToClose(visible, onClose)` (`src/ui/useEscapeToClose.ts`), wired into both existing confirmation modals (`ConfirmByTypingSheet.tsx`, `StepUpPrompt.tsx`) by calling each one's own internal `handleCancel` — never the raw `onCancel` prop — so Escape produces byte-identical behavior to tapping "Cancelar", including clearing local state. `useTabShortcuts(destinations, onHelp, suppressed)` (`src/ui/useTabShortcuts.ts`) plus `KeyboardShortcutsHelp.tsx`, wired into `app/(tabs)/_layout.tsx`. Two tasks via subagent-driven-development, both passed task review clean on the first pass. **Final whole-branch review found 2 Important findings**, both fixed in one wave, scoped re-review confirmed both addressed with no new breakage: (1) digit keys navigated the tab underneath the help overlay itself while it was open, since `useTabShortcuts`'s guard only checked for a focused text input and the new help overlay has none — the design doc's own stated reason for skipping a global "modal open" registry (both *existing* modals `autoFocus` a text field) didn't actually cover this branch's own new component; fixed by adding an explicit `suppressed: boolean` third parameter (fed by `helpVisible`) rather than building the full registry, and the design doc was updated to record this. (2) no modifier-key guard meant `Cmd+1`..`Cmd+6`/`Ctrl+1`..`Ctrl+6` (the browser's own tab-switching shortcuts — real muscle memory for the admin power-user persona this app targets) also silently navigated the app first; fixed with an early return on `event.metaKey || event.ctrlKey || event.altKey`. Manual verification was genuinely run in-browser (`claude-in-chrome` tooling) against a real logged-in session on `npm run mock-gateway` + `npx expo start --web`: all six digit keys confirmed navigating correctly, `?` opening/closing the help overlay (backdrop tap too), a real `ConfirmByTypingSheet` (Status → Detener) closing via Escape exactly like "Cancelar" including clearing typed text, and typing a digit inside that same sheet's focused text field correctly NOT triggering navigation. iOS/Android verified by code inspection only (both hooks short-circuit on `Platform.OS !== 'web'` before attaching any listener) — no live device/simulator run for this sub-part specifically. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing repo-wide warnings), `npm run format:check` all clean. ⚠️ **Found and flagged, NOT fixed here (out of scope)**: while getting `npx expo start --web` running for manual verification, discovered the web target is currently completely broken on `development` — `src/features/status/ServerStatusActivity.tsx` (OC-47, Live Activities) does a module-scope `import { Text, VStack } from '@expo/ui/swift-ui'`, and that package's `Text` calls `requireNativeView(...)` at import time, which throws unconditionally on web with no `Platform.OS` check able to help (the crash happens before any guard in the importing code runs). Since `(tabs)/index.tsx` → `StatusScreen.tsx` → `useServerStatusLiveActivity.ts` → `ServerStatusActivity.tsx` is on a route Expo Router's web dev server eagerly loads to build its manifest, one bad import currently takes down the entire web app, not just the Status tab or the Live Activity feature. Flagged to Matías directly, not addressed on this branch. **Fixed separately the same day** — see the OC-47 row's own "Correction (post-merge fix, 2026-08-21)" note for the full fix (`ServerStatusActivity.ios.tsx`/`ServerStatusActivity.ts` platform split). **Sub-part 2, master-detail player-moderation screen: still parked, but the original 3-repo block cleared and a new, narrower one replaced it.** `xindeler-auth` O-01, `xindeler-new-horizon` BL-84, and `xindeler-zuul` ZG-56 all shipped and merged for real 2026-08-21 (confirmed against actual merged source, not assumed) — full account-level moderation flags (yellow/red, escalation, revocation) and connection-level ban/kick/unban, orchestrated through `GET/POST /api/v1/players/{uuid}[/flags|/kick|/ban|/unban]` on `xindeler-zuul`, the only backend this app talks to for this feature. Real contract confirmed by deep-reading all three repos' merged source (not the original speculative ticket text): `player_detail` returns account + character list combined in one call; flag escalation is time-based-automatic decay (3/7/30 days per rung) but the 4-total-ever-yellow and 2nd-red thresholds both route to the same terminal `Deactivated` state (not a distinct "banned" state as originally described); unban is all-or-nothing (revokes every outstanding flag, not just the triggering one); a ban's connection-half failure doesn't surface why. **New, narrower blocker found while scoping the actual screen**: there is no way to navigate from "tap an online player" to a uuid — `GET /players` (this app's existing Jugadores screen) returns bare online aliases only, no uuid, and no search/lookup-by-name route exists anywhere. Filed as `xindeler-new-horizon` **BL-86** (PR #221, source-side fix — expose uuid in `ListPlayers`) and `xindeler-zuul` **ZG-57** (PR #74, consumer-side, parked on BL-86). **Separately, and unrelated to the moderation feature itself: found and fixed OC-62** (this app's own Jugadores screen was already broken against the real gateway — see that row — discovered while confirming the real `/players` shape for this investigation). **Sub-part 3, orientation handling: ✅ Done 2026-08-21**, `docs/specs/2026-08-21-orientation-handling-design.md` / `docs/superpowers/plans/2026-08-21-orientation-handling-implementation.md`. Real gap found by investigation, not guessed: `useBreakpoint.ts`'s `WIDE_BREAKPOINT = 768` only considered width, so a landscape phone (~700-850px wide × ~350-430px tall, can fall on either side of 768) kept getting the bottom `<Tabs>` bar — the worst-looking case, since `EnvironmentBadge`/`StreamStatusBanner` above it eats a large fraction of a landscape phone's little vertical space. `useWindowDimensions()` (already used by `useBreakpoint`) confirmed genuinely reactive to rotation out of the box (read RN's own source) — no new dependency needed, no `expo-screen-orientation`. **Task 1**: `useBreakpoint()` now returns `'wide'` when `width >= WIDE_BREAKPOINT` **or** `width > height` (landscape) — one-line logic change, portrait provably unaffected since `width > height` is false by definition whenever `height >= width`. Zero changes needed to `app/(tabs)/_layout.tsx` (the sole consumer). **Task 2**: `ConfirmByTypingSheet.tsx`, `StepUpPrompt.tsx`, `KeyboardShortcutsHelp.tsx` each got their card content moved into a height-capped `ScrollView` (`max-h-[85%]`/`max-h-full` on the outer `View`) so a short landscape screen with the keyboard open can scroll to reach "Confirmar"/"Cancelar" instead of clipping them — same `ScrollView`/`keyboardShouldPersistTaps="handled"` idiom already used in `OracleComposerScreen.tsx`/`OracleDryRunScreen.tsx`, not a novel pattern. No props/exports/handler logic changed in any of the three, confirmed by both task and final review. Both tasks passed review clean, no findings, no fix loop needed for either. **Manual verification, done with real device automation** (not simulated): confirmed portrait unchanged, confirmed a landscape phone genuinely renders `SidebarLayout` instead of bottom tabs, and confirmed the full `ConfirmByTypingSheet`→`StepUpPrompt` flow (typing, keyboard, button state transitions, a real mock action completing) works correctly with the new `ScrollView` wiring in portrait. ⚠️ **One acceptance criterion not visually proven**: a clean, legible screenshot of a modal specifically while settled in landscape with the keyboard open — blocked by a genuine Simulator screenshot-pipeline artifact and a reproducible touch-misrouting quirk once landscape-settled (not a code issue; the same tap/rotation mechanism worked correctly for 8+ other interactions at different coordinates). Both the task reviewer and the final whole-branch reviewer independently reasoned through RN/Yoga's layout mechanics (a `View{maxHeight}` child of a definite-height `flex-1` parent correctly resolves percentage/max-height, `ScrollView` then bounds to it — the textbook idiom) and judged this an acceptable, low-risk gap: the higher-risk regression surface (interaction logic, not layout geometry) was empirically proven in portrait, and the worst case if the layout reasoning were somehow wrong is a no-worse-than-before failure mode (content renders at natural height, doesn't scroll), not a new regression. `KeyboardShortcutsHelp.tsx` also got the same `ScrollView` treatment for consistency even though it currently has no reachable trigger on iOS at all (`useTabShortcuts` is `Platform.OS === 'web'`-gated, pre-existing, unrelated to this ticket) — confirmed harmless by both reviewers, not dead code, just untriggered on the one platform this branch was verified against. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing repo-wide warnings), `npm run format:check` all clean throughout both tasks. **This closes out OC-35's client-actionable scope** — only sub-part 2 (master-detail) remains, parked on the three cross-repo backend tickets. <br><br>**Update 2026-08-22 — O-02 contract change, still parked, investigation only.** `xindeler-auth`'s O-02 (paginated player-listing endpoint) got redesigned for a real security issue: the first version leaked every player's raw uuid and email through the listing, turning the service credential into a bulk-email-export tool — fixed by replacing both with an opaque `reference` string and a separate listing-only credential. `xindeler-zuul` has its own task to adapt its direct integration with `xindeler-auth` to this new contract. **Investigated whether this applies directly to `xindeler-overlord` — it does not, confirmed against real code, not assumed:** this app never calls `xindeler-auth` directly (every `baseUrl` in `environments.ts` points at a Zuul instance; the only players-related call today is `/api/v1/players`, Zuul's own prefix). There is also currently no code anywhere in this app that consumes a paginated listing at all — the existing "player-accounts" screen (`src/features/playerAccounts/PlayerAccountsScreen.tsx`) is an unrelated single-purpose 2FA-unlock form (type a username, unlock), not a directory/listing UI. **Net effect: nothing to change in this app's code right now.** But the eventual sub-part 2 build (whenever BL-86+ZG-57+O-02 all land and this row un-parks) will consume *Zuul's own* re-exposed shape, not `xindeler-auth`'s contract directly — and Zuul hasn't finished adapting yet. Recorded here so the eventual implementer doesn't have to reconstruct this context: the real `xindeler-auth` O-02 contract (for reference, since Zuul's own re-exposed shape may or may not mirror it) is `GET /admin/players?cursor&limit&state` → `{ players: [{ reference, display_username, account_state }], next_cursor }` (no more `uuid`/`email` per row, `reference` is opaque and must be passed back unmodified to the detail route, which now accepts either a real `uuid` or a `reference` in the same path segment). **Do not build sub-part 2's data layer until Zuul's own adapted contract is confirmed against its real merged source** — same discipline as every other cross-repo dependency in this backlog. <br><br>**Actualización 2026-08-23 — Sub-parte 2 completada y mergeada.** El contrato real terminó siendo el de `docs/specs/2026-08-23-player-moderation-master-detail-design.md` (confirmado contra el código real de `xindeler-zuul` en su rama `development`, ver ZG-60 — no el contrato de O-02 de arriba, que resultó no aplicar directamente a esta pantalla). Construido vía subagent-driven-development, `docs/superpowers/plans/2026-08-23-player-moderation-master-detail-implementation.md`, 6 tareas: (1) capa de datos — schemas + rutas nuevas en `tools/mock-gateway` para `/players/directory` y `/players/{segment}`; (2) pantalla maestra "Jugadores" (reemplaza la anterior, solo-online); (3) ruta dinámica nueva `app/(tabs)/players/[reference].tsx` con header de cuenta + flag/kick/ban/unban reusando `ConfirmByTypingSheet`/`useDestructiveAction`; (4) lista de personajes + acción "Suspender" por personaje; (5) checkbox de ban-por-email en el flujo de ban existente. Las dos capacidades nuevas (ban-por-email, ban-por-personaje) no tienen backend real todavía — quedaron marcadas en el código como "EXPECTED SHAPE, NOT CONFIRMED", tal como pedía el diseño. **La revisión final de rama (modelo más capaz, escrutinio elevado por tratarse de acciones destructivas/step-up) encontró 2 Critical + 6 Important**, todos arreglados en una sola tanda y re-revisados limpios antes de mergear: el Critical más serio era que la nueva ruta `players/[reference]` no estaba excluida del tab-bar (`app/(tabs)/_layout.tsx`), lo que abría una 7ma pestaña fantasma en ancho angosto/nativo — invisible en desktop-web porque ese viewport usa `SidebarLayout`, un code path totalmente distinto; el segundo Critical era que el campo `outcome` de ban/unban (`success`/`banned_account_only`/`banned_connection_only`/`failed`) se descartaba en la UI, reportando éxito total ante fallas parciales. Un bug real de layout (el botón "Suspender" ocupaba casi todo el ancho de la fila por reusar el `Button` compartido, que hardcodea `w-full`) se encontró solo por verificación visual en vivo, no por revisión de código — arreglado con un `CompactCharacterButton` local sobre `Pressable`, siguiendo el patrón ya usado en `OperatorRow.tsx`. Mergeado a `development` vía PR #72 (2026-08-23, fixes del review final en el commit `e34ee44`). Toda la verificación manual al momento del merge fue contra `tools/mock-gateway` en un navegador de escritorio únicamente — ancho angosto y nativo (iOS/Android) quedaron sin ejercer en vivo. <br><br>**Actualización 2026-08-27 — verificación nativa cerrada, sub-parte 2 (y por lo tanto OC-35 completo) ahora ✅.** Se corrió la app en un simulador real de iPhone 17 (iOS 26.5) contra `tools/mock-gateway`, usando `xcrun simctl` para instalar/lanzar/capturar pantalla y una versión parcheada del CLI `idb` de Facebook para tap/texto — ver la sección nueva "7. Automating a real iOS Simulator" en `.claude/skills/ops-run/SKILL.md` para el método completo, ya que `claude-in-chrome` solo controla pestañas de Chrome, no simuladores nativos. Confirmado en vivo: el tab-bar muestra exactamente 6 pestañas visibles de 13 rutas totales registradas (6 visibles + 7 con `href: null`, incluida la nueva `players/[reference]`) — cierra el Critical del opt-out faltante; el `CompactCharacterButton` renderiza compacto y alineado a la derecha, no full-width — cierra el bug de layout; el flujo completo de una acción destructiva (razón → `ConfirmByTypingSheet` → step-up TOTP → mutación → refetch → actualización de UI) se ejerció de punta a punta suspendiendo un personaje real contra el mock, terminando en "Suspendido"/"Levantar suspensión"; y un flag amarillo confirmó que el metadata (`issued_at` + `issued_by_operator_uuid`) del fix del review final renderiza correctamente. Con esto se cumple la barra de verificación de este mismo repo (`ops-run`: "al menos dos de las tres plataformas"), que no se había cumplido al momento del merge. | ✅ |
| OC-36 | Android-specific pass | Split en 2 tickets/PRs per Matías (chat, 2026-08-21) — la investigación previa encontró una asimetría real de tamaño entre las 4 áreas originales: back-button y notification channels ya estaban resueltos por el framework/código existente (casi nada que hacer, solo verificar), edge-to-edge era un gap chico (config + verificación), pero Material ripple/press states es un gap real y grande (cero `android_ripple` en toda la app, ~25 archivos con `Pressable` sin feedback visual en Android). **Sub-part 1 (OC-36a), verificación de plataforma (back-button + notification channels + edge-to-edge): ✅ Done 2026-08-21**, `docs/specs/2026-08-21-android-platform-verification-design.md` / `docs/superpowers/plans/2026-08-21-android-platform-verification-implementation.md`. Cambio de una sola línea efectiva en `app.config.ts`: `predictiveBackGestureEnabled` re-habilitado a `true` (estaba en `false` desde el scaffold original de OC-3 sin ninguna razón documentada). **Desviación del plan, justificada y verificada de forma independiente**: la propuesta original también incluía `edgeToEdgeEnabled: true`, pero esa propiedad ya no existe en el tipo `Android` de Expo SDK 57.0.11 — Android 16 hace obligatorio el edge-to-edge sin excepción, así que el SDK removió la opción; confirmado con un error real de `tsc` (`TS2353`) reproducido de forma independiente tanto por el implementador como por ambos reviewers, no solo asumido. Se omitió la propiedad y se documentó inline en el comentario del archivo. Task único, pasó review de task y revisión final de rama ambas limpias, sin findings bloqueantes. **Verificación manual real en emulador Android** (`xindeler-ops-test` AVD): (1) el botón de back cierra un `ConfirmByTypingSheet`/`StepUpPrompt` real exactamente igual que tocar "Cancelar" — confirmado con capturas antes/después. (2) navegación entre pantallas con back funciona, aunque reveló un hallazgo real no relacionado a este ticket (ver OC-61 abajo) — diagnosticado con un experimento de control (togglear el atributo de manifest que controla la flag, con y sin ella, resultado idéntico) que descarta que este cambio lo haya causado. (3) edge-to-edge confirmado con `dumpsys window`/`display`: el contenido no queda debajo de la status bar (136px) ni de la barra de gestos (63px). (4) canal de notificación verificado de punta a punta (no solo best-effort): `dumpsys notification` confirma `NotificationChannel{mId='default', mImportance=5}` (MAX) tras conceder el permiso y activar push desde la UI. `npx tsc --noEmit`, `npm run lint` (0 errores, mismos warnings preexistentes), `npm run format:check` todo limpio. **Sub-part 2 (OC-36b), Material ripple/press states: ✅ Done 2026-08-21**, `docs/specs/2026-08-21-material-ripple-press-states-design.md` / `docs/superpowers/plans/2026-08-21-material-ripple-press-states-implementation.md`. Un wrapper nuevo, `src/ui/Pressable.tsx`, reemplaza el `Pressable` de `react-native` en los 25 archivos que lo usaban (no 24 — la investigación previa había contado mal por uno; el plan y el diff real ya usan 25 de forma consistente). El wrapper agrega `android_ripple` (color del tema, `colors.accentMuted`, overridable) en Android, y un dimming de opacidad (`opacity: 0.6` mientras `pressed`) en iOS/Web vía la función `style={(state) => ...}` de RN — compuesto correctamente con cualquier `style`/`android_ripple` que un caller pase (verificado explícitamente por dos reviewers distintos que la lógica de composición no descarta ni duplica overrides), aunque ningún consumidor real de los 25 ejerce ese camino hoy (todos usan el default). **Task 1** (el wrapper) y **Task 2** (la migración de los 25 imports, cambio mecánico, cero className/prop/lógica tocada en ningún archivo — confirmado línea por línea por el reviewer) ambas pasaron review de task limpias. Revisión final de rama también limpia — incluyó un chequeo específico de si el ripple/dim tiene sentido semántico en los dos usos menos obvios (`StatusScreen.tsx`'s toggle `accessibilityRole="switch"`, `EnvironmentSwitcher.tsx`'s `accessibilityRole="radio"`): confirmado que ambos son componentes custom-estilizados (no widgets nativos de switch/radio), así que un flash de feedback al tocar no choca con ninguna animación de "checked" existente. **Verificación manual real en Android**: ripple de Material confirmado con evidencia de pixel-diff en 4 tipos distintos de `Pressable` (botón completo, fila de lista, chip, link de texto) y confirmado que un control deshabilitado no muestra ningún feedback (diff de píxeles cero). **Verificación manual real en iOS**: el flujo funcional completo (login→TOTP→Status→modales) confirmado funcionando con los imports migrados, y la supresión en estado deshabilitado confirmada con el mismo método de pixel-diff (cero diferencia) — pero el dimming de opacidad durante una pulsación sostenida en sí **no se pudo capturar visualmente**, pese a un intento genuino con 4 enfoques de automatización distintos (`idb ui tap` en 11 offsets de tiempo distintos, `idb ui swipe`, grabación de video + extracción de frames, y `cliclick`/`osascript` bloqueados por permisos de Accesibilidad no otorgables sin acceso interactivo) — el gesto llega correctamente a la app (`onPress` dispara, abre los sheets), es específicamente el frame intermedio "presionado" el que ninguna herramienta de este equipo pudo capturar en pantalla. Dos revisores independientes (el de task y el de rama completa) juzgaron esto un gap aceptable y no bloqueante: la lógica del wrapper (Task 1) ya estaba verificada de forma independiente como correcta por razonamiento explícito de composición, la mitad de Android del mismo condicional quedó confirmada visualmente, la supresión en disabled quedó confirmada en ambas plataformas con el mismo booleano `pressed`, y Task 2 no tocó ninguna lógica (solo imports) — pero queda anotado como lo único de todo este ticket que ningún método (automatizado o manual) mostró en vivo en vez de inferir; una prueba manual real de 10 segundos en un dispositivo/Simulador cerraría esto de forma definitiva. `npx tsc --noEmit`, `npm run lint` (0 errores, mismos warnings preexistentes), `npm run format:check` todo limpio en ambas tasks. **Esto cierra por completo OC-36.** | ✅ |
| OC-37 | iOS-specific pass | ✅ Done 2026-08-21, `docs/specs/2026-08-21-oc37-closure-and-sheet-inset-design.md` / `docs/superpowers/plans/2026-08-21-oc37-closure-and-sheet-inset-implementation.md`. Investigación previa a diseñar encontró que 3 de las 4 áreas originales ya estaban resueltas o no eran un problema real, muy distinto al patrón de OC-35/OC-36: **safe areas / Dynamic Island** ya bien manejado — `expo-router` envuelve toda la app en `SafeAreaProvider` automáticamente, `EnvironmentBadge.tsx` absorbe el inset de arriba una sola vez de forma global, `Screen.tsx`/`SidebarLayout` excluyen `top` deliberadamente para no duplicar padding (documentado inline en ambos), sin valores de píxeles hardcodeados en ningún lado. **Swipe-back gesture vs custom headers**: no existe el conflicto — toda la navegación usa `headerShown: false`, cero headers nativos o custom en toda la app, `gestureEnabled` nunca se pone en `false`; el link "Volver" de `app/(auth)/totp.tsx` es contenido normal, no un botón de header, así que coexiste bien con el gesto nativo de swipe. **Background/foreground stream resume**: ya implementado — `src/stream/StreamContext.tsx` ya tiene un listener de `AppState` que llama `client.reconnectNow()` al volver a `'active'` (confirmado en la revisión final de rama, línea por línea). **Face ID gate**: ya shippeado completo como **OC-46** (`✅ Done 2026-08-15`, `src/auth/AppLockGate.tsx`/`AppLockScreen.tsx`). **Único detalle real encontrado y arreglado**: `ConfirmByTypingSheet.tsx` y `StepUpPrompt.tsx` no manejaban el inset de abajo (home indicator) — podían quedar con contenido pegado al borde inferior en iPhones sin botón físico. El plan original proponía envolver el contenido en un `<SafeAreaView edges={['bottom']}>` nativo (mismo patrón que `Screen.tsx`), pero **la verificación manual obligatoria encontró que esa solución literal no funcionaba en absoluto**: produce inset cero cuando se renderiza dentro de un `<Modal>` de React Native en iOS — una limitación real y documentada de `react-native-safe-area-context` (el componente nativo `SafeAreaView` lee los insets de UIKit directamente de su propia vista nativa, que no se propagan correctamente dentro de la jerarquía de ventanas nativas separada que crea un `Modal`). Diagnosticado con un probe de fondo magenta mostrando el frame del `SafeAreaView` extendiéndose hasta el borde físico pese a que `useSafeAreaInsets()` reportaba correctamente `bottom: 34` en el mismo árbol. Arreglado reemplazando el `SafeAreaView` nativo por el hook `useSafeAreaInsets()` (basado en contexto de React, no en la vista nativa propia, así que no le afecta en qué superficie nativa termina renderizando) + un `paddingBottom` manual — verificado en vivo con capturas reales mostrando el espacio visible antes/después. Commiteado en dos pasos deliberados (el fix literal del plan primero, después la corrección documentada) en vez de amend, dejando la historia clara de qué se probó y por qué falló. La revisión final de rama hizo un barrido específico buscando otras instancias latentes del mismo bug (`SafeAreaView` dentro de `Modal` en cualquier otro lado de la app) — ninguna encontrada; los otros usos de `SafeAreaView` (`Screen.tsx`, `EnvironmentBadge.tsx`, `app/(tabs)/_layout.tsx`) nunca renderizan dentro de un `Modal`. `KeyboardShortcutsHelp.tsx` y `AppLockScreen.tsx` sí renderizan dentro de `Modal` pero nunca usaron `SafeAreaView` en absoluto — una condición distinta y preexistente, juzgada de bajo riesgo (contenido corto y fijo, o centrado verticalmente respectivamente) y fuera de alcance de este fix. `npx tsc --noEmit`, `npm run lint` (0 errores, mismos warnings preexistentes), `npm run format:check` todo limpio. | ✅ |
| OC-38 | Web deploy | **Done 2026-08-21.** Cierra el ciclo cross-repo con `xindeler-zuul`'s ZG-58 (ya mergeado — sirve el export estático de esta app vía `tower-http::ServeDir`, mismo origen `zuul.xindeler.com`). Design: `docs/specs/2026-08-21-oc38-web-deploy-design.md`; plan: `docs/superpowers/plans/2026-08-21-oc38-web-deploy-implementation.md`. <br><br>**Task 1 — fix del CSP (reset de layout):** el plan original asumía que `public/index.html` (la convención `EXPO_PUBLIC_FOLDER` de Expo) overridea el template de export — confirmado FALSO para esta app en la propia verificación real (`expo export --platform web`): esta app usa `web.output: 'static'` (Expo Router SSG), que bypassea ese mecanismo por completo (`useServerRendering` filtra `'web'` fuera de `spaPlatforms`, así que `createTemplateHtmlFromExpoConfigAsync` nunca corre para web). El mecanismo real para SSG es `app/+html.tsx`, que overridea el componente `Html` default de `expo-router/html`. Corregido: `public/index.html` borrado (inerte, confirmado 0/32 archivos de ruta con el mecanismo activo), `public/expo-reset.css` mantenido, `app/+html.tsx` nuevo linkeando ese CSS en vez del `<style id="expo-reset">` inline. Re-verificado en la re-visión final de esta rama: 0/32 archivos HTML exportados contienen el style inline, los 32 linkean `/expo-reset.css`, byte-idéntico al archivo fuente. <br><br>**Task 2 — perfil de entorno `public`:** `src/config/environments.ts` ganó una tercera entrada (`id: 'public', label: 'Público', baseUrl: 'https://zuul.xindeler.com'`), `DEFAULT_ENVIRONMENT_ID` sin tocar (sigue `'mock'` — el build desplegado sigue arrancando en mock, el operador cambia manualmente vía `EnvironmentSwitcher`, mismo patrón ya establecido de "el entorno activo nunca se asume"). Verificado en vivo en `npx expo start --web`: las tres opciones (Mock/WireGuard/Público) aparecen y son seleccionables. <br><br>**Task 3 — CI de deploy:** `.github/workflows/deploy-web.yml` nuevo, separado de `ci.yml`. Trigger en push a `development` + `workflow_dispatch` manual. Exporta el build Web y lo sincroniza vía `rsync -avz --delete --safe-links` a `VPS_SSH_USER@VPS_SSH_HOST:/opt/xindeler-zuul/web/` — el path exacto que `xindeler-zuul`'s ZG-58 ya espera, poblado por este pipeline independiente. `--safe-links` es el hardening que la revisión final de ZG-58 pidió explícitamente (`tower-http::ServeDir` sigue symlinks sin guard contra que apunten fuera del árbol servido). **Queda inerte hasta que Matías agregue `VPS_SSH_HOST`/`VPS_SSH_USER`/`VPS_SSH_KEY` como secrets de GitHub Actions en este repo** — sin ellos, el workflow corre igual hasta el paso de SSH y falla ahí con un error de conexión, no antes; esto es el comportamiento diseñado, no un bug. No se pudo verificar el paso de rsync end-to-end sin esos secrets/acceso real al VPS — documentado explícitamente como limitación de esta verificación, no como algo pendiente de arreglar acá. <br><br>**Nota de seguridad de la sesión, no bloqueante:** el propio harness marcó el commit de la Task 3 para revisión, porque el workflow apunta al VPS de producción (`/opt/xindeler-zuul/web/`, con `--delete`). Revisado por el controller: el archivo está commiteado solo localmente en esta rama en el momento de escribir esto, no tiene ningún secret configurado (inerte de verdad), y el path/mecanismo es exactamente el que ZG-58 diseñó para ser poblado por un pipeline externo — no es config nueva ni no revisada de Zuul. Igual, avisado explícitamente a Matías al cerrar esta ticket, ya que es el paso que activa el primer deploy real una vez que él agregue los secrets. <br><br>**Testing:** `npx tsc --noEmit` / `npm run lint` (0 errores) / `npm run format:check` — todo limpio en las 3 tareas juntas. `expo export --platform web` real corrido y verificado múltiples veces a lo largo de la implementación. <br><br>**Follow-ups, todos 2026-08-22, ya mergeados:** (1) Matías pidió que el deploy solo se dispare al mergear a `main` y cortar un tag versionado, no en cada push a `development` — trigger cambiado a `push: tags: ['v*.*.*']`, con un chequeo real de `git merge-base --is-ancestor` contra `origin/main` (mismo enforcement que `deploy.sh` de `xindeler-zuul` ya hace, ZG-41), aplicado tanto al trigger de tag como a `workflow_dispatch` manual. (2) Configuración real del VPS, hecha por esta sesión con confirmación explícita de Matías: `/opt/xindeler-zuul/web` creado (sin sudo — `mgrinberg` ya es miembro del grupo `xindeler-zuul`, confirmado que `WEB_DIST_DIR` no está sobreescrito en el `.env` del servicio y que `ProtectSystem=strict` de su unit de systemd no bloquea la lectura, ningún restart necesario); clave SSH dedicada generada y instalada en `authorized_keys` restringida vía `command="rrsync -wo /opt/xindeler-zuul/web/"` (confirmado que un login normal con esa clave es rechazado); los 3 secrets (`VPS_SSH_HOST`/`VPS_SSH_USER`/`VPS_SSH_KEY`) cargados por Matías mismo — la clave privada nunca pasó por esta sesión hacia GitHub. (3) Probando la clave real de punta a punta se encontró un bug real que ninguna verificación anterior pudo detectar (Task 3 documentó explícitamente esa limitación): `rrsync` ya hace `chdir` al directorio restringido antes de aplicar el path del cliente, así que repetir el path absoluto en el destino del `rsync` lo duplicaba (`.../web/opt/xindeler-zuul/web`) y el transfer fallaba. Arreglado (destino sin path, solo `user@host:`), reverificado en vivo contra el VPS real con subdirectorios, `--delete` y `--safe-links`. (4) Esquema de versionado documentado a pedido de Matías: `docs/reference/versioning.md` — `vMAJOR.MINOR.PATCH`, MAJOR reservado para producción real/cambios que rompen retrocompatibilidad (queda en `0` mientras estemos en Beta), MINOR para cada release Beta significativo, PATCH para bugfixes sobre una versión ya cortada. Primer release real acordado: `v0.1.0`, todavía no cortado — decisión de Matías, no de esta sesión. | ✅ |
| OC-39 | Error/telemetry | Crash + error reporting that does **not** phone home to a third party with ops data — prefer logging to the gateway. Decide explicitly rather than defaulting to Sentry. | ⬜ |
| OC-40 | E2E smoke on device | Maestro (or equivalent) run against the mock gateway, in CI where possible. | ⬜ |

---

## Phase 5 — Chat with ORACLE

Maps to NH-75 P5. Gateway-side work dominates; the app's job is the chat surface and reusing the
OC-33 preview card.

| ID | Item | Notes | Status |
|---|---|---|---|
| OC-41 | Chat UI with token streaming | ✅ Done 2026-08-15, per `.superpowers/sdd/2026-08-15-oracle-chat-implementation` plan. Three tasks. **Task 1** (`129d73a`, fixed by `e31cb0b`): `OracleChatTokenSchema`/`DmEventSchema` reused for the terminal draft, `ChatTurn`/`ChatThread` types (`src/features/oracleChat/types.ts`), `streamOracleChat.ts` — a **standalone, one-shot async generator**, deliberately **not** wired through the OC-17 shared `/api/v1/stream` connection despite the row's original note suggesting reuse: `POST /api/v1/oracle/chat` opens its own `text/event-stream` response directly on the POST request itself (mirroring the mock's own §6 design), so there is nothing to subscribe to on the shared stream and no reconnect/backoff logic — the mock's stream just ends after the terminal `draft` event or an error. Fix `e31cb0b`: malformed JSON in a single SSE event (token or draft) is now skipped rather than throwing and killing the rest of the stream. **Task 2** (`f9885fb`): `useOracleChatThreads()` — an **in-memory, session-only** thread list (`useState`, no persistence), because the gateway has no fetchable chat history endpoint at all (§6 is fire-and-respond only); reload loses every thread, which is the honest reflection of what the backend actually offers, not a missing feature. `send`/`retryTurn` share one `runAssistantTurn` that consumes `streamOracleChat`'s `token`/`draft` events, appending token text and, on a terminal `draft`, marking the turn `complete`. **Honest-failure discipline**: a stream that ends without ever emitting a terminal `draft` event is treated as `failed`, not silently left as a half-written turn displayed like a success — matching OC-34's own "an indeterminate outcome is not a success" pattern. `retryTurn` re-runs the assistant turn against the **existing** assistant turn id and the **original** operator turn's text — it never creates a new operator turn. `ChatTurnRow` (presentational, `memo`'d) is the **single component** that renders a turn's role label, text, failed-state + Reintentar, the draft block, and the Copiar affordance (clipboard write via `expo-clipboard`, local `copied` UI state auto-reset after 1.5s) — deliberately one undifferentiated component rather than splitting per-turn-state variants, so the visual pass Matías has deferred for the rest of Phase 5 has exactly one place to land later. **Task 3** (this row's own commit): `OracleChatScreen.tsx` — thread chips + "+ Nueva conversación", a `FlatList` of `ChatTurnRow`, and the message composer (`TextField` + `Enviar`, disabled while `sending` or empty). `app/(tabs)/oracle-chat.tsx` mounts it inside `Screen`; `app/(tabs)/_layout.tsx` gained a fourth `href: null` entry (alongside `audit`/`oracle-composer`/`oracle-trigger`) so it doesn't appear as an unlabeled phone-width tab. `OracleEventsScreen.tsx` gained a "Chat con ORACLE" link directly below "Componer evento" (same bordered-row-with-chevron pattern, tighter `mt-2` spacing so the pair reads as connected). **Deliberately inert draft display**: the `kind`/`template_id`/`intensity`/`radius` fields and "Aplicar: pendiente (OC-42)" note are read-only — no Apply button anywhere, since OC-42 owns that. **Live verification**, full pass against `npm run mock-gateway` + `npx expo start --web` (already-running processes confirmed newer than the last relevant mock-gateway route change; logged in `matias`/mock, TOTP `000000`), all 7 of this task's checks, all directly observed: (1) "Chat con ORACLE" rendered below "Componer evento" and navigated to `/oracle-chat`; the phone-width tab bar was confirmed to omit it by temporarily forcing `useBreakpoint.ts`'s threshold to an unreachable value (never committed, reverted immediately after the screenshot, `git diff` empty afterward) — the same technique OC-11 used for its own breakpoint check, since this browser automation tool's window resize doesn't reliably change the page's actual viewport. (2) Token-by-token growth was captured by sampling the DOM's rendered text length at ~1s intervals mid-stream (151 → 323 → 496 characters, then flat once the terminal draft landed) — not an instantaneous jump. (3) The draft block rendered `kind`/`template_id`/`intensity`/`radio` and the "Aplicar: pendiente (OC-42)" note on every completed turn across three separate threads; no Apply control was ever present. (4) Copiar was verified against the **actual** clipboard write, not just the UI toggle: `navigator.clipboard.writeText` was wrapped in-page to record its argument, and tapping Copiar under a completed ORACLE turn recorded exactly that turn's own text. (5) A stream failure was forced by registering a temporary same-origin Service Worker (a throwaway static file under a newly-created `public/`, deleted immediately after, confirmed via `git status` leaving no trace) that intercepted only `POST …/api/v1/oracle/chat` and rejected it — needed because `useOracleChatThreads.ts` imports `expo/fetch`'s `fetch` export and binds it as the injected `fetchImpl` passed into `streamOracleChat` (that function itself is DI'd — `fetchImpl` is just `StreamOracleChatDeps`, accepting either `expo/fetch` or Node's global `fetch`, mirroring `StreamClient.ts`'s own pattern) — on web, `expo/fetch`'s export is a reference captured once at module load, since it's just `globalThis.fetch` re-exported, so reassigning `window.fetch` from the page console — the technique OC-34 used for its own plain-`httpClient`-based POST — doesn't reach it; a Service Worker intercepts at the network layer regardless of which JS reference issued the request. The blocked turn rendered exactly "No se pudo completar esta respuesta." + "Reintentar", no partial or invented content. Updating the same SW to stop blocking (still logging bodies transparently) and tapping Reintentar showed the turn complete successfully, the operator-turn count in that thread unchanged (3 before, 3 after — no duplicate), and the resent request's own captured body was byte-identical in its `message` field to the original failed attempt's operator text. (6) "+ Nueva conversación" created independent threads (three were used across this pass); switching back to an earlier chip after activity in a later one showed its turns untouched. (7) No tier-switch control or budget figure appeared anywhere on the screen — confirmed both visually and via a DOM text scan for "tier"/"bedrock"/"budget"/"presupuesto"/"$", all negative. Zero console errors observed across the whole pass. `npx tsc --noEmit` clean, `npm run lint` 0 errors / 56 warnings (identical pre-existing `import/no-duplicates` resolver baseline), `npm run format:check` clean. **Final-review fix wave (2026-08-14)** — a whole-branch review found no Critical issues but 7 Important and 5 Minor ones; all 12 fixed in one pass. **(1) Errors are no longer swallowed.** `streamOracleChat.ts` now throws `ApiError` (never a bare `Error`) on every failure path, classifying the way `httpClient.ts` does: `network_error`/`timeout` at status 0, the parsed error envelope when the gateway sends one, and a synthesized `unauthorized` for a 401 with an unparseable body. `ChatTurn` gained an `error: Error | null` field; the hook stores the real error on the turn instead of discarding it, logs a `console.warn` breadcrumb (there was none at all before), and feeds it to `useAuthErrorRouting` — so **a 401 on this screen now drops to the login screen** instead of failing and retrying forever, and `ChatTurnRow` renders the error through `ActionError`/`gatewayErrorMessage` (VPN diagnosis and `VpnSettingsButton` included on `wireguard`) instead of the hardcoded "No se pudo completar esta respuesta." string. **(2) Timeout + abort actually wired.** `abortRef` was created but never read; it is now aborted on unmount and before each new turn, state updates are gated on a `mountedRef`, and `streamOracleChat` owns an internal controller with a 10s deadline (this app's `httpClient`/`StreamClient` convention) armed before the POST and **re-armed on every SSE event**, so a backend that stalls mid-response no longer pins the global `sending` flag `true` and disables the composer forever. **(3) A failed turn no longer shows its partial text** or a "Copiar" affordance — the renderer contradicted the hook's own honest failure classification. **(4) The headline no-terminal-`draft` branch was finally live-verified** (see below) — the original pass only exercised request-level failure. **(5) Follow-tail added.** Note for anyone porting `ChatScreen.tsx`'s implementation again: **`scrollToEnd()` is a silent no-op on this list** (verified: 0px moved with 185px of scrollable height — `VirtualizedList` never gets cell layout metrics here, and `onContentSizeChange` fires exactly once with `height: 0`), and **react-native-web only emits `onScroll` for user-driven scrolls**, so `ChatScreen.tsx`'s previous-offset direction check can never fire. This screen uses `scrollToOffset` to the content bottom, and disengages on any user scroll event that reports it meaningfully away from the bottom — same race-free property, different mechanism, both documented in-file. **(6) Design-spec correction:** the claim that threads "survive navigating away and back within the same session" is **false on the wide/sidebar layout** — `SidebarLayout` renders `<Slot />`, which unmounts the route. Now recorded as a known limitation; hoisting the state into a provider is deliberately out of scope. **(7)** OC-42's row gained a pointer to the design spec's "Why the OC-33 card doesn't literally apply" analysis. **(8)** `ChatTurnRow`'s `memo()` was a no-op — `retryTurn` now reads threads/sending through refs (the `playersRef` pattern from OC-32/33) and `onRetry` takes a turn id so the screen passes one stable callback; `renderItem` is `useCallback`'d. **(9)** Thread chips now use the shared `ChipPicker` instead of a hand-rolled copy missing its `accessibilityState`. **(10)** Two stale doc identifiers fixed (`useOracleChatStream` → `useOracleChatThreads`; `ChatTurnRow({turn, onCopy, onRetry})` → `{turn, onRetry}`). **(11)** `handleSend` no longer clears the composer before `send`'s own guards. **(12)** `Accept: text/event-stream` added to the chat POST. **Live re-verification, all five checks directly observed** against `npm run mock-gateway` + `npx expo start --web`, using a throwaway same-origin Service Worker (under a temporary `public/`, deleted and unregistered afterwards, `git status` clean) that reshaped only `POST /api/v1/oracle/chat`: (a) a rejected request rendered "No se pudo conectar con el gateway" — real diagnostic content, not the old hardcoded string — plus a `[oracle-chat] assistant turn failed ApiError:` console breadcrumb; (b) **finding 4's exact shape** — 200 + 8 real `token` events + connection closed with no `draft` — showed the tokens streaming mid-flight ("Las sombras se agitan entre los ") and then, on close, **only** "La respuesta se cortó antes de completarse — el gateway nunca mandó el borrador final." + Reintentar, with the partial text and Copiar both gone; (c) follow-tail held the list pinned (gap 0px) as content grew 447→573→709px, a wheel-scroll up flipped the toggle to "Seguir" and held position while content grew 294→447px, and re-engaging jumped back to the bottom; (d) a stalled stream (200 + one token, never closed) left the composer disabled for ~12s and then recovered on its own with "La respuesta del gateway se quedó sin avanzar", composer editable again, and Reintentar then completed normally; (e) `ChipPicker` chips behaved identically — a new thread started empty, thread 1 kept all 5 of its turns, and switching back and forth preserved each independently. Additionally verified beyond the five: a **401 with a `session_expired` envelope routed straight to `/login`**, which is the finding-1 gap that mattered most. `npx tsc --noEmit` clean, `npm run lint` 0 errors / 56 warnings (unchanged pre-existing `import/no-duplicates` resolver baseline), `npm run format:check` clean. **This closes out this ticket; OC-42 (draft → preview → apply) is next.** | ✅ |
| OC-42 | Draft → preview → apply | ✅ Done 2026-08-15, per `docs/specs/2026-08-15-oracle-draft-apply-design.md`. **Bridge decision:** rather than building a new draft-preview surface, this reuses OC-30/31's existing preset-clone mechanism verbatim — per `docs/specs/2026-08-15-oracle-chat-design.md`'s "Why 'the OC-33 card' doesn't literally apply to OC-41" section, `OracleDryRunScreen.tsx`'s "Resultado" card is `event_id`-keyed and has no concept of an in-memory, unstaged `DmEvent`, and `/oracle/trigger` has no request field for one — closing that gap with a second bridge design was explicitly deferred to this ticket. `ChatTurnRow`'s draft block already displays a `DmEvent`-shaped object exactly like a preset does; the composer's `applyPreset()` already turns a `DmEvent` into pre-filled form state. Building a second preview card would have duplicated both without adding anything a human reviewing before staging actually needs. **Mechanism:** a route param, not shared state — `ChatTurnRow`'s new "Aplicar" button (`Pressable`, replacing the old inert "Aplicar: pendiente (OC-42)" text) calls a new `onApply: (draft: DmEvent) => void` prop; `OracleChatScreen.tsx`'s `handleApply` (`useCallback`) does `router.push({ pathname: '/oracle-composer', params: { draft: JSON.stringify(draft) } })`. `OracleComposerScreen.tsx` reads it back via `useLocalSearchParams<{ draft?: string }>()` and a new `parseDraftParam()` that `JSON.parse`s the string and re-validates it through `DmEventSchema.safeParse` — **fail-closed**: any parse or schema failure (malformed JSON, a param tampered with in the URL, a future draft shape mismatch) yields `null` silently, and every one of the seven `useState` lazy initializers (`id`, `kind`, `templateId`, `intensityText`, `radiusText`, `biomeProfile`, `weatherEffect`) falls back to exactly the same blank-form defaults the existing direct entry point already used — there is no separate "broken draft" UI state, a bad param is indistinguishable from no param at all. `id` gets a fresh `slugify(`oracle_chat_${Date.now()}`)` rather than the draft's own (it has none — a `DmEvent` has no `id` field until staged), mirroring `applyPreset()`'s own collision-avoiding timestamp suffix. No new validation was added anywhere: the pre-filled state flows through the same `buildDmEvent()`/bounds-checking/id-collision logic the hand-typed path always used. A provenance note ("Prellenado desde una propuesta de ORACLE — revisá antes de guardar.") renders directly under the title whenever `draft` is non-null. **Live verification**, all 7 of this task's checks, all directly observed against `npm run mock-gateway` + `npx expo start --web` (already-running processes; logged in `matias`/mock, TOTP `000000`): (1) sending a chat message and waiting for the terminal draft showed a real "Aplicar" button (cyan, `Pressable`) in place of the old grey "pendiente" text. (2) Tapping it navigated to `/oracle-composer?draft=…` with every field correct for the mock's `tpl_wolf_pack` spawn draft: `id` was a non-empty auto-generated slug (`oracle_chat_1786758149662`), "Tipo" showed "Aparición" selected, the Template `ChipPicker` showed "Manada de lobos" selected, intensity read `6` and radius `20` — the draft's exact values. (3) The provenance note was visible directly below the title. (4) The pre-filled `id` was edited like a hand-typed one (triple-click + retype), and setting it to `verify_oc34_reenable` (an id already loaded from a prior ticket's fixture) correctly surfaced "Ya existe un evento con este id — se reemplazará."; a fresh id cleared the warning. (5) Staging the pre-filled form end-to-end — "Guardar en etapa" → TOTP step-up modal → `000000` → confirm — succeeded exactly as the hand-typed path does, and the new event (`verify_oc42_apply`) appeared under "Cargados" on `/oracle`, proving the pre-fill only changes initial state, not the staging flow. (6) A second chat message produced the mock's other canned draft (`kind: weather`, intensity 8, radius 50 — confirmed against `oracleDraftPool` in `tools/mock-gateway/src/fixtures.js`); applying it pre-selected "Clima" and rendered the composer's existing "Los eventos de clima se guardan pero el motor todavía no los aplica al mundo — nada va a pasar en vivo cuando se dispare." warning with zero new code, purely from `kind === 'weather'` already being true post-pre-fill. (7) Navigating to `/oracle-composer` directly via `OracleEventsScreen`'s "Componer evento" link (no `draft` param) opened a completely blank form with no provenance note — the existing entry point is unaffected. `npx tsc --noEmit` clean, `npm run lint` 0 errors / 56 warnings (pre-existing `import/no-duplicates` resolver baseline, unchanged), `npm run format:check` clean. **Fix round 1 (2026-08-15, `1996d97`)** fixed two issues a scoped re-review found. **Finding 1 (phone-width re-apply):** at phone width `oracle-composer` is a sibling route inside the same `<Tabs>` navigator as `oracle-chat`, so navigating to it from chat reuses the already-mounted screen instance instead of remounting — the mount-time `useState` lazy initializers only run once, so a second applied draft on an already-open composer never took effect. Fixed not with a `useEffect` (which trips this repo's `react-hooks/set-state-in-effect` rule) but by adjusting state **during render** (React's own documented pattern, same idiom as `useLifecycleState.ts`/`StatusScreen.tsx`): a new `appliedDraft` state tracks the identity of the last-applied `draft`, and a render-body `if (draft && draft !== appliedDraft) { … }` block re-applies whenever `draft` changes — idempotent and self-terminating, redundant-but-harmless on a genuine fresh mount. **Finding 2 (hallucinated `template_id`):** `DmEvent.template_id` is an unconstrained string, so nothing guarantees an LLM-authored (or hand-edited-URL) draft's `template_id` names a template that actually exists. Fixed via `resolveTemplateId(templateId, templates)`, which rejects any id absent from the live `entity_templates` list; `templateId` state always holds the RAW value, and a `resolvedTemplateId` derived value — recomputed fresh every render, never baked into state at set-time — is what `ChipPicker` and `buildDmEvent()` actually consume, so a genuinely valid template applied before the templates query has finished loading self-corrects the moment it does instead of staying permanently stuck rejected. **Fix round 2 (2026-08-15)** — the same re-review's scoped follow-up found one new Important regression round 1 introduced, plus two Minor issues; all fixed in one pass. **The id-suggestion counter (`applySeq`) was seeded at a fixed `0`**, so its first increment was always `oracle_chat_1` on literally every fresh mount — and since desktop width unmounts and remounts this entire screen on every navigation (`SidebarLayout` renders `<Slot/>`), that made silently overwriting the previously-staged ORACLE event (the id-collision warning is a non-blocking hint, not a guard) the DEFAULT outcome of a second desktop apply, not an edge case. Fixed by seeding `applySeq` from `Date.now()` via a lazy `useState` initializer instead of a fixed `0` — a lazy initializer runs exactly once, at mount, so it's exempt from `react-hooks/purity` the same way this file's other lazy initializers are — so the first suggested id on a fresh mount is as unique as the pre-round-1 `Date.now()`-based one was, while repeated applies within the same still-mounted instance keep incrementing distinctly from each other. Also, two Minor cleanups: the `id` field's own lazy initializer (previously `slugify(`oracle_chat_${Date.now()}`)` when a draft was present) had become dead code — always immediately overwritten by the render-time re-apply block the moment a draft exists — so it was simplified to an unconditional `''`; and this row's description of the mechanism (this paragraph) is corrected here to reflect the render-time re-apply block, `resolveTemplateId`, and the seeded-counter id scheme, replacing the original implementation's now-inaccurate claims of "every one of the seven `useState` lazy initializers" handling pre-fill and an id suffix that "mirror[ed] `applyPreset()`'s own collision-avoiding timestamp suffix." `npx tsc --noEmit` clean, `npm run lint` 0 errors / 56 warnings (same baseline), `npm run format:check` clean. | ✅ |
| OC-43 | Tier switch + budget | ✅ Done 2026-08-15, per `.superpowers/sdd/2026-08-15-oracle-tier-budget-implementation/`. Two tasks. **Task 1**: `useOracleBudgetQuery()` — a plain `useQuery` read of `GET /oracle/budget` with no polling and no refetch-on-send; the budget label is a snapshot shown when the composer opens, not a live meter, so there was nothing to invent there. `send`/`retryTurn`/`runAssistantTurn` in `useOracleChatThreads.ts` gained a third `tier: 'local' | 'bedrock'` parameter (previously hardcoded to `'local'`), and `ChatTurn` gained a `tier: 'local' | 'bedrock' | null` field set once at turn creation; `retryTurn` re-reads the **existing** turn's own `tier` rather than defaulting to `'local'`, so a retried Bedrock send stays Bedrock. **Task 2**: **two explicit actions, never a toggle** — "Enviar" (unchanged `Button`, tier `'local'`) and a new "Pensar mejor" action next to it, labelled with month-to-date spend (`Pensar mejor ($3.42 este mes)` against the mock) and falling back to plain "Pensar mejor" before the budget query resolves. "Pensar mejor" is a `Pressable` + `Text`, not a second `Button` — `Button` is hardcoded `w-full` in this codebase, and two full-width buttons in the same row would break the layout; a toggle was rejected because it makes the operator's tier choice a stateful setting to remember rather than a one-shot decision made at send time, and because a persistent "currently on Bedrock" toggle risks accidental expensive sends. Both actions share the exact same enabled condition (`draftText.trim().length > 0 && !sending`) so they always disable/enable together. `ChatTurnRow` now shows "ORACLE (Bedrock)" instead of plain "ORACLE" whenever the turn's `tier === 'bedrock'` — an honesty label, not decoration, so a reviewer scanning a thread can tell which replies came from the more expensive tier without opening dev tools. **Live verification**, all 5 of this task's checks, all directly observed against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock, TOTP `000000`): (1) "Pensar mejor ($3.42 este mes)" rendered below "Enviar" on `/oracle-chat`, both dimmed while the composer was empty. (2) Sending via "Pensar mejor" produced a row labelled "ORACLE (Bedrock)"; the actual request body was confirmed server-side (a temporary `console.log` of `tier` in the mock gateway's `oracleChat.js` route, reverted before commit — `git diff` on that file empty afterward) to carry `tier=bedrock`. (3) Sending via "Enviar" in the same thread produced a plain "ORACLE" row and logged `tier=local`. (4) Both actions were confirmed dimmed/disabled together both on an empty composer and mid-send (spinner on "Enviar", "Pensar mejor" text dimmed) — never independently enabled. (5) A Bedrock send was forced to fail via a second temporary mock-gateway change (`tier === 'bedrock'` short-circuited to a 500, reverted before commit) — the row showed the failed state with "Reintentar"; tapping it re-sent, and the gateway log showed **two** consecutive `tier=bedrock` entries (original + retry), confirming retry preserves the original tier rather than defaulting back to `'local'`. `npx tsc --noEmit` clean, `npm run lint` 0 errors / 57 warnings (pre-existing `import/no-duplicates` resolver baseline, unchanged), `npm run format:check` clean. | ✅ |
| OC-44 | Untrusted-content affordances | ✅ Done 2026-08-15, per `.superpowers/sdd/2026-08-15-oracle-untrusted-content-implementation/` plan. Two tasks. **Scope discipline:** read-only visibility only — no prompt-injection detection, scoring, or sanitization; no operator control over which chat gets quoted; `GET /api/v1/chat` and `ChatScreen.tsx` (the real player-chat screen) are untouched. **Task 1** (`a67a318`): the mock gateway's `chatMessages` fixture array itself is unchanged and still lacks `ts` — it still doesn't satisfy `ChatMessageSchema` on its own; `ts` is instead synthesized fresh at emission time inside the new `nextContextSnippets()` (`snippets.map((snippet) => ({ ...snippet, ts: new Date().toISOString() }))`), and both snippets in a pair share that one emit-time timestamp rather than a real per-message time. That same `nextContextSnippets()` round-robins two entries at a time through that 4-entry pool (`contextIndex` advancing by 2, wrapping mod length — deterministic, no randomness, so a live pass can assert exact pairs). `POST /api/v1/oracle/chat` now emits one `context` SSE event (`nextContextSnippets()`'s return value) *before* the token stream starts, on every send. **Reused, not reinvented:** the payload is typed as `ChatMessage[]` — the exact same schema `GET /api/v1/chat` (the real player-chat screen) already uses — rather than a new OC-44-specific shape, since these literally are player-chat messages, just a different two of them being read back to the operator. `streamOracleChat.ts` gained a `{ type: 'context'; snippets: ChatMessage[] }` stream-event variant (`ChatMessageSchema.array().safeParse`, malformed JSON skipped like every other event type here); `ChatTurn` gained `contextSnippets: ChatMessage[] | null` (`null` for operator turns — only an assistant turn can have cited context); `useOracleChatThreads.ts` assigns `event.snippets` straight into the turn on receipt and **resets `contextSnippets` to `null` at the start of every `retryTurn`**, so a retry can never display the failed attempt's stale pair — the field only ever holds what the *current* in-flight (or completed) attempt actually received, matching the same never-show-stale-content discipline `ChatTurnRow`'s failed-turn text already followed. **Task 2** (this row's own commit): `ChatTurnRow.tsx` renders a bordered block — "Contexto citado (chat de jugadores, no confiable)" — directly between the role label and the reply text, whenever `turn.contextSnippets` is non-empty; each snippet renders as `"{author}: {message}"`. **Always visible, never collapsed, no confidence score or highlighting** — the brief's entire intent is that the operator sees exactly what untrusted text the model read, with the "no confiable" label doing 100% of the trust-signaling work; adding a collapse toggle or a risk score would imply a judgment this feature deliberately doesn't make. **Live verification**, all 5 of this task's checks, all directly observed against `npm run mock-gateway` + `npx expo start --web` (the already-running mock-gateway process predated commit `a67a318` — started before Task 1's `context` event existed — so it was killed and restarted before any check; logged in `matias`/mock, TOTP `000000`): (1) sending a first message in a fresh thread rendered the context block above the reply text with exactly `Kaelith: alguien vio el faro nuevo?` / `Voss: si, queda al norte del puerto`; a separate page-context `fetch()` probe of the same endpoint (run afterward so it wouldn't disturb the round-robin count used by checks 2–3, and so it landed on a later pair — `Ember`/`Doran`, not this same `Kaelith`/`Voss` one) confirmed the raw wire shape: `event: context\ndata: [{"author":"Ember","message":"gracias!","ts":"2026-08-15T09:35:15.784Z"},{"author":"Doran","message":"cuidado con los lobos cerca del bosque","ts":"2026-08-15T09:35:15.784Z"}]` — full `author`/`message`/`ts` objects on every pair, not pre-flattened strings, confirming the wire format the rendered text was itself parsed from. (2) A second message in the same thread advanced the round-robin to `Ember: gracias!` / `Doran: cuidado con los lobos cerca del bosque`. (3) A third message wrapped back to `Kaelith`/`Voss`, confirming the 4-entry cycle. (4) Across all three turns, the operator's own message never rendered a context block — confirmed visually each time, only assistant turns had one. (5) A send was forced to fail via a temporary same-origin Service Worker (a throwaway file under a newly-created `public/`, deleted and unregistered immediately after use, `git status` clean afterward — the same technique OC-41's own live pass used, needed because `streamOracleChat.ts` consumes `expo/fetch`'s module-captured `fetch` reference, which reassigning `window.fetch` from the page console can't reach) that rejected only `POST …/api/v1/oracle/chat`; the blocked turn showed no context block and "No se pudo conectar con el gateway" + Reintentar. Un-registering the blocking worker and tapping Reintentar completed the turn showing a **freshly-fetched** `Kaelith`/`Voss` pair (the round-robin cursor the failed attempt — never having reached the server — had left untouched), not a stale or empty block frozen from the failed attempt. Zero console errors observed. `npx tsc --noEmit` clean, `npm run lint` 0 errors / 57 warnings (pre-existing `import/no-duplicates` resolver baseline, unchanged), `npm run format:check` clean. **This closes out OC-44 and the untrusted-content-affordances plan.** | ✅ |

---

## Phase 6 — Later / opportunistic

| ID | Item | Notes | Status |
|---|---|---|---|
| OC-45 | Push notifications ("server is down") | ✅ Done 2026-08-15, per `.superpowers/sdd/2026-08-15-push-notifications-client-implementation/` plan, three tasks. **Architecture: Expo-relay (Option A)** — neither this app nor the gateway ever talks to APNs/FCM directly. The client gets an Expo push token and hands it to the gateway; the gateway (companion ticket `ZG-44` in `xindeler-zuul`, PR already opened tonight) relays to Expo's own push service (`https://exp.host/--/api/v2/push/send`) using platform credentials (APNs key, FCM service account) configured at the EAS-project level, never held by the gateway itself. This closes the open question `gateway-api-contract.md` §8 used to carry about whether push was gateway-initiated or app-polled — it's gateway-initiated, relayed through Expo. **Task 1** (`376c61a`): `POST /api/v1/push/register` (`{ expo_push_token, platform }` → 204 No Content on the real gateway, `{ ok: true }` on the mock) and `POST /api/v1/push/unregister` (`{ expo_push_token }` → same real-vs-mock split) added to `writeApi.ts`, both CSRF-protected (via the OC-53 plumbing) with **no step-up** — registering a device isn't destructive, nothing fires or is delivered by this action alone. Final review (2026-08-15) caught that `gateway-api-contract.md` §7 and this row originally claimed `{ ok: true }` as the real contract — confirmed against `xindeler-zuul`'s merged `server/src/push.rs` that the real gateway returns bare `204`; `writeApi.ts`'s `registerPushToken`/`unregisterPushToken` no longer pass `OkResponseSchema` and are typed `Promise<void>`. Mock-gateway support (`tools/mock-gateway/src/routes/push.js`, an in-memory `state.pushTokens` array) added purely to make this testable locally — the real behavior (actually relaying to Expo) only exists in `xindeler-zuul`. **Task 2** (`ae37b16`): platform split — `PushTokenServiceImpl.native.ts` (real `expo-notifications` implementation: requests OS permission, gets the Expo push token via `getExpoPushTokenAsync`, stores it in `SecureStore`, sets up the Android notification channel and a foreground-notification handler) and `.web.ts` (an honest `{ state: 'unsupported' }` stub — `expo-notifications` does not support web at all, this was never going to be one implementation) behind a `pushTokenService.ts` platform-resolved re-export, consumed by `usePushRegistration()` (`{ status, loading, error, enable, disable }`). **Explicit opt-in, never auto-requested at launch**: matches this app's own consequential-action-is-explicit culture (dry-run before fire, typed-verb confirms on lifecycle actions) and Apple/Google's own platform guidance discouraging a permission prompt before the user has done anything that motivates it — the operator taps "Activar" on `/more` first. **Task 3** (this row's commit): `PushNotificationsSettings.tsx` — a card on the Más screen with four states (`unsupported` → "No disponible en la versión web.", `not_requested` → "Activar" button, `denied` → "Abrir configuración" deep link via `Linking.openSettings()`, `registered` → "Desactivar" link), wired into `app/(tabs)/more.tsx` between "Auditoría" and the environment switcher. New `gateway-api-contract.md` §7 documents the two endpoints; the old §7 (open items) renumbered to §8. **Live verification, honest about what's actually reachable tonight** — no development build, no physical device, no real APNs/FCM credentials uploaded to EAS exist in this environment, so none of a real OS permission dialog, a real Expo push token, or real delivery was observed or is claimed. What *was* directly observed against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock, TOTP `000000`; the mock-gateway process running at the start of this task predated the push routes' files and was restarted mid-task, same staleness this backlog's OC-44/OC-53 rows already hit): (1) `/more` rendered the "Notificaciones push" card with "No disponible en la versión web." — no "Activar" button, no crash, zero console messages of any kind (not just zero errors) after navigating there fresh; (2) a **fabricated** test token (`ExponentPushToken[fake-test-token]`, never claimed as real) sent via `api.write.registerPushToken(...)` called directly from the browser console — `api` temporarily exposed as `(globalThis as any).api` inside `ApiProvider` for this check only, reverted immediately after, confirmed via `git diff`/`git status` showing no trace — returned `{ ok: true }` with a page-side `fetch` monkeypatch confirming a non-empty `x-csrf-token` header was sent, proving the write goes through the real `httpClient.ts`/CSRF chain rather than a bypass; (3) `api.write.unregisterPushToken(...)` with the same fabricated token also returned `{ ok: true }` with the same CSRF header present; (4) `npx tsc --noEmit` typechecked cleanly, including `PushTokenServiceImpl.native.ts` and `.web.ts`, even though this web-only pass never executes the native file's code path. `npm run lint` 0 errors / 59 warnings (same pre-existing `import/no-duplicates` resolver baseline as OC-43/44/53, +2 for the two new files). `npm run format:check` clean. **Explicitly deferred, not done tonight**: real device permission prompts, real Expo push token generation, and real end-to-end notification delivery all require EAS push credentials (APNs key, FCM service account) that do not exist yet — that testing is Matías's own to run once those credentials are uploaded to EAS, not something this session could reach or should claim. | ✅ |
| OC-46 | Biometric unlock | ✅ Done 2026-08-15, per `.superpowers/sdd/2026-08-15-biometric-unlock-implementation/` plan, one task plus two fix rounds. **What ships**: a genuinely new app-lock layer — nothing like it existed before. A new `AppLockGate` component wraps `RootNavigator` (inside `AuthProvider`, outside `RootNavigator` so it can render on top of whatever route is current) and holds its own `locked: boolean`, independent of `AuthContext`'s existing `status` (which stays untouched — folding "locked" into a fourth `AuthStatus` would force every `Stack.Protected`/`handleAuthError`/`useAuth()` call site to reason about a UI-only concept with zero bearing on request validity). Policy, confirmed with Matías: **immediate lock on any backgrounding** (app switch, Control Center, screen lock — no grace period, no idle timer) plus a lock on cold boot with an already-valid persisted session. Entirely local — `expo-local-authentication`'s `authenticateAsync()`, zero gateway/`xindeler-auth` calls; confirmed explicitly with Matías that no server-side change was needed anywhere. Web untouched (the library has no web implementation; `biometricsAvailable` is seeded `false` there, making the overlay provably unreachable, not just untriggered). New `AppLockScreen` (modeled on `StepUpPrompt.tsx`'s `Modal` conventions, but non-dismissable — no tap-outside, `onRequestClose={() => {}}` neuters the Android back button) auto-triggers the OS prompt, offers "Reintentar"/"Cerrar sesión" on failure. `disableDeviceFallback: false` deliberately allows the OS's own device-passcode fallback (not a new app-specific PIN — explicitly out of scope). **Task 1** (`00e6e2c`): implemented from the plan's literal code with one deliberate, task-reviewer-verified deviation — two `useEffect`s that called `setState` synchronously in the effect body tripped this repo's real, currently-enforced `react-hooks/set-state-in-effect` rule; rewritten using the "adjust state during render" idiom already established twice in this codebase (`StreamContext.tsx`, `OracleComposerScreen.tsx`), independently confirmed necessary and behavior-preserving by the task reviewer. **Final whole-branch review** (most capable model, elevated scrutiny — this feature is the only barrier between an unlocked phone and a live game-server console) found 2 Critical + 3 Important issues neither the plan nor the task review could see in isolation: (1) the biometric-availability probe had no `.catch()` — any rejection left `biometricsAvailable` `null` forever, silently failing the whole lock open for the session; (2) `attemptUnlock` had no `try`/`catch` — a rejecting `authenticateAsync` left the operator permanently trapped behind the non-dismissable overlay with a dead button; (3) the auto-trigger fired on the `AppState` transition AWAY from `'active'` (i.e. while backgrounding, not on return to foreground) — feeding directly into finding 2 by requesting the OS prompt at the moment it's least likely to be honored; (4) a real, narrow cold-boot race window where `(tabs)` could render before the biometric probe resolved; (5) the "Cerrar sesión" escape hatch discarded its promise with no error handling, so a failing `logout()` trapped the operator silently. **Fix round** (`f3bca6c`, `68b1dd0`): both async boundaries now `try`/`catch` explicitly, resolving to a safe default (`biometricsAvailable = false`, a shown failure message) rather than hanging; the auto-trigger is unified into one `tryTrigger` function invoked only from genuinely deferred contexts (the `AppState` listener callback, and `setTimeout(tryTrigger, 0)` for the already-foregrounded case) so it never fires mid-backgrounding; `AppLockGate` now withholds `children` entirely while `biometricsAvailable` is still resolving on an authenticated+locked session, closing the cold-boot window; `AppLockScreen` now awaits `onLogout()` itself with its own pending/failure state. **A real bug surfaced mid-fix-round and was caught, not shipped**: the first fix attempt's own literal code for the corrected auto-trigger called `attemptUnlock()` directly inside the effect body for the already-foregrounded case — tripping the same `react-hooks/set-state-in-effect` rule the rest of the file deliberately works around. The implementer correctly refused to improvise a silent workaround and reported the exact problem instead of shipping something broken; corrected to route that case through `setTimeout` instead of a direct call, then re-verified clean. The scoped re-review of the fix round caught one further residual (a real but non-blocking `react-hooks/exhaustive-deps` warning introduced by the correction dropping a suppression comment the original code had for the same reason) and one inaccurate claim in the implementer's own report about it — both fixed in a final one-line commit. **Live verification, honest about what's actually reachable in this environment**: `npx tsc --noEmit` clean throughout every round; `npm run lint` 0 errors (63 pre-existing unrelated `import/no-duplicates` warnings, no others); `npm run format:check` clean. `npx expo start --web` + Chrome confirmed live, repeatedly, that no lock overlay ever appears on web. `npx expo run:ios` built and launched successfully on every round, including after the final fix — confirmed via `Info.plist` inspection that the `NSFaceIDUsageDescription` string landed correctly, and via `simctl` that the app installs, launches, and stays alive. **Explicitly not reachable in this environment, on any pass**: interactive iOS Simulator UI control (the Features → Face ID menu for enrollment/matching, the app-backgrounding gesture, tapping inside the lock overlay) — confirmed unavailable by checking for a `simctl` biometric subcommand (none exists) and attempting AppleScript/System Events automation (blocked, no Accessibility permission granted to the process). This means the actual end-to-end behavior — the lock overlay appearing on backgrounding, a real Face ID success/failure cycle, "Cerrar sesión" actually working, and the no-biometrics-enrolled fallback — was never observed running, only reasoned through via static code trace (independently, twice, by the task reviewer and the final reviewer) and confirmed to build/launch/not-crash. **Recommend a manual pass on a real device or an interactive Simulator before merging**, specifically the 5 checks in `docs/specs/2026-08-15-biometric-unlock-design.md`'s "Testing" section. **Explicitly out of scope, all named in the design doc**: web (WebAuthn/passkeys, separate future ticket), an app-specific PIN fallback, configurable lock timing, re-checking biometric enrollment mid-session, and the iOS app-switcher snapshot (the overlay likely covers it given how early it mounts, but this isn't a hard guarantee and wasn't device-verified). | ✅ |
| OC-47 | Widget / Live Activity (iOS lock-screen server status) | ✅ Scaffoldable portion done 2026-08-20, per `.superpowers/sdd/2026-08-20-lock-screen-widget-scaffold-implementation/` plan, two tasks plus a final-review fix wave. **Scope, per Matías's own direction**: only what a coding agent can build and verify without his manual Apple Developer Portal / EAS credential / physical-device steps — a working Live Activity that starts/updates/ends **locally** from the running app, no push. **Library research before designing**: two candidates found — `expo-live-activity` (Software Mansion) turned out **dead** (deprecated on npm, GitHub repo archived 2026-06-01, its own README redirects to the alternative below); chose **`expo-widgets`** instead — Expo's own first-party package, alpha in SDK 55, **stable as of SDK 56**, versioned lockstep with this app's SDK 57. Live Activities are defined as React components over `@expo/ui`'s SwiftUI-mapped primitives — no hand-written Swift anywhere in this ticket. Content (Matías's choice, chat): lifecycle state + players online + a live drain countdown, sourced from the same `useStatusQuery`/`useLifecycleState` every other screen already uses, no new gateway call. Manual toggle (not auto-start) on `StatusScreen.tsx` — a Live Activity is event-scoped by Apple's own design intent, not an always-on background presence. **Task 1**: installed `expo-widgets`, confirmed its REAL API by reading the actual installed `node_modules` type declarations rather than trusting the plan's own pre-installation research (independently re-verified by the task reviewer against the live npm registry — an unusually strong corroboration pass), built a minimal smoke-test Live Activity, verified via a genuine `expo prebuild` + `npx expo run:ios` + `xcrun simctl` screenshot showing real Dynamic Island content — this was a deliberate early risk-reduction step, since the library had two "blank widget" bugs on record from its SDK-55 alpha period (likely fixed by the SDK 56 stable promotion, not assumed from docs alone). Flagged three findings for Task 2: `LiveActivityEnvironment`'s fields (e.g. `colorScheme`) are unreliable due to a real bug in `expo-widgets`' own native code (`@Environment` misused outside a SwiftUI `View`); the library auto-adds `aps-environment: development` to the app's entitlements, meaning a real-device/EAS build will need Push Notifications capability enabled on the Apple Developer Portal App ID; a misleading library JSDoc about Live-Activity naming that doesn't actually apply. **Task 2**: real status/players/countdown content using `@expo/ui/swift-ui`'s native `Text(timerInterval:countsDown:)` for the live countdown (not a hand-rolled JS-ticking string — more correct, avoids a whole class of background-suspend timer bugs), a new `useServerStatusLiveActivity()` hook (start/update/end lifecycle reactively wired to `useStatusQuery()`, with a redundant-`.update()` guard comparing derived state), the real toggle wired into `StatusScreen.tsx`, Task 1's throwaway `app/_layout.tsx` trigger removed. Correctly avoided everything Task 1 flagged as unreliable — confirmed zero references to `LiveActivityEnvironment`/`colorScheme` in the final content. Live-verified via another real native build: Dynamic Island showed real content ("Activo"/"5 jugadores", then "Drenando" with a live native countdown during a mock-triggered drain), `.update()` confirmed via unified log firing once per second matching the mock's tick, `.end()` confirmed cleanly transitioning to `state: ended`. **Final whole-branch review** found one real Important gap neither task-scoped pass caught: the toggle's `active` state never reconciled against `expo-widgets`' own `getInstances()` API on mount — if the app were killed abruptly (not a clean logout/toggle-off) while the activity was running, the OS-managed Live Activity would keep running independent of the app process, but the toggle would show "off" on relaunch; worse, re-toggling would `.start()` a SECOND, orphaned instance rather than adopting the still-running one, left alive until iOS's own ~8-12h system cap. Fixed with a mount-time reconciliation effect adopting any existing instance via `getInstances()` — genuinely live-verified with a real abrupt-kill-and-relaunch cycle on Simulator (twice, for determinism), unified log confirming the same activity UUID was adopted with no duplicate `.start()` call. Re-review confirmed the fix correct, only noting two non-blocking Minors (a slight over-claim in the fix's own code comment about matching an existing effect's exact structure, and a latent-but-currently-unreachable edge case in the update-dedup priming that would only matter if query-cache persistence is ever added to this app — traced and confirmed inert today). `npx tsc --noEmit` clean throughout; `npm run lint` 0 errors (71 pre-existing unrelated warnings); `npm run format:check` clean. **Out of scope, explicitly, per Matías's own scoping decision**: push-driven remote Live Activity updates (no ActivityKit-scoped APNs credentials exist in EAS yet — same category of gap `OC-45`'s push notifications already left open); enabling Push Notifications capability on the Apple Developer Portal App ID (needed for any real-device/EAS build, purely a manual portal step); physical-device end-to-end validation, including the Lock Screen banner surface specifically, which `xcrun simctl` can't cleanly simulate (a locked-device gesture) — only the Dynamic Island was ever visually confirmed; Android (Live Activities are iOS-only, this ticket never touched Android). ⚠️ **Correction (post-merge fix, 2026-08-21):** this ticket's own live verification was iOS-Simulator-only and never caught a real regression it introduced on the other two platforms — `ServerStatusActivity.tsx` did a module-scope `import { Text, VStack } from '@expo/ui/swift-ui'`, and that package's `Text` calls `requireNativeView(...)` at import time, unconditionally throwing outside iOS (no `Platform.OS` check can help, since the crash happens before any guard in the importing code runs). Since `(tabs)/index.tsx` → `StatusScreen.tsx` → `useServerStatusLiveActivity.ts` → `ServerStatusActivity.tsx` is the default route, this took down the **entire Web app** the instant it loaded, and — per the same import-time crash logic — almost certainly Android too (not independently confirmed live before the fix, since Android was never part of this ticket's own scope either). Found 2026-08-21 while doing manual verification for an unrelated ticket (OC-35's keyboard-shortcuts sub-part). Fixed on its own branch, `docs/specs/2026-08-21-server-status-activity-platform-split-design.md` / `docs/superpowers/plans/2026-08-21-server-status-activity-platform-split-implementation.md`: split by Metro's standard platform-extension convention into `ServerStatusActivity.ios.tsx` (real implementation, byte-identical to the original file) and a generic `ServerStatusActivity.tsx` (Android/Web stub — no `@expo/ui/swift-ui` import, a trivial all-`null` `layout` that never actually runs off-iOS since `expo-widgets`' own `createLiveActivity` already has its own correct `ExpoWidgets.ios.js`/generic-fallback split), plus a new shared `ServerStatusActivityState.ts` so the type isn't duplicated between them. `useServerStatusLiveActivity.ts` needed zero changes. **The mandated real-Simulator verification step caught a second, genuinely non-obvious bug the plan itself got wrong**: naming the stub `ServerStatusActivity.ts` (as the plan's own literal brief specified) causes a real crash on iOS too — this project's Metro `sourceExts` order (`["ts","tsx",...]`) means Metro resolves one extension at a time across all platform suffixes before moving to the next extension, so a bare `.ts` file shadows `.ios.tsx` before Metro's "tsx" pass is ever reached, silently routing iOS to the wrong (stub) implementation. Fixed by renaming the stub to `ServerStatusActivity.tsx` (matching `.ios.tsx`'s extension so the platform-specific file correctly wins within the same resolution pass) — confirmed via a real, reproducible `ArgumentCastException` crash on Simulator before the fix, and confirmed clean after, including a full `npx expo start --clear` cache wipe to rule out a stale-cache artifact. **Live verification**: Web confirmed twice in a real browser (before/after the `.tsx` rename) rendering the Status tab with zero console errors; iOS confirmed via a full `expo prebuild -p ios --clean` + `expo run:ios` cycle on Simulator, toggling the Live Activity on (Dynamic Island showed real content, "Activo"/"5 jugadores", matching the mock gateway) and off (ended cleanly, no leftover activity) — toggle itself driven by a temporary, fully-reverted test-only effect in `StatusScreen.tsx` since no scriptable Simulator tap primitive exists without granting Accessibility permissions outside this session's authority (same known gap already documented in this ticket's own design doc). Android not independently re-verified (best-effort, skipped in favor of the deeper iOS investigation above; the fix's Android code path is identical in kind to the already-verified Web path, since both now resolve to the same generic stub file). Final whole-branch review found one Minor (a stale in-file comment referencing the stub's old, wrong `.ts` name — ironic given the whole point of that comment is warning against exactly that mistake), fixed and re-reviewed clean. `npx tsc --noEmit`, `npm run lint` (0 errors, same pre-existing repo-wide warnings), `npm run format:check` all clean throughout. | ✅ |
| OC-48 | Add a second operator | ✅ Closed 2026-08-16 — **resolved operationally, no code needed anywhere.** Investigated `xindeler-zuul`'s real source before assuming any client work was required (same discipline as every cross-repo mismatch found this week): the operator allowlist (`XINDELER_ZUUL_OPERATORS`, a `HashSet<Uuid>`, `config.rs`) was never a single hardcoded operator, and per-operator TOTP enrollment (`enroll-operator <uuid> <account-name>`, `cli.rs`, ZG-38) was already built multi-operator-ready from day one. **Adding a second operator today is purely an ops action Matías can already do**: add the uuid to `XINDELER_ZUUL_OPERATORS` in `/etc/xindeler-zuul/zuul.env`, restart `xindeler-zuul.service`, then SSH in and run `enroll-operator <uuid> <name>` to hand them their own TOTP QR. Zero code change in this app or in `xindeler-zuul` was needed to make that work — it already works. <br><br>Two real gaps turned up during the investigation, both pre-existing and unrelated to "can a second operator log in" specifically: the real gateway's `LoginResponse` carries no operator identity at all (so this app can't display who's logged in, and audit rows only ever show a raw uuid), and there's no role/permission concept distinguishing one operator from another (so nothing today lets exactly one operator, Matías's own `ΑΩ` account, administer the allowlist from a UI — that's an entirely new capability, not a gap in the existing one). Rather than invent scope to fill this ticket, both were written up as detailed, separate, cross-repo tickets instead — matching Matías's own explicit instruction to document thoroughly and file real backlog tasks for whoever picks each one up next, in the right repo, rather than build ad hoc: `xindeler-zuul`'s `ZG-47` (operator identity → login response + audit log), `ZG-48` (superuser role, designating `ΑΩ`), `ZG-49` (admin API for allowlist management, replacing the SSH flow) — PR: https://github.com/Matute289/xindeler-zuul/pull/53; this app's own `OC-55` (fix the login flow's request/response shape to match the real gateway's one-shot contract — a pre-existing, separate mismatch surfaced by this same investigation), `OC-56` (display operator identity, blocked on `ZG-47`), `OC-57` (the operator-admin screen itself, blocked on `ZG-48`+`ZG-49`). | ✅ |
| OC-49 | World Director approval queue | Reuse the OC-33 card as the `PROPOSED → VALIDATED` human step for the autonomous director (NH-75 §5.1). | 🅿️ |
| OC-50 | AURORA tab + ON/OFF placeholder | ✅ Done 2026-08-15, per `.superpowers/sdd/2026-08-15-ai-system-aurora-placeholder-implementation/` plan, one task. **Re-scoped from the original framing, per Matías's own explicit direction in chat**: rather than a standalone AURORA tab or a row tucked inside ORACLE's existing screen, the bottom tab formerly labeled `ORACLE` is renamed `Sistema IA` ("AI System") and gains a two-chip segmented control (`ORACLE` / `AURORA`) at the top of the screen — a phone-appropriate alternative to a persistent side rail (this app already has a separate, unrelated wide/tablet sidebar for its 6 main tabs; this is a second, narrower selector nested one level inside a single tab). `ORACLE` stays the default section and is `OracleEventsScreen`, byte-for-byte unmodified — confirmed by the final reviewer directly, not just claimed. `AURORA` is the new, genuinely inert placeholder: a bordered card visually mirroring ORACLE's own kill-switch (status label + button), matching the backlog's original ask, but with a `Button`'s `disabled` prop **hardcoded to the literal `true`** — never derived from state or a query — so `Pressable`'s own native disabled handling blocks `onPress` from ever reaching the network, not just styled to look inactive. Copy states plainly what AURORA is (companion system to ORACLE, per-NPC intelligence / social-simulation) and that it has zero engine implementation today, without inventing capability details beyond what's actually documented anywhere in this repo. New `src/features/aiSystem/AiSystemScreen.tsx` owns only the section-selection state (`useState<'oracle'\|'aurora'>('oracle')`) and the segmented control; new `src/features/aurora/AuroraPlaceholderScreen.tsx` holds the placeholder content in its own top-level folder (parallel to `src/features/oracle/`, anticipating AURORA growing into a real multi-screen feature the same way ORACLE already has). `app/(tabs)/oracle.tsx` now renders `AiSystemScreen` instead of `OracleEventsScreen` directly — the route path/URL stays `/oracle`, only what it renders changes. `app/(tabs)/_layout.tsx`'s `DESTINATIONS` array gets one entry's `label`/`icon` changed (`'ORACLE'`/`sparkles-outline` → `'Sistema IA'`/`hardware-chip-outline`), which updates both the phone tab bar and the wide/tablet sidebar automatically since `SidebarLayout` reads the same array. **Live verification**, both by the implementer and independently re-checked by the task reviewer and the final reviewer (not just trusted): the tab bar and sidebar both show the new label/icon; opening the tab defaults to ORACLE rendering identically to before this ticket; the AURORA button is visibly greyed out and confirmed to fire literally zero network requests when tapped; switching between sections works both directions; no console errors; grepped the whole `src`/`app` tree for the old `'ORACLE'` tab label and confirmed every remaining hit correctly refers to the ORACLE *system* (which keeps its own name), not the old tab, so nothing else in the app is left pointing at a renamed tab by its old label. `npx tsc --noEmit` clean, `npm run lint` 0 errors (same pre-existing warning baseline), `npm run format:check` clean. **One residual, verified honestly rather than assumed**: the design's claim that the selected section always resets to ORACLE on navigating away and back was live-checked at the wide/tablet (`SidebarLayout`/`Slot`-based) breakpoint and confirmed true; at phone width, `expo-router`'s `<Tabs>` (React Navigation bottom tabs) keeps sibling tab screens mounted by default (no `unmountOnBlur` anywhere in this app), so the selected section may persist there instead of resetting — not independently re-verified on phone width due to browser-automation tooling limits in this session. Not fixed: harmless regardless, since AURORA is fully inert either way and a persisted UI selection can't misrepresent any capability or cause any side effect; a real fix would mean either accepting the phone-width persistence as reasonable UX or adding `unmountOnBlur`-style behavior that would also affect ORACLE's own scroll/query state across every other main-tab switch — a larger, unrelated behavior change not worth making for this. **Explicitly out of scope, unchanged from the design**: any real AURORA functionality (no engine support, no gateway route exists), a generic reusable `SegmentedControl` UI primitive (first use site), persisting section selection across app restarts. | ✅ |
| OC-51 | "Chat con ORACLE" mock-only notice | ✅ Done 2026-08-20, per `.superpowers/sdd/2026-08-20-oracle-chat-mock-only-notice-implementation/` plan, one task, clean on first review. **Premise corrected before implementing**: this row was written before OC-41/43/44 shipped (2026-08-15) — by the time this was picked up, "Chat con ORACLE" was already a fully built, fully working entry point and screen, not a stub. Re-scoped in chat with Matías: the real gap is that `xindeler-zuul` has no Bedrock implementation yet (`ZG-29`–`ZG-32`, confirmed all still `⬜` against the real repo before writing the design), so chat only ever responds against `tools/mock-gateway` — an operator on `WireGuard` would hit a real request failure with no advance warning. **Design deliberately diverges from the `ZG-42` web-dashboard precedent** (a single, permanently-shown static paragraph, since that dashboard has no environment switcher): since this app already has `useEnvironment()` everywhere, the notice is environment-aware instead — `environment.id !== 'mock'` — so it says nothing when the operator is genuinely testing against Mock (where chat works) and warns only when they're on `WireGuard`. Added identically to two screens: `OracleEventsScreen.tsx` (below the "Chat con ORACLE" entry-point link) and `OracleChatScreen.tsx` itself (below its header), since an operator could switch environments mid-session via the always-visible `EnvironmentBadge` while already on the chat screen. Copy: "El chat todavía no tiene implementación en el gateway real — solo responde contra el entorno Mock (falta Bedrock del lado del gateway)." No controls disabled anywhere — explicitly the opposite treatment from `OC-50`'s AURORA placeholder (which hardcodes a disabled button), matching this row's own original instruction. Final whole-branch review: 0 findings, ready to merge as-is — confirmed the environment check is exhaustively correct against the current closed `EnvironmentId` union (`'mock' | 'wireguard'`), confirmed no `disabled` prop was added anywhere. One Minor noted, not fixed: the notice block is duplicated verbatim across the two files rather than extracted into a shared component — a reasonable YAGNI call for exactly two call sites, worth extracting if a third consumer ever appears. `npx tsc --noEmit` clean; `npm run lint` 0 errors (70 pre-existing unrelated warnings); `npm run format:check` clean. **Live-verified**: notice absent on `Mock`, appears on both screens after switching to `WireGuard` via `EnvironmentSwitcher`, disappears again switching back; "Enviar"/"Pensar mejor"/the entry-point link remain fully tappable in every environment. | ✅ |
| OC-52 | Unlock a player's 2FA-locked account (support screen) | ✅ Done 2026-08-15, per `.superpowers/sdd/2026-08-15-player-2fa-unlock-implementation` plan, two tasks. **Confirmed tonight that both real blockers actually cleared** — not just their backlog rows saying so: `xindeler-auth`'s Fase L (G-03) is live in production, and `xindeler-zuul`'s `ZG-45` (PR #46) merged, `POST /api/v1/players/2fa/unlock` now exists on the real gateway. Verified by reading the real merged `server/src/players.rs` directly (not a summary of it), documented in `docs/reference/gateway-api-contract.md` §9. **Corrected design — the biggest change from the original plan**: the originally-planned three-way error split ("username not found" vs. "account exists but isn't locked" vs. "auth service unreachable") does not exist server-side. `players.rs`'s `Unlock2faError::Failed` branch collapses all three into one generic `502` with a hardcoded message and no `code` field — the real gateway itself cannot tell these cases apart, so this app must not pretend to either. Both the screen and the mock's route mirror that exact collapse rather than inventing a distinction the backend can't make. **Task 1** (`5c4f762`, fixed by `36ffc89`): `api.write.unlockPlayer2fa(username, stepUpCode, idempotencyKey)` added to `writeApi.ts` (`POST /api/v1/players/2fa/unlock`, `{ username }`, `Promise<void>`, real gateway returns bare `204`); mock route `tools/mock-gateway/src/routes/playerUnlock.js` implements the same fixture-alias-based success/generic-502 split, CSRF+step-up protected. Fix round: the route originally recorded `outcome: 'success'`/`'failed'` in the audit log, which fails `AuditRowSchema`'s `z.enum(['ok', 'error'])` and would break the entire `/audit` screen the moment one such row existed — corrected to this repo's established `'ok'`/`'error'` convention. **Task 2** (this row's commit): `PlayerAccountsScreen.tsx` — one `TextField` (username) + one `Button` ("Desbloquear 2FA"), gated by the exact same `ConfirmByTypingSheet` (`word="UNLOCK"`) + `useDestructiveAction`/`useStepUpAuth` TOTP step-up chain `StatusScreen.tsx`'s restart/stop/start buttons already use — no new confirmation or step-up primitive built for this. Wired into `app/(tabs)/more.tsx` as a second row below "Auditoría" (route `/player-accounts`, `href: null` in `_layout.tsx`, same secondary-nav pattern OC-28 established). Success renders a self-clearing (~3s) "Listo — 2FA desbloqueado para `<alias>`." message and clears the field; failure renders the generic collapsed message via `ActionError`, leaving the field as typed so the operator can retry. **Live verification**, all 5 planned checks, against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock/`000000`), driven via `claude-in-chrome`: (1) `/more` → "Cuentas de jugador" rendered below "Auditoría" with identical styling, tap → `/player-accounts`; (2) "Desbloquear 2FA" confirmed disabled with an empty username field (tap is a no-op, no sheet opens) and enabled once text is typed; (3) **success path** (fixture alias `Kaelith`, then `Ember` for a header-instrumented rerun): `ConfirmByTypingSheet` kept `Confirmar` disabled at `UNLOC` and enabled it only at the exact `UNLOCK`; the TOTP sheet blocked until a code was entered, `000000` accepted — a `window.fetch` monkeypatch (`read_network_requests` misreported this call's status as `503`, contradicted by every other signal, so the real request/response was captured directly) confirmed `POST /api/v1/players/2fa/unlock` carrying a valid `x-csrf-token` and `X-Ops-Totp: 000000`, response `204` with a literal empty body; UI showed "Listo — 2FA desbloqueado para Ember.", self-cleared, username field cleared; (4) **generic-error path** (`nombre_inventado`, not a fixture alias): same confirm+step-up flow, same fetch instrumentation confirmed `502` with body `{"error":{"code":"gateway_error","message":"failed to reach the auth service"}}` — `ActionError` rendered that exact message, no crash, no false success, username field left as typed (not cleared, matching the failure branch); (5) `/audit` showed three `players.2fa_unlock` entries by the end of the session (`Kaelith`/`ok`, `Ember`/`ok`, `nombre_inventado`/`error`) — outcome values are `ok`/`error`, not `success`/`failed`, confirming Task 1's fix round holds up end-to-end and the Audit screen renders normally. **One incidental finding, fixed mid-task, not a code bug**: the `mock-gateway` process running at session start predated tonight's `server.js` (it started before Task 1's route-mount commit landed), so it 404'd `POST /api/v1/players/2fa/unlock` until restarted — same staleness class OC-45/OC-53's rows already flagged, not a recurrence worth its own ticket. `npx tsc --noEmit` clean, `npm run lint` 0 errors / 61 warnings (same pre-existing `import/no-duplicates` baseline), `npm run format:check` clean. | ✅ |
| OC-53 | CSRF token support | ✅ Done 2026-08-15, per `.superpowers/sdd/2026-08-15-csrf-token-implementation` plan, two tasks. **What was broken and how it was found:** cross-checking `xindeler-zuul`'s real source tonight for an unrelated reason (OC-45's push-notification design) turned up that the real gateway — deployed to production as `v1.0.0`, confirmed by Matías — has always required a `csrf_token` (returned in `POST /auth/totp`'s JSON body, not a cookie) echoed back as an `x-csrf-token` header on every mutating request, checked unconditionally regardless of bearer-vs-cookie auth (`login.rs:184-189`, `csrf_token_matches`). This app never sent that header: `gateway-api-contract.md` predates `xindeler-zuul` and never modeled CSRF, the mock's `POST /auth/totp` had no `csrf_token` field, and `TotpResponseSchema` had no field to carry one. Every write this app makes — lifecycle, broadcast, ORACLE stage/trigger/kill-switch — has silently worked against the mock and would have `403`'d against the real gateway the first time an environment profile pointed at it; true since Phase 2 shipped, only noticed tonight. **Task 1** (`2fffdde`, fixed by `9c6f004`): threaded a second header through the exact plumbing `Authorization` already uses — `getAuthHeader()`/`getCsrfHeader()` are now symmetric siblings on `SessionStorage` (`src/auth/types.ts`), both called once from `httpClient.ts` and merged into non-GET request headers, wired in `apiClient.ts` the same one-line way. **Why the web platform stores the CSRF token instead of discarding it, unlike the bearer token**: `SecureSessionStorage.web.ts` already discards the bearer token by design (the real credential is the `HttpOnly` cookie) but a CSRF token has to be readable by the page's own JS — that's the entire mechanism, proving the request came from a script that can read this origin's storage, which a cross-site attacker's forged request can't — so it joins the existing `localStorage` metadata blob instead, matching how `xindeler-zuul`'s own built-in web UI handles its analogous token (`server/src/web_ui.rs`, ZG-17: `sessionStorage`, never a cookie). `SecureSessionStorage.native.ts` joins it to the same single `SecureStore` JSON blob the bearer token already lives in — one more field, same atomic write, no new torn-write case. `AuthContext.tsx`'s `totp()` call reads `session.csrf_token` off the now-widened `TotpResponseSchema` and saves it alongside everything else at the only point a session is *created*. A reviewer fix round (`9c6f004`) closed a real bug before it shipped: both platforms' original `getCsrfHeader()` didn't guard the field itself, so a session persisted before this deploy (missing `csrfToken`) would return a truthy `{ 'x-csrf-token': undefined }` that `fetch()` coerces to the literal string `"undefined"` — changed to `stored?.csrfToken ? {...} : undefined`. **Task 2** (this row's mock-gateway half, `tools/mock-gateway/`): `auth.js`'s `issueSession()` now mints a `csrfToken` alongside `expiresAt`/`createdAt` on every login *and* every `/refresh` (a refreshed session correctly gets a fresh CSRF token too, same as a fresh login), returning it as `csrf_token` in the response body. New `src/middleware/csrf.js`'s `requireCsrf` mirrors `requireStepUp`'s exact shape, checked after `requireAuth` (it depends on `req.token`, which only `requireAuth` sets) — `403 invalid_csrf` on a missing or mismatched `x-csrf-token` header. Wired into `server.js` on **exactly the 5 routes this ticket confirmed against real `xindeler-zuul` source actually require it**: `/server` (lifecycle), `/broadcast`, `/oracle/stage`, `/oracle/trigger`, `/oracle/enabled` (the latter three keep their existing `requireStepUp` too, CSRF-then-step-up in that order). **`/oracle/chat` deliberately excluded**: it has no real `xindeler-zuul` counterpart yet at all — that repo's own Phase 5 (`ZG-29`..`ZG-32`) is entirely unimplemented, blocked on Matías's own AWS Bedrock account setup — and its client (`streamOracleChat.ts`) is a deliberately standalone fetch implementation that doesn't even go through `httpClient.ts`; inventing a CSRF requirement for an endpoint whose real shape doesn't exist yet would be exactly the kind of unratified guess `gateway-api-contract.md` already warns against. Every GET-only route (`status`, `players`, `logs`, `chat`, `chronicle`, `audit`, `stream`, `oracle/events`, `oracle/presets`, `oracle/budget`) is unchanged. **Separately flagged, not fixed here**: while reading `xindeler-zuul`'s real source, `oracle/stage`'s mock route was found to require step-up where the real gateway's `ZG-22` explicitly does not ("CSRF-only, no step-up: staging is inert until an operator explicitly fires it"). That's a second, independent contract mismatch, unrelated to CSRF — loosening a step-up requirement changes this app's actual security posture and deserves its own explicit decision, not a ride-along fix inside a CSRF-plumbing ticket. **Live verification**, all 6 planned checks, against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock/`000000`), driven via `claude-in-chrome` with a `window.fetch` monkeypatch (this app is SPA-routed, so the patch survives in-app navigation but resets on a hard URL navigation — reinstalled once, mid-sequence, for that reason) rather than the Service-Worker/`public/` technique, since fetch-patching reached the real outgoing request path directly and needed no throwaway files to revert: (1) raw `POST /auth/totp` response body — `{token, expires_at, operator, csrf_token: "c92456f9-…"}` — confirmed `csrf_token` present; (2) a real write (`/broadcast`) carried `x-csrf-token: c92456f9-…`, exactly the login's value, status `200`; (3) logged out and back in — the new session's `csrf_token` (`c92456f9-…`) differed from the prior session's write (`d18a38da-…`), proving a fresh token per login, not a reused one; (4) **the negative case**: patched `fetch` to strip `x-csrf-token` from the next write, sent it, and got back `403 {error:{code:"invalid_csrf", message:"Falta o es inválido el header X-Csrf-Token"}}` — the UI surfaced that exact message inline and the message never appeared in the chat log, proving `requireCsrf` actually rejects, not just that the happy path looks right; (5) `GET /players` (Jugadores screen) succeeded `200` with an empty header object — no `x-csrf-token` sent at all; (6) sent a message via `/oracle-chat` and got a normal ORACLE proposal back (`POST /oracle/chat` → `200`), confirming that route's deliberate exclusion didn't break it. | ✅ |
| OC-54 | Session-scoped step-up mechanism (replace `X-Ops-Totp` header) | ✅ Done 2026-08-15, per `.superpowers/sdd/2026-08-15-step-up-session-mechanism-implementation/` plan, two tasks. **What was broken and how it was found:** OC-52's own final review turned up that this app's step-up design — a per-request `X-Ops-Totp: <6 digits>` header sent alongside every destructive write — has never matched the real, already-deployed `xindeler-zuul` gateway, confirmed by reading `login.rs`/`session.rs`/`lifecycle.rs` directly: the real gateway is session-scoped, exposing a bare `POST /step-up` that verifies a TOTP code once and opens a 5-minute window on the *session*; it never reads a per-request TOTP header on any route, destructive or otherwise. Written up in `docs/specs/2026-08-15-step-up-session-mechanism-design.md`. Every destructive write this app has ever made would have been silently accepted by the mock (which trusted the header) and silently ignored — not rejected, just never checked — by the real gateway, a correctness gap of the same shape OC-53 found for CSRF. **Task 1** (`6470c5d`, client): `useDestructiveAction.run()` now calls the new `api.auth.stepUp(code)` (→ `POST /api/v1/step-up`, `{ totp_code }`) immediately before invoking the caller's write closure, on *every* `run()` — including when `requestStepUp()` serves a cached client-side code from `StepUpContext`'s 90s window — so the server-side 5-minute window is always freshly opened right before the write that depends on it; the write closure itself no longer receives or sends a code at all. `writeApi.ts`'s 10 step-up-gated methods dropped their `stepUpCode` parameter; `httpClient.ts` dropped the `X-Ops-Totp` header entirely. **Task 2** (this row's commit, `tools/mock-gateway/` + docs): mirrored the same session-scoped model instead of merely deleting the header check. `state.js`'s `sessions` comment now documents the new `steppedUpUntil` field (set dynamically, not at session creation). `middleware/stepUp.js` was rewritten in the exact shape OC-53 gave `requireCsrf`: `requireStepUp` now reads `state.sessions.get(req.token).steppedUpUntil` and fails closed (`403 step_up_required`) if absent or lapsed, no longer touching any header. New `routes/stepUp.js` implements the bare (not `/auth`-nested) `POST /api/v1/step-up`: wrong/missing `totp_code` → `401 invalid_totp` (401, not 403 — this route *grants* step-up, it isn't gated by it, matching the real gateway's `rejected()` status for a bad code here); a correct `000000` sets `session.steppedUpUntil = Date.now() + 5min` and returns bare `204`. `server.js` mounts it at `/api/v1/step-up` behind `requireAuth, requireCsrf`, next to the auth mount. `gateway-api-contract.md` §1's step-up bullet and a new §2.1 subsection now document the session-scoped contract, including a separately-flagged (not fixed here) note that the real gateway's error bodies are plain text, not this doc's own JSON envelope. `.claude/skills/ops-gateway-api/SKILL.md` rule 4 updated to match. **Live verification**, against `npm run mock-gateway` + `npx expo start --web` (logged in `matias`/mock/`000000`, driven via `claude-in-chrome` with a `window.fetch` monkeypatch reinstalled once after a hard reload, same SPA-navigation caveat OC-53's row already documents) — both dev processes were found already running from a prior session (mock-gateway 2h41m old, predating this task's `server.js` edit; `expo start --web` over a day old, predating even Task 1's `6470c5d`) and were killed and restarted fresh before testing, the same staleness class OC-45/OC-52/OC-53's rows already flagged: (1)-(3) ORACLE kill switch "Desactivar": TOTP prompt appeared exactly once, and the observed network sequence was exactly two requests in order — `POST /api/v1/step-up` `{"totp_code":"000000"}` → `204` empty body, then `POST /api/v1/oracle/enabled` `{"enabled":false}` → `200 {"enabled":false}` — neither request carried an `X-Ops-Totp` header (confirmed by scanning every captured request's header keys for the whole session, zero hits), and the screen updated to "ORACLE: Desactivado"; (4) a second, distinct destructive action from a different screen — `/player-accounts`' "Desbloquear 2FA" (`ConfirmByTypingSheet` word `UNLOCK`, fixture alias `Kaelith`) — confirmed the mechanism isn't special-cased to ORACLE: no visible TOTP prompt this time (the 90s client-side code cache from the prior action was still warm), but a **fresh** `POST /api/v1/step-up` `{"totp_code":"000000"}` → `204` still fired immediately before `POST /api/v1/players/2fa/unlock` `{"username":"Kaelith"}` → `204`, exactly matching Task 1's documented intent that the server-side window is re-established on every `run()` regardless of whether the client-side prompt itself was skipped; UI showed "Listo — 2FA desbloqueado para Kaelith."; (5) wrong TOTP code: after the client cache aged out (forced via a full page reload rather than waiting out the 90s window, which reset `StepUpContext`'s in-memory cache but preserved the cookie-backed session), ORACLE "Activar" with `123456` produced `POST /api/v1/step-up` `{"totp_code":"123456"}` → `401 {"error":{"code":"invalid_totp","message":"Código TOTP inválido"}}`; the UI surfaced that exact message inline, the `oracle/enabled` write was never sent, and the screen stayed "ORACLE: Desactivado" — no silent failure, no false success. **Incidental finding, fixed same-day (`5e3880d`):** because `useDestructiveAction`'s retry-on-403 branch only wrapped the write `call()`, not the `api.auth.stepUp()` call itself, and because a wrong code is a `401` rather than a `403`, a rejected code was *not* automatically retried — a second immediate tap silently reused the same now-known-wrong cached code from `StepUpContext` and failed identically, with no re-prompt, until the 90s cache aged out. Fixed by wrapping `api.auth.stepUp(code)` in its own single-retry catch (`401` → force a fresh `requestStepUp({ forceFresh: true })` prompt, retry `stepUp()` once) and correcting `StepUpContext.tsx`'s stale comment to describe both the `401` (bad code) and `403` (lapsed window) retry paths. Live re-verified end-to-end twice: wrong code → visible `401` → automatic fresh re-prompt (not a silent reuse) → correct code → action completes normally. Scoped re-review: approved, 0 findings. (6) **Not live-tested, and not practically reachable under this design**: the retry-on-403 branch that re-steps-up after the *destructive write itself* returns `403` (session step-up window expired server-side, or an unrelated CSRF 403) — since `api.auth.stepUp()` runs fresh immediately before every write, this path is only reachable by artificially shortening the mock's 5-minute `STEP_UP_TTL_MS`, which the brief explicitly said not to do mid-test; recorded here honestly rather than skipped or falsely claimed. `npx tsc --noEmit` clean (0 errors, unaffected — this task touches no `.ts` files). `npm run lint` 0 errors / 61 warnings (same pre-existing `import/no-duplicates` resolver baseline as OC-52/OC-53). `npm run format:check` clean (`.prettierignore` excludes `*.md`, so the two doc edits were never in scope for that check — expected, not a gap). **Explicitly out of scope**, all three already named in the design spec's "Out of scope" section and this plan's Global Constraints: *which* endpoints require step-up (`oracle/stage`'s already-flagged mismatch with the real gateway stays unaddressed); the real gateway's plain-text (non-JSON-envelope) error body shape, noted but not fixed in the new §2.1; and the audit-row shape/outcome-value mismatch OC-52 already flagged separately. **Final whole-branch review** (most capable model, elevated scrutiny given this ticket gates every destructive action) found two Important issues beyond what either task reviewer could see in isolation, both fixed in one consolidated round (this row's commit): (1) the write-call retry's blanket `err.status === 403` check also caught the mock's `oracle_disabled` 403 (ORACLE's kill switch, unrelated to step-up) — an operator tapping Fire/Stage while ORACLE is off would get a spurious forced TOTP re-prompt before an identical, identically-failing retry; narrowed to exclude `err.code === 'oracle_disabled'` specifically, preserving every other previously-accepted 403 retry case (including CSRF). (2) this ticket's own doc edits (`gateway-api-contract.md` §1, `ops-gateway-api/SKILL.md` rule 4) re-asserted "reads are session-only" and "broadcast is not step-up'd," both now confirmed FALSE against the real gateway: `GET /api/v1/audit` (aggregates every operator's destructive history) and `POST /api/v1/broadcast` are both real-gateway step-up-gated (`console.rs`, `lifecycle.rs`), and this app calls both without ever opening a window — `/audit` as a bare unguarded `GET`, `/broadcast` by calling `api.write.broadcastMessage()` directly from `BroadcastComposer.tsx`, bypassing `useDestructiveAction` entirely. Both will `403` against the real gateway. Corrected the doc claims so this branch doesn't ship a now-known-false invariant; the actual gating fix for `/audit` and `/broadcast` is a new, real, **not-yet-ticketed** gap — flagged here for Matías, tracked the same way the plain-text-error-body and audit-row-shape gaps already are. Also corrected three smaller doc inaccuracies caught in the same pass: the design spec's `/auth/step-up` typo (real route is bare `/step-up`), §2.1's "bearer or cookie" claim (real gateway's step-up route reads the session cookie only), and §2.1's wrong-code response claim (mock: JSON `invalid_totp`; real gateway: plain-text "invalid credentials", no parseable code at all). One remaining Minor, parked: a `401` from `/step-up` can mean either "wrong code" or "session already gone" (`login.rs`'s `mark_stepped_up` `Ok(false)` branch) — real zuul's plain-text body can't distinguish these, so an operator with an expired session sees a TOTP re-prompt instead of a session-expired message; not fixed, low-frequency edge case, real zuul's plain-text bodies make it unfixable client-side without the audit/broadcast follow-up ticket's gateway-side changes anyway. | ✅ |
| OC-55 | Fix the login flow to match the real, one-shot gateway contract | ✅ Done 2026-08-20, per `.superpowers/sdd/2026-08-20-one-shot-login-flow-implementation/` plan, one task plus a final-review fix round. **Design**: kept the existing two-screen UX (username/password, then a separate TOTP screen) — approved by Matías in chat — but restructured *when* the network call fires: the first screen (`app/(auth)/login.tsx`) no longer talks to the gateway at all, it just stashes the two values in a new in-memory-only `AuthContext.beginLogin()` (a `useRef`, never `sessionStorage`, never a route param — a password must never transit anything URL-shaped) and navigates straight to `/totp`. The second screen fires the one real request via a new `AuthContext.completeLogin(totpCode)` once the operator finishes their code — `POST /api/v1/login`, `{ username, password, totp_code }`, returning `{ csrf_token, operator_uuid, operator_username, is_superuser }` directly, no token/expiry in the body (the session lives entirely in the real gateway's `HttpOnly` cookie). On a wrong TOTP code, the pending credentials are deliberately NOT cleared, so the operator retries with just a new code, matching real 2FA UX. Two adjacent, same-file path bugs fixed in the same pass (both confirmed against the real route table, `web.rs`): `login`/`logout` move from the wrongly-assumed `/api/v1/auth/login`/`/api/v1/auth/logout` to the real bare `/api/v1/login`/`/api/v1/logout`; `totp()`/`refresh()` are deleted outright — both called endpoints that never existed on the real gateway at all (`refresh()` already dead code, confirmed unreachable during OC-53's own investigation). Without a server-communicated expiry, the boot-time session restore stopped locally pre-validating a stored `expiresAt` (the mock's own fabrication) — a persisted session is now optimistically treated as authenticated, and the already-existing `handleAuthError` reactive path (a real 401 from any subsequent request) correctly demotes it, confirmed to have real coverage across all of `useAuthErrorRouting`'s query-hook consumers plus `StreamContext`'s own SSE `onUnauthorized`. `StoredSession`/`SaveSessionInput` gained `operatorUuid`/`operatorUsername`/`isSuperuser`, threaded onto `AuthContext`'s own public interface (`operator` kept its existing name, now backed by real data; `operatorUuid`/`isSuperuser` are new) so `OC-56`/`OC-57` can consume them later without another storage-shape rewrite — no new UI was built for either. The mock gateway (`tools/mock-gateway/src/routes/auth.js`) was rebuilt around the same one-shot contract. **Final whole-branch review** (most capable model, elevated scrutiny — this gates access to the entire app) found a real Critical bug neither the task review nor the design itself could see in isolation: `AuthContext`'s environment-switch effect cleared session state on a gateway switch but not the *pending login* state, meaning an operator who typed credentials, navigated to `/totp`, switched environments via that stack's own reachable `EnvironmentBadge`, then completed their code, would have `completeLogin` send the OLD gateway's username/password to the NEW gateway's `/api/v1/login` — a real credential-misdirection window this specific restructuring (moving the password's transmission to the second screen) is what created; the old two-request flow could only ever misdirect an opaque challenge id and a 6-digit code, never a password. Fixed by clearing `pendingCredentials`/`hasPendingLogin` in that same effect, closing the window (confirmed end-to-end: `/totp`'s own `hasPendingLogin` guard now correctly redirects to `/login` after a mid-flow environment switch). Also fixed in the same round: the mock's `/login` had invented a failure distinction (`invalid_credentials` vs `invalid_totp`) the real gateway's single undifferentiated `rejected()` response doesn't have — collapsed to one generic check, matching the "never invent a distinction the backend doesn't provide" discipline OC-52 already established; and `docs/reference/gateway-api-contract.md` §1/§2/§2.1, which still documented the deleted two-step flow, corrected to the real one-shot contract. Two cheap Minors bundled in: boot-time session restore now validates `operatorUsername` is actually a string before trusting a stored record (clean upgrade path for a device with a pre-OC-55 stored session, rather than merely self-healing on the first 401); `login.tsx` now clears any abandoned pending credentials on its own mount, bounding how long a plaintext password could otherwise sit in the in-memory ref if an operator reached `/totp`, tapped "Volver," and never returned. Scoped re-review of the fix round: approved, 0 findings — every edit traced and confirmed against the actual diff, including the easy-to-miss `async` keyword change the fix needed. `npx tsc --noEmit` clean throughout every round; `npm run lint` 0 errors (64 pre-existing unrelated `import/no-duplicates` warnings, same baseline); `npm run format:check` clean. **Live verification**, all 6 checklist points from the design doc, run against `npm run mock-gateway` + `npx expo start --web` via `claude-in-chrome`, with exact request/response bodies captured through a `window.fetch` monkeypatch: (1)–(2) confirmed byte-for-byte — submitting the login screen fires zero network requests and navigates instantly; entering the TOTP code fires exactly one `POST /api/v1/login` (`{"username":"matias","password":"mock","totp_code":"000000"}` → `200 {"csrf_token":"...","operator_uuid":"11111111-1111-4111-8111-111111111111","operator_username":"matias","is_superuser":true}`); (3) a wrong code shows one generic inline error and allows an immediate retry on the same screen with no re-entry of username/password; (4) "Volver" returns to `/login` (fields not cleared on Volver itself — confirmed genuinely pre-existing behavior, traced via `git show` back to the very first commit that introduced the login screen, unrelated to this ticket's own contract fix, correctly left out of scope) and a fresh attempt still succeeds end-to-end; (5) a persisted session restores to `authenticated` on reload with no blocking network round trip, and subsequent real requests (status, players, stream) still succeed; (6) `/more`'s "Conectado como {operator}" line shows the mock's real `operator_username` value, confirming the full pipeline is genuinely wired end-to-end, not just type-correct. **Explicitly not fixed, named rather than silently dropped**: the login screen lost its VPN-down/gateway-unreachable affordance (`gatewayErrorMessage`/`VpnSettingsButton`) as a direct, correctly-scoped consequence of no longer making a network call on that screen — an operator on a dead WireGuard tunnel now only discovers it after typing a time-sensitive TOTP code on the second screen, up to `httpClient.ts`'s 10s timeout; design-approved as an accepted tradeoff at brainstorming time, flagged by the final review as worth a follow-up, not addressed here. **Out of scope, unchanged from the design**: any UI for operator identity/superuser status (`OC-56`/`OC-57`); native bearer-token session auth (`OC-58`, blocked on `xindeler-zuul`'s `ZG-52`); collapsing the two screens into one (explicitly decided against). | ✅ |
| OC-56 | Display operator identity once the gateway returns it | ✅ Done 2026-08-20, per `.superpowers/sdd/2026-08-20-audit-row-shape-and-identity-implementation/` plan, two tasks, both clean on first review. **Scope grew mid-investigation, with Matías's explicit go-ahead in chat**: items (1)-(2) of this row's original scope (thread operator identity through session storage, render it somewhere visible) turned out to already be fully satisfied as an incidental side effect of `OC-55`/`OC-58`'s own work — `AuthContext`'s `operator` field is already backed by the real gateway's `operator_username`, and `Más` already renders "Conectado como {operator}" against real data, confirmed live during `OC-55`'s own verification pass. The one remaining item — updating the Auditoría screen (`OC-28`) to show a readable name instead of a raw uuid — surfaced a much bigger problem on inspection: comparing this app's `AuditRowSchema` against the real, deployed gateway's actual `AuditEntry` struct (`git show origin/main:server/src/audit.rs` in `xindeler-zuul`) found 4 of 6 client fields wrong, not just the operator one — `ts: string` vs the real `created_at: i64` (unix seconds, wrong name AND type), `outcome: enum('ok'\|'error')` vs the real `outcome: String` (actual values `"success"`/`"failed"`, not what the client assumed), a `detail?: string` field with no backing in the real struct at all (a pure mock invention), and a missing `id: i64` the real gateway has and the client never used for its `FlatList` key. Matías confirmed absorbing the full shape fix into this ticket now rather than patching only the operator field and leaving the rest for later, since `AuditRow.tsx`/`AuditScreen.tsx` were already being touched. **Design**: `AuditRowSchema` rewritten to the real shape `{ id, operator_uuid, operator_username, action, payload, outcome, created_at }`; `outcome` deliberately kept as `z.string()` rather than a closed 2-value enum, since the real gateway's field is a Rust `String`, not a fixed set — validated mid-implementation when `xindeler-zuul` deployed unrelated work (`ZG-53`/`ZG-54`/`ZG-55`, pre-mutation audit rows + a year-long retention pruner) that added a third real outcome value (`"requested"`) and `RestartOutcome`-variant-name outcomes, confirmed via a fresh read of the newly-deployed `origin/main` to have zero impact on this branch precisely because of that choice. A new `formatUnixTime(seconds: number)` sibling to `formatTime` handles the unix-seconds `created_at` without touching `formatTime` itself (still used correctly by Chat/Logs, which get real ISO strings). `AuditRow.tsx`'s error styling changed from `outcome === 'error'` to `outcome !== 'success'`, matching the real semantics of an open-ended outcome string rather than a closed pair. `AuditScreen.tsx`'s `keyExtractor` moved from a fragile `${ts}-${operator}-${action}` composite to the real `id`. The local mock gateway (`tools/mock-gateway`) was rebuilt to match: `recordAudit()` in `audit.js` now emits the real shape (an incrementing `id`, unix-second `created_at`, no more `detail`); `operatorUuid` threaded from the login response through the session record (`routes/auth.js`) and `requireAuth` middleware (`middleware/auth.js`) so all 13 `recordAudit()` call sites across 6 route files could pass real `operator_uuid`/`operator_username` fields instead of a single fabricated `operator` string, and their `outcome` literals corrected from the invented `'ok'`/`'error'` to the real `'success'`/`'failed'`. Both `GET /audit` and the live `audit` SSE event are validated against the same `AuditRowSchema` (confirmed via `StreamClient.ts`), so this one schema fix covers both paths with no separate SSE-specific change needed. **Final whole-branch review** (independently re-verified the real `AuditEntry` struct against `xindeler-zuul`'s actual `origin/main` source rather than trusting the design doc, confirmed a field-for-field match): 0 Critical, 0 Important — the two per-task reviews and the final review all came back clean on the substantive code. Two Minor findings (stale shape-documenting comments in `tools/mock-gateway/src/state.js`, describing the old `ts`/`operator`/`detail` shape) were fixed directly rather than spun into a fix-dispatch round, given the two-line, comment-only, zero-functional-impact scope. `npx tsc --noEmit` clean throughout; `npm run lint` 0 errors (pre-existing warning baseline unchanged); `npm run format:check` clean. **Live-verified** against the mock gateway via curl during Task 2: a fresh login plus a `server/start` call produces an `/audit` entry with exactly the real keys (`id`, `operator_uuid`, `operator_username`, `action`, `payload`, `outcome`, `created_at`), `outcome` reading `"success"`, `created_at` a 10-digit unix-seconds value (not 13-digit milliseconds). **Out of scope, unchanged**: any gateway-side change (the real `xindeler-zuul` contract was already correct — this was a client-only fix); building against `xindeler-zuul`'s newly-shipped `ZG-53`/`ZG-54`/`ZG-55` work beyond what `outcome: z.string()` already tolerates; `OC-57` (the operator-admin screen), unrelated and blocked on different `xindeler-zuul` tickets. | ✅ |
| OC-57 | Operator-admin screen — superuser-only | ✅ Done 2026-08-20, per `.superpowers/sdd/2026-08-20-operator-admin-screen-implementation/` plan, three tasks (Task 2 had one task-level fix round) plus a final-review fix wave. **Unblocked**: `xindeler-zuul`'s `ZG-48` (superuser flag) and `ZG-49` (the admin API) both confirmed shipped and stable on `origin/main`, re-verified fresh against source rather than trusting this row's own earlier sketch — `GET/POST/DELETE /admin/operators` (`admin.rs`/`operators.rs`): list returns `{uuid, display_name, is_superuser, totp_status: "none"|"pending"|"confirmed", added_at}`; add is CSRF+step-up-gated, always non-superuser, does not trigger TOTP enrollment; remove is CSRF+step-up-gated, rejects self-removal outright before touching the database, revokes sessions + TOTP enrollment server-side on success. **Design**: `docs/specs/2026-08-20-operator-admin-screen-design.md`. Existing patterns reused wholesale, no new UI primitives invented: `ConfirmByTypingSheet` (`word="ADD"`/`"REMOVE"`, matching this app's established all-caps-English confirm-word convention alongside `"FIRE"`/`"UNLOCK"`/`"RESTART"`), `useDestructiveAction` (unmodified — this ticket only *consumes* it, per an explicit global constraint), the `href: null` secondary-screen pattern, `PlayersScreen`/`PlayerAccountsScreen`'s own list+form precedents. **Task 1** (API layer): `Operator` schema/type, `getOperators()`/`addOperator()`/`removeOperator()`, `useOperatorsQuery()` — clean on first review. **Task 2** (screen): `OperatorRow.tsx` (display name, superuser badge, TOTP-status label, "Quitar" hidden entirely on the operator's own row — not disabled, absent), `OperatorsScreen.tsx` (list + add form + two confirm sheets), the gated `more.tsx` entry point (`{isSuperuser && <Link .../>}`, confirmed genuinely absent from the render tree for anyone else, not just visually hidden). Task review found and fixed two real Important bugs missed by the plan's own code: a stale error banner surviving sheet cancellation (no `reset()` call), and a possible add double-submit (the trigger button wasn't disabled during the in-flight window) — both fixed, one deferred Minor noted (the equivalent remove-side guard, bounded consequence since a duplicate `DELETE` just 404s). **Task 3** (mock gateway): new `admin/operators` routes matching the real shape exactly, `req.isSuperuser` threaded through the mock's session/middleware, a new `requireSuperuser` middleware (403 for a valid-but-non-superuser session, mirroring the real `AuthenticatedSuperuser` extractor). **Final whole-branch review (most capable model, elevated scrutiny — this branch adds a new privilege boundary, not just a display feature)** found one real Important bug neither task-scoped pass could see in isolation: the `/operators` *route* itself had no superuser guard — only `more.tsx`'s entry-point link was gated, and `_layout.tsx`'s `href: null` explicitly leaves a route "mounted and reachable," not actually protected. Compounded by two pre-existing, newly-consequential gaps this review surfaced: `OperatorsScreen` silently swallows a `403` on an already-populated query (no error UI shown), and TanStack Query's cache isn't cleared on logout — so a non-superuser deep-linking `/operators` within the cache's `gcTime` window after a superuser's session could have seen a stale, previously-fetched operator list. Fixed with a `<Redirect>` guard directly in `app/(tabs)/operators.tsx`, matching this app's existing `totp.tsx` guard idiom exactly — verified to prevent the screen (and its query) from ever mounting for a non-superuser, not merely rendering a redirect alongside it. Two Minors bundled into the same fix wave: a fragile `removeTarget?.uuid ?? ''` fallback (never actually reachable, but replaced with a loud thrown error instead of a silent malformed request — deliberately without touching `useDestructiveAction`'s signature, honoring the same "don't touch that hook" constraint the whole ticket set for itself, since 9 other call sites depend on it); the mock's `POST /admin/operators` accepting any non-empty string as a uuid where the real gateway would `400` on a malformed one (added a UUID-format check so local testing stays honest). Two other deferred Minors from the task-loop (remove-side double-submit guard, no audit entry on a rejected self-removal) were explicitly triaged by the final review and confirmed to genuinely stay non-blocking under whole-branch context, not silently dropped. `npx tsc --noEmit` clean throughout; `npm run lint` 0 errors (70 pre-existing unrelated warnings); `npm run format:check` clean. **Live-verified**: full curl sequence against the mock (login → list 1 seeded operator → 403 before step-up → step-up → add → list 2 → 409 on duplicate → 400 on self-removal → delete → list 1 again); the route guard verified in-browser by temporarily flipping the mock's `isSuperuser` to observe the redirect, fully reverted before committing. **Out of scope, named not silently dropped**: `queryClient.clear()` on logout (a broader, separate follow-up touching `AuthContext.logout()` generally, not just this screen's cache); a `403 forbidden` from `requireSuperuser` would trigger `useDestructiveAction`'s existing retry-on-403 logic the same unproductive way `oracle_disabled` once did before a prior ticket excluded it — real but low-reachability, and this ticket's own global constraint forbids touching that hook, so it's flagged as an `OC-59`-adjacent follow-up rather than fixed here; a handful of small nits (a doc-comment enumeration drift in `_layout.tsx`, a brief "undefined" flash during a confirm sheet's dismiss animation, an unmount-cleanup gap on the success-message timer already present in the sibling screen this one copied from, a Spanish/English message-language inconsistency in mock error text) deferred as follow-up polish. | ✅ |
| OC-58 | Native session auth via bearer token (`expo-secure-store`) | ✅ Done 2026-08-20, per `.superpowers/sdd/2026-08-20-native-bearer-session-auth-implementation/` plan, one task plus two final-review fix rounds. **Unblocked**: `xindeler-zuul`'s `ZG-52` shipped and deployed (PR #61) — `POST /api/v1/login`'s response now carries `session_token`, the exact same raw value minted for the `Set-Cookie` header, and the gateway's session extractor accepts either the cookie (checked first) or an `Authorization: Bearer <session_token>` header (native's fallback). Confirmed directly against the real merged source before writing this ticket's plan, not assumed from the backlog row's own earlier guess (which happened to match: `session_token` was the real field name). **A genuinely good architectural detail confirmed during the final review, better than the design doc itself claimed**: `ZG-52` changed a single *shared* token-recovery helper (`auth_extractor::session_token`) that every already-`AuthenticatedOperator`-gated handler in the real gateway builds on for its own CSRF/step-up checks — so native gets step-up, `/audit`, `/broadcast`, and SSE too, not a half-working session limited to plain reads. **Implementation**: `LoginResponseSchema` gained `session_token`; `SaveSessionInput` gained `sessionToken`; `AuthContext.completeLogin`'s single `sessionStorage.save(...)` call threads it through; native's `SecureSessionStorage.getAuthHeader()` stopped being a hardcoded `undefined` and now returns `{ Authorization: 'Bearer <sessionToken>' }` when present. `httpClient.ts`, `StreamClient.ts`, and `streamOracleChat.ts` — the three consumers of `getAuthHeader()` — needed zero changes, confirmed by the final review, since native's real header now flows into every request (including SSE, which bypasses `httpClient` entirely) automatically via the existing dependency-injection pattern. All four session-teardown paths (`logout()`, the environment-switch effect, `handleAuthError`'s reactive 401/CSRF demotion, the boot-time stale-record guard) were confirmed to already call `sessionStorage.clear()` uniformly, so no stale bearer token can survive in `expo-secure-store` after the app believes a session is gone — zero new code needed there, the final review traced this explicitly rather than assuming it, given OC-55's own precedent of finding exactly this class of bug. **Final whole-branch review (most capable model, elevated scrutiny — this touches session-credential handling) found a real Critical bug neither prior pass caught**: `SecureSessionStorage.web.ts`'s `save()` was untouched by the task itself, but `SaveSessionInput` making `sessionToken` a required field meant every web login silently started writing the full bearer credential to `localStorage` in plaintext — both prior passes had checked that `read()` strips the field on the way *out*, but nobody checked that `save()` never wrote it in the first place. This converted a `Strict`/`HttpOnly`-confined cookie (unreadable by JS, unusable cross-site) into a portable, JS-readable, exfiltratable credential for the session's full 12h lifetime — a real increase in blast radius for XSS, a malicious extension, or shared-machine devtools access, not a restatement of an existing risk. Fixed by stripping `sessionToken` in `save()` itself, before the write, not just on read — confirmed live: a fresh web login's `localStorage.getItem('overlord.session.metadata')` genuinely contains no `sessionToken` key at all. Also fixed in the same round: `docs/reference/gateway-api-contract.md`, which after `OC-55`'s own correction pass now claimed the *opposite* of what shipped in four places — missing `session_token` from the route table, still calling native's bearer mechanism "not-yet-shipped," restating the pre-OC-55 "bearer header only, no cookie jar" framing backwards, and (most dangerously) still saying step-up's session extractor is "cookie only," which would have misled a future reader into concluding native can't step up at all and therefore can't run any destructive action — the opposite of what the final review actually verified against the real source. A cheap Minor bundled in: the mock gateway's own `requireAuth` middleware had cookie/bearer precedence inverted relative to the real gateway (bearer-first instead of cookie-first) — unobservable today since both transports carry the same value locally, but exactly the "mock fabricates behavior the real gateway doesn't have" root cause that produced this whole ticket, so it was aligned to match. **One small residual caught by the scoped re-review of that fix round and corrected in a follow-up commit**: the doc-fix brief's own two edits (§1 and §2) ended up asserting opposite claims about whether native has a cookie jar at all (§1 correctly said yes, just weaker storage than `expo-secure-store`; §2's edit said no cookie jar exists) — a self-contradiction introduced by applying two edits from the same brief without cross-checking them against each other, caught and reworded to agree. `npx tsc --noEmit` clean throughout every round; `npm run lint` 0 errors (64 pre-existing unrelated warnings, same baseline); `npm run format:check` clean. **Live verification**: curl-verified the mock genuinely accepts a bearer header carrying the same raw value as the cookie, and (after the fix round) correctly prefers the cookie when both are present; a real `npx expo run:ios` Simulator build succeeded, installed, and launched (screenshot-confirmed, not crashed); a rigorous web negative-check (via a temporary, cleanly-reverted debug middleware logging every request's `Authorization` header across a full login→browse→logout UI cycle) confirmed web never sends the header, both before and after the fix round; the post-fix `localStorage` check confirmed above. **Explicitly and honestly not verified**: the native login→bearer-header→authenticated-request→logout sequence was never driven end-to-end through the Simulator's own UI — this session's tooling is Chrome-only, with no way to interact with the iOS Simulator, the same limitation `OC-46`'s biometric-lock ticket hit repeatedly. Build/install/launch succeeding is a real, checkable signal; the actual bearer-header round trip inside the native app itself was not directly observed, only proven correct by code inspection, the mock's own curl-verified acceptance of a bearer header, and the real gateway's own regression tests (`auth_extractor.rs`'s `accepts_a_bearer_only_request_for_authenticated_operator`/`_superuser`, `stream.rs`'s bearer-only SSE test) confirming the server half of the handshake. **Out of scope, unchanged from the design**: any change to web's own auth mechanism (unaffected, correct as-is); the login-flow request/response-shape fix itself (`OC-55`, already shipped independently). | ✅ |
| OC-59 | Fix audit + broadcast against the real gateway (step-up gap, `msg` field mismatch) | ✅ Done 2026-08-20, per `.superpowers/sdd/2026-08-20-audit-broadcast-step-up-fix-implementation/` plan, three tasks plus a two-round final-review fix wave. **What was broken and how it was found:** while scoping `OC-57`, re-read `docs/reference/gateway-api-contract.md` and found two already-flagged-but-never-ticketed gaps (2026-08-15, `OC-54`'s own final review): the real gateway step-up-gates both `GET /api/v1/audit` and `POST /api/v1/broadcast`, but this app called `/audit` as a plain unguarded `GET` and sent `/broadcast` outside `useDestructiveAction` entirely — both would `403` against the real, deployed gateway despite working fine against the mock's own invention. A third, new mismatch surfaced mid-investigation: the real gateway's broadcast body field is `msg` (`lifecycle.rs`'s `BroadcastRequest`), not `message` — the client and the mock had independently invented `message`, agreeing with each other but not the real gateway. A fourth: the real gateway's `stream.rs` has no `audit` SSE event at all — the "new row appears live" behavior this app relied on was a pure mock invention, meaning Auditoría also needed a manual-refresh affordance it didn't have. Matías confirmed (chat): ticket it as urgent and start before `OC-57`, and fold in the also-flagged plain-text-error-body issue since it directly affects the new work. **Design**: `docs/specs/2026-08-20-audit-broadcast-step-up-fix-design.md`, one open UX fork resolved in chat (automatic step-up prompt on Auditoría entry, matching this app's existing "operator only ever sees the transparent TOTP prompt" convention, not a manual gate button). **Task 1** (`httpClient.ts`): reads a non-2xx body as text first, attempts the JSON envelope, falls back to the raw trimmed text (capped 500 chars) instead of a generic "Error inesperado" message — the real gateway sends plain text for many error responses. **Task 2** (broadcast): `writeApi.ts` sends `{ msg }` and switched from a `{ok:true}`-assuming `OkResponseSchema` to `http.request<void>` (the real gateway returns `204`, confirmed directly); `BroadcastComposer.tsx` rewritten around `useDestructiveAction` (free step-up) plus a new `ConfirmByTypingSheet` (`word="BROADCAST"`) it didn't have before, since the real gateway's own doc comment ranks broadcast alongside restart/immediate-stop in consequence and every other action at that level already has typed confirmation. **Task 3** (audit): new `useStepUpGate()` hook (`src/auth/`, deliberately not `features/status/` where the write-oriented `useDestructiveAction` lives) auto-establishes step-up on screen entry; `AuditScreen.tsx` restructured into a gate wrapping the pre-existing list body, which also gained pull-to-refresh (the real gateway's only path to new rows, absent a live SSE event). Task 3's own task-review found and fixed an Important bug in the new hook's first draft (a boolean `cancelled` flag didn't prevent a superseded invocation's late-arriving network response from overwriting a newer one's outcome — fixed with a monotonic ref-based invocation-identity check instead). **Final whole-branch review (most capable model, elevated scrutiny — this branch touches step-up/auth-adjacent logic)** found this ticket's own failure signature recurring inside its own fix: a **Critical** bug where `useStepUpGate` reimplemented `useDestructiveAction`'s step-up-establishing call without its hard-won 401-recovery branch (a cached-but-rejected TOTP code — the client caches for 90s, a real code is valid ~30s — left "Reintentar" a silent dead end until the cache naturally expired; invisible against the mock's fixed `'000000'` code, the ordinary case against the real gateway), plus two **Important** findings (the audit gate's re-arm-on-lapse logic only covered manual pull-to-refresh, not the automatic window-focus refetch TanStack Query already performs — arguably the more common trigger, and the only one reachable at all on web since `RefreshControl` is a no-op stub there; and `gateway-api-contract.md` still asserting the exact contract this branch just disproved in five places). Fixed by extracting a shared `src/auth/establishStepUp.ts` helper both hooks now call (verified line-by-line to preserve `useDestructiveAction`'s exact `forceFreshStepUp` semantics and leave its separate write-retry-on-403 mechanism untouched), correcting all five stale doc assertions, and — since the SDD skill's fix-wave is single-shot but the residual gap was real and reachable via the ticket's own headline scenario (an operator backgrounding the app past the window) — one further Matías-directed manual round: routing the automatic-refetch path through the same re-arm logic via a stale-cache-race-safe timestamp comparison (`query.errorUpdatedAt` against a per-gate-session anchor, deliberately not `useFocusEffect`, which the implementer correctly identified would never observe this app's `AppState`-driven `refetchOnWindowFocus` trigger), plus closing a Minor the same round surfaced: a cancelled step-up prompt on a re-armed gate previously left the operator with zero retry affordance and no way back short of an app restart, since `useStepUpGate`'s `ready:false, error:null` shape couldn't distinguish "loading" from "cancelled" — fixed with an explicit `pending` field. Every round's re-review confirmed its fix correct with no new breakage, including a careful trace of the shared-helper extraction's blast radius (every destructive action in the app routes through `useDestructiveAction`) and the timestamp-comparison's race-safety. `npx tsc --noEmit` clean throughout; `npm run lint` 0 errors (66 pre-existing unrelated warnings, baseline drifted by +1 from `OC-56`/`OC-58`'s own count for unrelated reasons, not this branch); `npm run format:check` clean. **Live-verified**: broadcast's full confirm→step-up→send flow against the mock with the request body confirmed via curl to genuinely be `{"msg": ...}`; audit's full prompt-on-entry→cancel-shows-retry→complete-reveals-list→pull-to-refresh flow; the 401-recovery fix end-to-end (a wrong TOTP code now re-prompts automatically instead of dead-ending); the automatic-refetch re-arm via injected `visibilitychange` events (network logs confirmed exactly one automatic `403` → re-arm → one clean `200`, no loop). **Out of scope, named not silently dropped**: the other five `OkResponseSchema`-typed write methods (`startServer`/`stopServer`/`restartServer`/`cancelShutdown`/`disconnectAll`) share broadcast's pre-fix return-type inaccuracy (the real gateway returns `204` for all of them too) — harmless today only because `httpClient.ts`'s `204` short-circuit runs before schema validation, but a real follow-up worth ticketing; raw English gateway error text (Task 1's fallback) technically breaks this app's Spanish-only convention on that one rare path, accepted as a legibility-over-strict-Spanish trade-off rather than fixed; a content-type guard on the plain-text fallback, deferred (no disclosure risk per the final review's own analysis); and the broader pattern this investigation surfaced — three independent client/mock agreements-with-each-other-but-not-the-real-gateway found from re-reading one pair of handlers — flagged as worth a dedicated follow-up ticket auditing every mock route's shape against real source, not investigated further here. | ✅ |
| OC-60 | Manual dark/light mode toggle | Added 2026-08-20 (Matías, chat). The app already follows the OS `prefers-color-scheme` automatically across all three targets (iOS/Android/Web) — NativeWind's `darkMode: 'media'` (`tailwind.config.js`) plus `useTheme()`'s own `useColorScheme()` call (`src/ui/theme.ts`), consistently applied via `dark:` classNames across 41 files. Design is dark-first (`ops-ui` SKILL.md) — light is "a courtesy... so the app doesn't break if the OS is set to light," not an equally-designed alternative. **This ticket is not about building dark mode from scratch — it already exists.** It's about adding an in-app manual override (e.g. a segmented control or toggle in `Más`, next to `EnvironmentSwitcher`) so an operator can force light or dark regardless of the OS setting, plus a persisted preference (likely `AsyncStorage`, matching `EnvironmentContext.tsx`'s own precedent) and a way to fall back to "follow system." Scope check before starting: confirm whether RN's `useColorScheme()` itself supports a manual override natively (`Appearance.setColorScheme()` exists on newer RN/Expo versions — check against this app's installed RN 0.86/Expo SDK 57 before assuming a React-Context-based manual-override layer needs to be hand-built) before designing the mechanism. | ⬜ |
| OC-61 | Android back button from hidden tabs lands on Status instead of the entry screen | Added 2026-08-21, encontrado durante la verificación manual de OC-36a (pasada de verificación Android), no bloqueante para esa rama — diagnóstico ya completo, solo falta implementar el fix. Repro: navegar Más → Auditoría (o cualquier otro screen alcanzado como `Tabs.Screen` oculto — `player-accounts`, `operators`, `oracle-composer`, `oracle-trigger`, `oracle-chat`, todos declarados con `href: null` en `app/(tabs)/_layout.tsx`), completar el step-up si aplica, presionar el botón de back de Android **una sola vez**. En vez de volver a Más (la pantalla desde la que se entró), la app aterriza en Status (el primer tab). **Causa raíz ya confirmada, no es una regresión de OC-36a**: verificado con un experimento de control real (editar `android:enableOnBackInvokedCallback` directamente en el `AndroidManifest.xml` ya buildeado, rebuild, repetir la secuencia idéntica con la flag apagada — resultado idéntico con la flag prendida o apagada), descartando que sea causado por `predictiveBackGestureEnabled`. El comportamiento es del propio navegador de tabs: estos screens son hijos ocultos del `Tabs` navigator (no screens apiladas en un stack), y ese navegador resuelve el back-press a la ruta inicial en vez de al historial de visitas para esta configuración. Severidad baja (no crashea, no pierde datos, aterriza en una pantalla válida) pero es una inconsistencia real y reproducible para un operador en medio de un incidente. Ver `docs/superpowers/plans/2026-08-21-android-platform-verification-implementation.md`'s Task 1 report (ya no existe en disco, era workspace de SDD gitignoreado, pero el diagnóstico completo quedó citado en la fila de OC-36a) para el detalle completo del experimento de control. No investigado todavía: si la solución correcta es envolver estos screens en un `<Stack>` en vez de tabs ocultos, o algo más liviano a nivel de navegación. | ⬜ |
| OC-62 | Fix: pantalla de Jugadores rota contra el gateway real (`GET /players` sin uuid) | ✅ Done 2026-08-21, `docs/specs/2026-08-21-players-real-shape-fix-design.md` / `docs/superpowers/plans/2026-08-21-players-real-shape-fix-implementation.md`. Bug urgente encontrado investigando el contrato real de la feature de moderación de jugadores (ver OC-35 sub-parte 2 y ZG-57 abajo) — no relacionado a esa feature, preexistente en la pantalla de Jugadores ya shippeada. **Causa raíz confirmada contra fuente real**: `GET /players` en `xindeler-zuul` (`server/src/console.rs`/`engine.rs`'s `fetch_players`) devuelve `Option<Vec<String>>` — un array plano de nombres online, sin uuid, sin wrapper de objeto (confirmado con el propio test del handler, que espera literalmente `["Jugadora","Jugador2"]`). El cliente esperaba `PlayerSchema = {alias, uuid}`, así que `PlayersResponseSchema.parse()` fallaba de inmediato contra el gateway real — y aunque no fallara la validación, `PlayerRow.tsx` igual crasheaba (`player.uuid.slice(0,8)` sobre un string plano). El mock nunca lo detectó porque inventaba un uuid que el gateway real nunca tuvo — mismo patrón de "cliente y mock de acuerdo entre ellos, no con la fuente real" que ya se vio varias veces esta sesión (login, CSRF, step-up, audit; ver también la nota de cierre de OC-59 sobre este patrón recurrente). **Fix**: `Player` pasa de objeto a `string` en todo el cliente — `src/api/schemas.ts` (`PlayerSchema` eliminado, no deprecado), `PlayerRow.tsx` (deja de mostrar uuid), `PlayersScreen.tsx` (`keyExtractor` ajustado), `OracleDryRunScreen.tsx` (el picker de jugador de la vista previa de ORACLE — `isOnline`/`buildTarget`/`playersRef`/`ChipPicker`, ya solo usaban `.alias` en todo el archivo, se simplifican a `string[]` sin otro cambio de comportamiento), y `tools/mock-gateway/src/routes/players.js` (la ruta ahora mapea el fixture rico a strings planos en la respuesta HTTP — el fixture interno no se toca, porque `oracleTrigger.js` sigue necesitando la forma rica). Task único, review de task y revisión final de rama ambas limpias, sin findings bloqueantes. **Verificación manual real** contra `npm run mock-gateway` + `npx expo start --web`: Jugadores muestra los 5 alias sin uuid y sin crashear; el picker de ORACLE sigue funcionando igual (selección, validación de "sin jugadores conectados" en escenario `down`); pull-to-refresh no se pudo ejercer literalmente con mouse en Web (limitación de `react-native-web`, no de este fix — el único cambio en `PlayersScreen.tsx` fue el `keyExtractor`, `handleRefresh` quedó intacto), verificado indirectamente vía cambio de escenario + reload. `npx tsc --noEmit`, `npm run lint` (0 errores, mismos warnings preexistentes), `npm run format:check` todo limpio. ⚠️ **Hallazgo preexistente, no arreglado acá (fuera de alcance)**: la revisión final de rama notó que `console.rs` documenta que estas rutas devuelven `null` (no `[]`) cuando el engine no es alcanzable, pero `PlayersResponseSchema` sigue sin aceptar `null` — fallaría la validación de Zod en ese caso puntual (no ejercido por el escenario `down` del mock, que devuelve `[]`). No es una regresión de este fix (el schema viejo tampoco aceptaba `null`), queda anotado para una futura pasada de auditoría de nullability contra el gateway real. | ✅ |
| OC-63 | Fix: pantalla de Status rota contra Zuul real — mismo patrón recurrente que OC-62/OC-59 (contrato inventado, nunca verificado contra la fuente real) | Encontrado en vivo por Matías 2026-08-27 probando el login real contra Público: Status no carga nada, "La respuesta del gateway no tiene el formato esperado". Investigación completa hecha en la sesión que abrió esta fila, contra el código fuente real de `xindeler-zuul` y `xindeler-new-horizon` (no asumido) — mismo patrón que OC-62 y OC-59 ya documentaron: el cliente y el mock (`tools/mock-gateway`) se pusieron de acuerdo entre ellos sobre un contrato que nunca existió del lado real. En este caso el gap es más grande que OC-62 (un solo campo): `StatusSchema` entero (`src/api/schemas.ts`), `useLifecycleState.ts`, y el evento `lifecycle` del stream fueron diseñados contra un contrato especulativo, antes de que Zuul existiera de verdad. **Bloqueado hasta que aterricen `xindeler-new-horizon` BL-89 y `xindeler-zuul` ZG-63** (filas ya escritas en esos repos, mismo día, con el detalle técnico completo) — no arrancar antes, los campos nuevos todavía no existen del lado real. <br><br>**Lo que realmente devuelve Zuul hoy** (confirmado leyendo `status.rs`/`stream.rs` fuente): `GET /status` → `{game_server: string (systemctl crudo, no un enum cerrado), info: {version, player_count, shutdown_pending_secs} | null, restart: {operator_uuid, started_at, outcome} | null}`. `/stream/status` hoy empuja un string plano sin nombre de evento (`Event::default().data(value)`, ni JSON ni `event: status`) — el stream queda completamente indispatchable contra `StreamClient.ts`'s `handleRawEvent`, que despacha por `event:` y valida JSON contra `STREAM_SCHEMAS`. Una vez BL-89/ZG-63 aterricen, `info` va a sumar `entity_count`/`tick_time_ms`/`uptime_secs`/`shutdown_reason`, y el stream va a mandar `event: status` con el `StatusResponse` completo como JSON. <br><br>**Cambios necesarios en este repo, una vez desbloqueado:** <br>1. `src/api/schemas.ts`: reescribir `StatusSchema` para matchear el shape real (`game_server`/`info`/`restart`, no `service`/`health`/`uptime_secs` sueltos) — `info` opcional (`null` cuando el motor no responde, hay que decidir cómo se ve la UI en ese caso: `isUp` ya no puede depender de un campo `health` que no existe, la señal real es `game_server === 'active' && info !== null`). <br>2. `StreamClient.ts`'s `STREAM_SCHEMAS`: el evento `status` pasa a validar el `StatusResponse` real; **eliminar el evento `lifecycle` por completo** — no existe del lado real y no hace falta: `useLifecycleState.ts` ya tiene un fallback `deriveFromStatus()` (escrito originalmente como fallback "antes de que llegue un evento real") que puede pasar a ser el único camino, ahora que `status` va a traer `game_server`+`shutdown_pending_secs`+`shutdown_reason` en cada push. <br>3. `useLifecycleState.ts`: simplificar a derivar todo de `status` (sacar la reconciliación `live`/`contradicts` que existía solo para pelear entre dos fuentes que ahora colapsan en una). Al mapear `game_server`, decidir junto con quien implemente ZG-63 cómo se pliegan `activating`/`deactivating`/`unknown` (valores reales de `systemctl is-active`, no un enum cerrado) sobre los 4 estados de `LifecycleState`. <br>4. `StatusScreen.tsx`: `StatRow`s de Versión/Jugadores pasan a leer `info.version`/`info.player_count` (con fallback visible cuando `info` es `null`, no un crash); **Uptime/Tick time/Entidades ya no existen como "—" perpetuo sino como datos reales** una vez BL-89 los expone; **Chunks se cae del todo** — la investigación no encontró ningún dato equivalente en el motor real, no inventar un placeholder. `pending_shutdown.reason` pasa a leer `info.shutdown_reason`. <br>5. `tools/mock-gateway`: actualizar para servir el shape REAL (mismo remedio que OC-62 ya aplicó ahí) — si no, este mismo patrón de "mock miente, cliente le cree" va a volver a pasar la próxima vez que se toque esta pantalla. <br><br>No se tocó código todavía en esta fila — queda para cuando BL-89/ZG-63 estén mergeados y desplegados; verificar entonces contra Zuul real (Público), no solo contra el mock actualizado. <br><br>**Update 2026-08-28**: implementado en coordinación directa con la sesión de `xindeler-zuul` (trabajando ZG-63 del lado de ese repo, mientras esa fila pasaba por revisión de seguridad) sobre la misma rama compartida `oc63/status-contract-match-real-zuul` — los 5 cambios de la lista de arriba están hechos, más el fix de la URL del stream (`/api/v1/stream` → `/api/v1/stream/status`, ver OC-65). `npx tsc --noEmit`/`npm run lint`/`npm run format:check` limpios, verificados independientemente por las dos sesiones antes de commitear. **Pendiente**: verificar en vivo contra Zuul real (Público) una vez que ZG-63 esté mergeado y desplegado — el trabajo del cliente está listo pero no se probó todavía contra producción real, solo contra `tsc`/`lint`/el mock actualizado. | ⬜ |
| OC-64 | Fix crítico: el manejo de sesión vencida/revocada nunca se activa contra Zuul real — la app queda trabada mostrando errores genéricos en vez de mandar al login | Encontrado 2026-08-28, auditoría completa del contrato Overlord↔Zuul pedida por Matías tras el merge de ZG-63 (mientras Zuul hace una revisión de seguridad antes de mergear). `AuthContext.handleAuthError` (`src/auth/AuthContext.tsx:176-191`) solo demota a `unauthenticated` cuando `ApiError.code` es `session_expired`/`unauthorized`/`invalid_csrf` — pero `httpClient.ts` (`:65-93`) solo produce esos códigos parseando un envelope JSON `{error:{code,message}}` que Zuul real **nunca manda** para fallas de auth: `auth_extractor.rs:17-18` (401 texto plano "not logged in"), `login.rs:87-89` (401 texto plano "invalid credentials"), `lifecycle.rs:246` (403 texto plano "step-up required"), `lifecycle.rs:272-273` (403 texto plano "invalid csrf token"). Sin JSON parseable, `httpClient.ts` cae siempre a `code: 'unknown_error'` (línea 88-92) — así que una sesión que expira (`IDLE_TTL_SECS=30min`, `ABSOLUTE_TTL_SECS=12h`, `session.rs:9,12`) o se revoca **nunca** dispara `handleAuthError` en ningún llamado que pase por el `httpClient` compartido (o sea, casi todo el cliente). El operador ve un error genérico y la app se queda mostrando `authenticated` (el restore al montar es puramente optimista/local, nunca re-valida con el server) — solo un logout manual lo saca de ahí. Único parche parcial existente: `streamOracleChat.ts:116-121` sintetiza `code:'unauthorized'` a mano leyendo el status 401 directamente, pero solo para ese único endpoint. **Fix**: `httpClient.ts` debe mapear por status code además de (o en vez de) por el envelope JSON — un 401 sin envelope parseable debería seguir siendo tratado como `unauthorized`, un 403 sin envelope como candidato a `invalid_csrf`/step-up, no colapsar todo a `unknown_error`. Repasar cada call site que hoy asume el envelope JSON para decidir el mapeo correcto por status. **Fix implementado 2026-08-28** (rama `oc64-oc71-client-contract-fixes`): `httpClient.ts` ahora sintetiza `unauthorized`/`invalid_csrf`/`step_up_required` desde status+texto plano en vez de depender de un código JSON que Zuul real nunca manda. Verificado contra `tsc`/lint/format y manualmente contra el mock actualizado; falta verificación en vivo contra Zuul real. | ⬜ |
| OC-65 | El stream compartido (`/api/v1/stream`) no existía en Zuul real — solo `/stream/status` — logs/chat/auditoría en vivo nunca tuvieron soporte real | Encontrado 2026-08-28, misma auditoría que OC-64. `StreamContext.tsx:26` conectaba a `${environment.baseUrl}/api/v1/stream`, esperando multiplexar 5 eventos (`status`/`log`/`chat`/`lifecycle`/`audit`, ver `StreamClient.ts:18-24`). El router real de Zuul (`server/src/web.rs:39-71`, tabla completa confirmada línea por línea) solo tiene `/stream/status` — no existía ninguna ruta `/stream` a secas, y el fallback dentro de `/api/v1/*` es un 404 explícito (`web.rs:101-103`). **La parte de la URL ya quedó resuelta** — la sesión de `xindeler-zuul` (trabajando la misma rama `oc63/status-contract-match-real-zuul` en paralelo, coordinado en el chat) arregló `StreamContext.tsx:26` a `/api/v1/stream/status` como parte del fix de OC-63. **Lo que queda pendiente, más chico de lo que parecía**: `StreamEventMap`/`STREAM_SCHEMAS` (`StreamClient.ts`) todavía listan `log`/`chat`/`audit` como eventos posibles — pero `/stream/status` es de un solo propósito (confirmado, solo emite `event: status`), así que esos tres nombres de evento nunca van a disparar contra el gateway real: no rompen nada (no hay ningún `.on('log', ...)` que crashee por falta de datos), simplemente quedan mudos para siempre. Fix menor: sacar `log`/`chat`/`audit` de `StreamEventMap`/`STREAM_SCHEMAS` (ya no queda ningún caso de uso real detrás de esos tres), y confirmar que `LogsScreen`/`ChatScreen`/`AuditScreen` no dependían de esos eventos para nada más que una actualización en vivo opcional (deberían tener su propio bootstrap fetch vía `readApi.ts` como fuente principal, ver OC-67 para los bugs de shape de esas mismas rutas). **Client-side cleanup 2026-08-28**: `LogRow`/`useLogsQuery`/`LogsScreen`/`ChatMessageRow`/`useChatQuery` ya no asumen el stream compartido inexistente — la suscripción SSE muerta queda documentada pero sin quitar, por si algún día existe. No agrega el stream real (sigue sin existir del lado de Zuul). | ⬜ |
| OC-66 | `getPlayers()` rompe siempre contra Zuul real — el schema espera un array de strings, Zuul manda objetos (o `null`) | Encontrado 2026-08-28, misma auditoría. `PlayersResponseSchema` (`src/api/schemas.ts:46-52`) es `z.array(z.string())`, con un comentario que dice estar "confirmado contra la fuente real" — describe una versión vieja de la ruta, de antes de que el trabajo de ZG-57 (directory) le sacara el `uuid` y la reshapeara. Hoy `GET /players` (`server/src/console.rs:23-36`, `engine.rs:87-117`) devuelve `Option<Vec<PlayerOnlineView>>` donde cada fila es `{alias, position: [f32;3]|null, character_id: number|null}` — confirmado por el propio test del handler (`console.rs:235-285`) — y devuelve `null` (no `[]`) cuando el motor no responde. Como el schema es `z.array(z.string())`, tanto el array de objetos como el `null` fallan validación siempre — **`getPlayers()` no tiene ningún camino que funcione hoy contra producción**. Rompe la lista de Jugadores y el selector de destino de Oracle dry-run (`OracleDryRunScreen.tsx`, que reusa `usePlayersQuery`). Fix: reescribir `PlayersResponseSchema` para matchear `{alias, position, character_id}[] \| null`, y actualizar todo lo que hoy lee `player` como string plano (comparar con el fix ya hecho en OC-62 para el mismo tipo de problema en esta misma ruta, en otro momento). **Fix implementado 2026-08-28**: `PlayersResponseSchema` reescrito a `{alias, position, character_id}[] | null` (`schemas.ts`), `OracleDryRunScreen.tsx` proyecta `.alias` en el único punto de consumo. Mock (`tools/mock-gateway/src/routes/players.js`) actualizado al mismo shape. Verificado contra `tsc`/lint/format y manualmente (curl + browser) contra el mock; falta verificación en vivo contra Zuul real. | ⬜ |
| OC-67 | Chat, Logs y Crónica: rutas equivocadas y formas de datos totalmente distintas — las tres rompen siempre contra Zuul real | Encontrado 2026-08-28, misma auditoría. Tres bugs relacionados, mismo patrón: <br>**Chat** — `getChat()` (`readApi.ts:34-37`) pega a `/api/v1/chat`, la ruta real es `/api/v1/chat/history` (`web.rs:56`, handler `console.rs:89-102`) → 404 siempre. Aunque se arregle la ruta, la forma tampoco matchea: `ChatMessageSchema` espera `{author, message, ts}` (`schemas.ts:151-157`), el motor real serializa `{time, parties, content}` donde `content` es un enum con tag (`Say`/`Tell`/`Group`/`Region`/...) que carga un `PlayerInfo` (`xindeler-new-horizon/server/src/chat.rs:51-56`) — no hay ningún `author`/`message` plano en ningún lado. El parámetro `since` que arma el cliente tampoco existe — el real es `from_time_exclusive_rfc3339` (`console.rs:81-84`) — hoy es código muerto porque `useChatQuery.ts` nunca lo pasa, pero hay que arreglarlo si algún día se usa. <br>**Logs** — `LogLineSchema` espera objetos `{ts, level, target, message}` (`schemas.ts:140-146`); la ruta real (`console.rs:40-45`) devuelve un array de strings crudas (líneas de texto, `Vec<String>` desde el motor). Además el `limit` que manda el cliente (`readApi.ts:29-32`, pide hasta 200) se ignora del todo — no hay `Query` extractor en el handler, y el motor hardcodea `.take(30)` — el cliente nunca puede recibir más de 30 líneas pase lo que pase, así que el buffer de 200/500 líneas que arma la UI está construido sobre una capacidad que el backend no ofrece. <br>**Crónica** — mismo problema que Logs: `ChronicleResponseSchema = z.array(z.record(...))` espera objetos, la ruta real (`console.rs:69-79`) devuelve `Vec<String>` directo desde el motor (`ListChronicle`, `xindeler-new-horizon/server-cli/src/main.rs:538-546`). <br>Fix para las tres: reescribir los tres schemas a array de strings simples (Logs y Crónica) y al shape real con el enum de `content` (Chat), ajustar las pantallas que leen `.author`/`.message`/`.ts`/campos de objeto, y bajar la expectativa de "200/500 líneas" de Logs a lo que Zuul realmente entrega (30). **Fix implementado 2026-08-28**: `LogLineSchema`/`ChatMessageSchema`/`ChronicleResponseSchema` reescritos a los shapes reales, rutas de chat corregidas (`/chat/history`), nuevo `describeChatMessage.ts` como fallback honesto para el `parties`/`content` sin confirmar. Verificado contra `tsc`/lint/format y manualmente contra el mock actualizado; falta verificación en vivo contra Zuul real. | ⬜ |
| OC-68 | `broadcastMessage()` pega a la ruta equivocada — nunca funciona | Encontrado 2026-08-28, misma auditoría. `writeApi.ts:164-170` POSTea a `/api/v1/broadcast`; la ruta real es `/api/v1/server/broadcast` (`web.rs:63`, handler `lifecycle.rs:688`) — 404 siempre, el operador nunca puede mandar un mensaje a los jugadores conectados, y ni siquiera llega a la lógica de step-up/CSRF para enterarse de que algo anda mal (el 404 llega antes). Fix de una línea: cambiar el path en `writeApi.ts`. El resto ya está bien: la respuesta real es `204` sin body (`lifecycle.rs:739`) y el cliente no pasa schema para esta llamada, así que una vez arreglado el path no hace falta ningún otro cambio. **Fix implementado 2026-08-28**: `broadcastMessage()` ahora pega a `/api/v1/server/broadcast`. Mock actualizado al mismo mount. Verificado contra `tsc`/lint/format y manualmente contra el mock; falta verificación en vivo contra Zuul real. | ⬜ |
| OC-69 | `restartServer()` espera un body JSON, Zuul manda `202 Accepted` vacío — reiniciar el servidor parece fallar siempre aunque el reinicio sí se ejecute | Encontrado 2026-08-28, misma auditoría (confirmado leyendo `lifecycle.rs` directamente, no vía subagente). Todas las acciones de ciclo de vida (`start`/`stop`/`cancel_shutdown`/`disconnect_all`/`broadcast`) devuelven `204 No Content` en éxito — que `httpClient.ts` (`:95-97`) reconoce como "sin body" y salta la validación de schema. `restart` es la única que devuelve `202 Accepted` (`lifecycle.rs:989`) — un status que `httpClient.ts` NO trata como especial, así que sigue de largo hasta `response.json()` sobre un body vacío, falla el parseo, y tira `ApiError('invalid_response', ...)`. Pero `writeApi.ts`'s `restartServer()` (línea ~55-62) pasa `OkResponseSchema` esperando `{ok: true}` en el body — que Zuul real nunca manda para esta ruta. Resultado: **cada vez que un operador reinicia el servidor, la app le muestra un error, aunque el reinicio se haya disparado correctamente** (confirmar contra Público antes de dar por buena cualquier UI que dependa de este resultado). Fix: `httpClient.ts` debe tratar `202` igual que `204` (sin body esperado) para esta ruta, o `restartServer()` debe dejar de pasar `OkResponseSchema` y simplemente no esperar body — la señal real de "se disparó" ya está en el status 202 mismo, no en un payload. **Fix implementado 2026-08-28**: `httpClient.ts` trata `202`/`204` igual (sin body), `startServer`/`stopServer`/`restartServer`/`cancelShutdown`/`disconnectAll` ya no esperan un `{ok:true}` que Zuul real nunca manda — de paso se encontró y corrigió un bug real distinto: `disconnectAllAction`'s `if (ok)` en `StatusScreen.tsx` quedaba SIEMPRE falso contra este fix (`undefined` no es truthy), corregido a `!== null`. Verificado contra `tsc`/lint/format y manualmente contra el mock; falta verificación en vivo. | ⬜ |
| OC-70 | Ban/unban: el resultado "falló del todo" nunca se muestra — llega con status 502, el cliente lo trata como error genérico antes de leer el `outcome` | Encontrado 2026-08-28, misma auditoría. `BanPlayerResponseSchema`/`UnbanPlayerResponseSchema` (`schemas.ts:126-138`) incluyen `outcome: 'failed'`, y `PlayerDetailScreen.tsx` tiene mensajes dedicados para ese caso ("El ban falló por completo.") bajo un comentario que asume que ese resultado "sigue devolviendo un 200 OK". Zuul real (`players.rs:919-924` ban, `:1077-1082` unban) sí devuelve `200` para los casos parcial/completo-éxito, pero el caso `'failed'` viaja en un **`502 Bad Gateway`**, aunque serializa el mismo JSON (`{"account":null,"connection":null,"outcome":"failed"}`). Como `httpClient.ts` trata cualquier no-2xx como error ANTES de intentar leer el body como el tipo de respuesta esperado, ese body nunca llega a la lógica de `outcome` — el operador ve un `ApiError` genérico con el JSON crudo como mensaje, no el texto en español pensado para ese caso. Fix: `httpClient.ts` (o un manejo puntual en `banPlayer`/`unbanPlayer`) necesita reconocer este 502-con-body-conocido como el caso `'failed'` en vez de tratarlo como un error de transporte genérico. **Fix implementado 2026-08-28**: `parseNonOkBodyAsData` nuevo en `httpClient.ts`, usado por `banPlayer`/`unbanPlayer` para recuperar el body `outcome`-shaped de una respuesta 502. Verificado contra `tsc`/lint/format; el mock no simula todavía el caso `outcome: 'failed'` (gap de cobertura conocido, aceptable por ahora). | ⬜ |
| OC-71 | Oracle: casi toda la función está desconectada de la realidad — presets/trigger/fire con formas distintas, y la clasificación de seguridad de errores no matchea nada real | Encontrado 2026-08-28, misma auditoría — la parte más grande de todas. Además de lo que no existe en absoluto (ver ZG-64), lo que SÍ existe tiene el shape mal: <br>**Presets** — `getOraclePresets()` espera un array `{id,name,dm_event}[]` (`schemas.ts:224-230`); la ruta real (`presets.rs:171-174`) devuelve un OBJETO `{events: [...], entity_templates: [...]}`, y cada evento tiene `title` (no `name`) más un `summary` extra que el cliente no modela — array-vs-objeto solo ya rompe el parseo siempre. <br>**Trigger/Fire** — `OracleTriggerResponseSchema` espera `{would_spawn, bodies, resolved_pos, nearest_player_dist}` (`schemas.ts:238-244`); la ruta real (`engine.rs:286-305`) devuelve una unión con tag: `Preview{...}` para dry-run (con `distance_to_nearest_player`, no `nearest_player_dist`) o `Triggered{...}` para un fire real — **sin ningún campo `bodies`**. Como ningún nombre de campo matchea, **un Fire real y exitoso (200 OK, el mundo ya se modificó) se muestra al operador como "no pudimos confirmar si funcionó"**, siempre — es el hallazgo más delicado de toda la auditoría porque toca seguridad operativa, no solo UX. <br>**Clasificación de seguridad muerta** — `OracleDryRunScreen.tsx`'s `DEFINITE_NO_SPAWN_ERROR_CODES` (códigos como `oracle_disabled`/`invalid_body`/`event_not_found`) viene del mock, no de la fuente real — Zuul real nunca manda esos códigos en JSON, manda texto plano en casi todos los rechazos (`oracle.rs:415-421,477-481,504-508,626-648`) que `httpClient.ts` no puede parsear como envelope, así que caen siempre en `unknown_error`, que nunca está en la lista — ningún error real, ni siquiera uno trivial como "event_id inválido", se clasifica jamás como "seguro que no disparó nada". Falla hacia el lado cauteloso (no hacia el peligroso), pero el mecanismo entero es código muerto contra producción. Ver también ZG-65 (pedido a Zuul de sumar un envelope JSON acá, que destrabaría este punto del todo). <br>**Fix en este repo**: reescribir `OraclePresetsResponseSchema`/`OracleTriggerResponseSchema` a los shapes reales, reescribir `DEFINITE_NO_SPAWN_ERROR_CODES`/`isIndeterminateFireFailure` para clasificar por status+mensaje de texto plano en vez de por código JSON inexistente (hasta que ZG-65 exista, si se hace). `Stage` queda fuera de esta fila — ver ZG-64, el modelo entero de `DmEvent` que este cliente usa (`kind`/`intensity`/`radius`) no se parece en nada al real (`spawning_rules`/`narrative`/`dimension_config`/`atmosphere`), así que ese es un rediseño, no un ajuste de schema. **Fix implementado 2026-08-28**: `OraclePresetsResponseSchema`/`OracleTargetSchema`/`OracleTriggerResponseSchema` reescritos a los shapes reales (unión con tag `kind: 'triggered'|'preview'`), `high_impact_override` agregado al request de trigger/fire, `DEFINITE_NO_SPAWN_ERROR_CODES` reemplazado por los códigos reales confirmados por la sesión de Zuul (ZG-64/65). Verificado end-to-end contra el mock actualizado (stage → dry-run preview → fire real, los tres con resultado visualmente correcto) además de `tsc`/lint/format; falta verificación en vivo contra Zuul real. `Stage` sigue fuera de este fix — ver OC-72. | ⬜ |
| OC-72 | Oracle "stage": rediseño completo del lado cliente — el `DmEvent` que arma el composer no se parece en nada al real, y el body/response tampoco | Encontrado 2026-08-28, misma auditoría que OC-71 (separado a propósito porque es mucho más grande que un ajuste de schema). `stageOracleEvent()` (`writeApi.ts:186-192`) POSTea `{id, dm_event: <DmEventSchema JSON>}` a `/oracle/stage` — el handler real (`oracle.rs:230-233`, `StageDmEventRequest`) espera `{event_id: String, ron_body: String}`: no solo cambia el nombre del campo (`id`→`event_id`, cosmético), espera un **string serializado en RON**, no un objeto JSON (esto solo ya hace fallar la extracción `Json` de Axum). Peor: el `DmEventSchema` del cliente (`schemas.ts:201-209`: `kind`/`template_id`/`intensity`/`radius`/`dimension_config?`/`atmosphere?`) no se parece en nada al `DmEvent` real (`presets.rs:44-49`: `{dimension_config, atmosphere, spawning_rules, narrative}`, con `spawning_rules`/`narrative` — `entity_templates`, `spawn_count`, `world_rumor`, etc. — ausentes del todo en el modelo del cliente). El composer no puede armar un request válido, punto. Encima, en éxito la ruta real devuelve `204` sin body (`oracle.rs:283`), pero `StageOracleEventResponseSchema` espera `{loaded, sanitized, diff}` — `httpClient.ts` salta el parseo en 204 y devuelve `undefined`, y `OracleComposerScreen.tsx:213` hace `if (result === null || ...)` (nunca es `null`, es `undefined`) así que sigue de largo y explota con un `TypeError` leyendo `.loaded`/`.diff` de `undefined` — **la pantalla de Compose crashea en cada stage exitoso**, aparte del problema de shape. Fix: esto es un rediseño real del composer (formulario, tipos, generación de RON en vez de JSON) más que un ajuste de schema — evaluar si conviene resolverlo junto con quien mantiene los presets reales del lado de Zuul (`presets.rs`), ya que el shape de `DmEvent` que hay que replicar vive ahí. <br><br>**Actualización 2026-08-28**: Matías propuso en el chat que sea Zuul quien convierta JSON↔RON, en vez de que este cliente tenga que generar el string RON a mano — de acuerdo, y escribí `ZG-66` en el backlog de `xindeler-zuul` (`docs/backlog.md`, PR #95) proponiendo exactamente eso: `stage` acepta/devuelve `DmEvent` como JSON (reusando el struct que `presets.rs` ya tiene, con el crate `ron` que ya es dependencia de ese repo), y la serialización a RON queda del lado de Zuul, antes de escribir el archivo. **Este OC-72 queda bloqueado por ZG-66** — si esa fila se implementa, el alcance de este ítem se reduce mucho: en vez de "construir un serializador RON en TypeScript", pasa a ser solo "alinear el formulario del composer con los campos reales de `DmEvent`" (`dimension_config`/`atmosphere`/`spawning_rules`/`narrative`, nada de `kind`/`intensity`/`radius` como hoy). No re-priorizar este ítem hasta saber qué decide Zuul con ZG-66. <br><br>**Nota relacionada, 2026-08-28** (de una charla de coordinación con la sesión de Zuul sobre ZG-67, la investigación de Bedrock): el evento `draft` de `streamOracleChat.ts` (Oracle Chat, todavía bloqueado en Bedrock/ZG-29) también manda `DmEvent` con el mismo `DmEventSchema` especulativo de acá (`kind`/`template_id`/`intensity`/`radius`), no el real. Cuando Oracle Chat deje de estar bloqueado y alguien arme un draft real del lado de Bedrock, ese evento también va a necesitar el shape real de `DmEvent` — mismo fix, otro punto de contacto. No es trabajo a hacer ahora (Oracle Chat entero sigue sin base real), solo dejarlo anotado para no repetir la investigación. <br><br>**Hecho 2026-08-28**, desbloqueado por ZG-66 (la sesión de Zuul lo implementó, PR #97). `DmEventSchema` reescrito al shape real (`dimension_config`/`atmosphere`/`spawning_rules`/`narrative`, sin `kind`/`intensity`/`radius`), `stageOracleEvent()` manda `{event_id, dm_event}` como JSON plano, sin schema de respuesta (204 real, confirmado directamente contra `oracle.rs` post-ZG-66). `OracleComposerScreen.tsx` reescrito por completo: multi-select de `entity_templates` (nuevo `MultiChipPicker`), `spawn_count`/`spawn_radius` con los rangos reales de ZG-68 (0-200/0-400, no forzados por el server todavía pero sí por este form), `ai_behavior_override` como chips (`passive`/`stalk`/`aggro`/`flee`), `seed_modifier`/`biome_profile`, toggle de `time_lock` fijo vs. ciclo día/noche normal, `weather_effect` como chips (Despejado/Nublado/Lluvia/Tormenta) con `transition_secs`, y los dos campos de narrativa (`world_rumor`/`on_enter_message`). Ya no hay concepto de `diff`/`loaded` en la respuesta — el crash real que existía antes (`TypeError` leyendo `.loaded` de un `undefined`) desaparece solo por eso. `ChatTurnRow.tsx`'s preview del `draft` también actualizado al shape real. Mock (`tools/mock-gateway`) actualizado: `oracleStage.js` acepta `{event_id, dm_event}` y devuelve 204, se borró `oracleSanitizer.js` (ya no hay sanitización simulada), presets/draft pool de `fixtures.js` reescritos con el shape real. Verificado con `tsc`/lint/format limpios y manualmente contra el mock (aplicar preset → multi-select de templates → guardar en etapa → aparece en la lista de eventos, sin errores de consola). | ✅ |
| OC-73 | Oracle Chat: el cliente sigue mandando `tier: 'local'` por defecto y esperando un `tier_breakdown` en el presupuesto — ninguno de los dos existe en el real, ahora que ZG-67 lo implementó de verdad | Encontrado 2026-08-28, coordinación en vivo con la sesión de Zuul mientras implementaba ZG-67 (Bedrock real para `oracle/chat`/`oracle/budget`, PR #98 de ese repo, sin mergear todavía). Confirmado directamente por esa sesión, no adivinado: **`POST /oracle/chat`** solo acepta `tier: "bedrock"` — cualquier otro valor (incluido `"local"`, que este cliente mandaba por defecto en el botón "Enviar", `OracleChatScreen.tsx:111` original) se rechaza con `400 unsupported_tier`. No hay ni va a haber un tier local (CLAUDE.md Q6 de ese repo: exclusivo a Bedrock, ni siquiera como primer paso). El botón "Pensar mejor" (tier `bedrock`, con el costo del mes en la etiqueta) quedaba entonces como la ÚNICA forma de que un mensaje funcionara contra Zuul real — "Enviar" hubiera fallado siempre. **`GET /oracle/budget`** tampoco matcheaba: el cliente esperaba `{month_to_date_tokens, month_to_date_cost_usd, tier_breakdown: {local, bedrock}}`; el shape real es `{month_to_date_input_tokens, month_to_date_output_tokens, month_to_date_cost_usd, monthly_cap_usd: number\|null, bedrock_configured: bool}` — sin desglose por tier (no hay más que uno), con conteo de tokens de entrada/salida separado, y con `bedrock_configured` como la señal real de si hay cuenta de AWS configurada (falso en todos lados hasta que ZG-29 desbloquee). <br><br>**Fix**: `OracleBudgetResponseSchema` reescrito al shape real. `streamOracleChat.ts` ya no expone `tier` como parámetro — lo fija a `'bedrock'` internamente (mismo patrón que `fireOracleEvent` con `dry_run: false`: un solo valor válido, no tiene sentido que sea un parámetro). `useOracleChatThreads.ts`/`ChatTurn` ya no rastrean `tier` por turno (no queda nada que distinguir). `OracleChatScreen.tsx`: se sacó el botón "Pensar mejor" (la duplicación no tiene sentido si "Enviar" ya manda el único tier real que existe), el aviso estático "todavía no tiene implementación en Zuul real" se reemplazó por uno que lee `budgetQuery.data.bedrock_configured` (la señal real, no atada al nombre del entorno), y la cifra de gasto mensual se muestra siempre bajo "Enviar" en vez de solo en el botón que ya no existe. Mock (`tools/mock-gateway`) actualizado: `oracleChat.js` rechaza cualquier `tier` que no sea `'bedrock'`, `oracleBudget.js` devuelve el shape real con `bedrock_configured: true` (para poder seguir probando el chat localmente). Verificado end-to-end contra el mock (enviar mensaje → contexto citado → streaming → borrador con el `DmEvent` real → Aplicar → precarga correcta en el composer de OC-72) además de `tsc`/lint/format limpios. | ✅ |
| OC-74 | Oracle stage: `spawn_count` por encima del límite operacional (50) ahora se rechaza con `412 high_impact_override_required` — el composer no tenía forma de mandar `high_impact_override` ni de confirmarlo | Encontrado 2026-08-28, revisión completa de todo lo que la sesión de Zuul implementó para cerrar Fase 5 (ZG-68/ZG-70, PR #100/#104 de ese repo, ambos mergeados). `StageDmEventRequest` ahora tiene `high_impact_override: bool` (`#[serde(default)]`) — confirmado directo contra `oracle.rs`. Con `spawning_rules.spawn_count` por encima de `SPAWN_COUNT_OPERATIONAL_CAP` (50, no confundir con el techo duro de 200 que `SPAWN_COUNT_BOUNDS` ya modelaba desde OC-72), Zuul rechaza con `412 high_impact_override_required` a menos que `high_impact_override: true` venga en el request, y aun así solo lo deja pasar si la sesión tiene un step-up activo (`403` si no). El composer nunca mandaba este campo — todo intento de guardar un evento con más de 50 criaturas hubiera fallado siempre, sin ninguna forma de confirmarlo desde la UI. <br><br>**Fix**: `stageOracleEvent()` (`writeApi.ts`) gana un parámetro `highImpactOverride` real y expuesto (a diferencia de `fireOracleEvent`, que sigue con `high_impact_override: false` fijo porque lo que significa "alto impacto" para un fire real sigue sin diseñarse del lado del motor). `OracleComposerScreen.tsx`: nuevo `SPAWN_COUNT_OPERATIONAL_CAP` en `schemas.ts`, warning visible en el campo Cantidad cuando se supera, y un `ConfirmByTypingSheet` (palabra "OVERRIDE") que gatea el guardado — mismo patrón que el gate de Fire en `OracleDryRunScreen.tsx`. El step-up se resuelve solo, vía el reintento automático en 403 que `useDestructiveAction` ya tenía. Mock actualizado: no se simula la validación de bounds/cap en profundidad (ver nota de alcance en OC-68 original), queda documentado como gap conocido, no bloqueante para probar el flujo de confirmación en sí. Verificado con `tsc`/lint/format limpios. | ✅ |
| OC-75 | Oracle Chat: `GET /oracle/budget` ahora manda `alert_level`, y `POST /oracle/chat` acepta `override_budget` para pasar el corte del 100% — el cliente no leía ninguno de los dos | Encontrado 2026-08-28, misma revisión que OC-74 (ZG-32, PR #102 de xindeler-zuul, mergeado). `BudgetResponse` gana `alert_level: "ok"\|"warning"\|"critical"\|"exceeded"` (`bedrock::alert_level`, umbrales reales 50%/80%/100% del cap configurado). `ChatRequest` gana `override_budget: bool` (`#[serde(default)]`) — al llegar al 100% del cap, Zuul devuelve `429 budget_exceeded` a menos que este campo venga en `true`, y aun así solo lo deja pasar con un step-up activo (`403` si no) — mismo patrón que ZG-70/OC-74 y que `oracle::trigger`'s `high_impact_override`. Sin esto, llegar al tope del presupuesto dejaba el chat completamente inutilizable el resto del mes, sin ninguna forma de continuar. <br><br>**Fix**: `OracleBudgetResponseSchema` gana `alert_level`, con el texto del gasto mensual en `OracleChatScreen.tsx` coloreado según ese nivel (`warning`→amarillo, `critical`/`exceeded`→rojo) en vez de un gris plano. `streamOracleChat.ts` gana `overrideBudget` (a diferencia de `tier`, este sí varía por request — no tiene sentido fijarlo). `useOracleChatThreads.ts`: `retryTurn` gana un tercer parámetro `overrideBudget`, establece el step-up (con `forceFresh: true`, mismo motivo que el Fire gate) ANTES de resetear el turno a `streaming` — así un step-up cancelado deja el turno intacto en vez de colgado a mitad de un reset. `ChatTurnRow.tsx`: cuando el error de un turno fallido es `budget_exceeded`, el botón "Reintentar" se reemplaza por "Reintentar anulando el presupuesto"; `OracleChatScreen.tsx` gatea eso con un `ConfirmByTypingSheet` (palabra "OVERRIDE") antes de llamar a `retryTurn` con el override. Mock actualizado: `oracleBudget.js` devuelve `alert_level` fijo (no simula la escalada dinámica en función del gasto, gap de cobertura conocido y aceptable — no hay forma de generar gasto real contra el mock). Verificado con `tsc`/lint/format limpios y manualmente contra el mock (presupuesto se muestra con el color correcto). | ✅ |
| OC-76 | Web Push (ZG-35): Zuul ya tiene el backend completo (register/unregister/vapid-public-key) y un hand-off documentado — el cliente web no tenía ninguna implementación, `PushNotificationsSettings.tsx` mostraba directamente "No disponible en la versión web" | Encontrado 2026-08-28, hand-off explícito de la sesión de Zuul (`docs/handoffs/2026-08-28-web-push-client-integration-for-zg35.md` en ese repo, PR #105 mergeado) — contrato completo y ya confirmado, no había nada que investigar del lado del servidor, solo implementar. Real: RFC 8291/8292 (cifrado + VAPID) hand-rolled en Rust puro (sin OpenSSL), `POST /push/web/register`/`/unregister` (CSRF-only, sin step-up, bodies de error en texto plano — no el envelope JSON de `oracle.rs`), `GET /push/web/vapid-public-key` (solo sesión, devuelve la clave pública VAPID en base64url como texto plano, o `503` si Zuul no tiene configurada la clave todavía). <br><br>**Fix**: `PushRegistration`/`PushStatus` (`PushTokenService.types.ts`) ampliados a una unión real — Web Push no tiene un `token` único, tiene `{endpoint, p256dh, auth}`. Nueva `PushTokenServiceImpl.web.ts`: detecta soporte real (`'serviceWorker' in navigator && 'PushManager' in window`), registra `public/service-worker.js` (nuevo — maneja el evento `push` mostrando `{title, body}`, el único payload que Zuul manda), pide el permiso del navegador (requiere gesto de usuario real, solo se dispara desde el botón "Activar") y se suscribe vía `PushManager.subscribe()` con la VAPID key real. `PushTokenServiceImpl.native.ts` ajustado a la nueva forma de la unión, sin cambios de comportamiento. `usePushRegistration.ts` despacha `registerPushToken`/`registerWebPush` según `registration.platform`. Nuevo `httpClient.ts`'s `requestText()` (la vapid-public-key es texto plano, no JSON — `request()` normal siempre intenta `response.json()`). `PushNotificationsSettings.tsx` actualizado: el estado `unsupported` ahora significa "este navegador específico no soporta push", no "la web en general"; mensajes de `denied`/`registered` diferenciados por plataforma (web no tiene `Linking.openSettings()`, ni depende de credenciales EAS). Mock (`tools/mock-gateway`) gana `routes/pushWeb.js` con el mismo contrato exacto (texto plano, 204, validación de `endpoint`/`p256dh`/`auth`). Verificado: `tsc`/lint/format limpios; contra el mock, confirmado por curl que `vapid-public-key`/`register`/`unregister` devuelven exactamente el shape real (probado los tres). En el navegador real (Chrome vía Claude-in-Chrome): confirmado que la detección de soporte funciona, que el service worker se registra correctamente (`scriptURL` real), y que el flujo llega hasta pedir el permiso del navegador — **el permiso en sí y el envío real de una notificación a través de un servicio de push real quedan sin probar de punta a punta**, mismo límite que el propio hand-off de Zuul ya documentó (requiere un gesto de usuario confiable que la automatización no puede otorgar, y una cuenta VAPID real desplegada). | ✅ (client; falta verificación manual real con un push service en vivo) |
| OC-77 | ⚠️ Primer login de un operador nuevo: pantalla de enroll de TOTP con QR — **implementado, deployado en v0.3.0, y revertido el mismo día por una falla de seguridad en el diseño de contrato que lo sustentaba** | Construido 2026-08-28 sobre el diseño original de `ZG-73` (propuesto por Matías, aprobado en el chat): pantalla `/enroll`, `AuthContext.checkLoginStatus()`/`confirmEnrollment()`, mock gateway actualizado — ver la entrada original de este mismo backlog (recuperable en el historial de git, commit `3b0df0c`) para el detalle completo de esa implementación. Mergeado vía PR #86 → PR #87 → tag `v0.3.0`, deployado a `zuul.xindeler.com`. <br><br>**Revertido 2026-08-29.** La sesión de Zuul reportó que el diseño original de `ZG-73` — `POST /login` con `totp_code` vacío devolviendo directamente el secret/QR de TOTP con solo la contraseña correcta — reintroduce exactamente el ataque que `ZG-38` (2026-08-10) había rechazado explícitamente: una contraseña robada pero nunca usada por su dueño real le permite a un atacante auto-enrolarse su propio TOTP y apropiarse la cuenta de forma permanente. Confirmado por dos revisores de seguridad independientes del lado de Zuul antes de siquiera abrir un PR ahí — **nunca se implementó del lado del servidor**, así que la pantalla que llegó a producción en `v0.3.0` fue inofensiva todo este tiempo (contra Zuul real, `/login` con código vacío seguía devolviendo el mismo rechazo genérico de siempre). Revertido igual, por higiene: el contrato que esta implementación asumía quedó completamente obsoleto. <br><br>**Nuevo diseño en curso** (Zuul, branch `zg73/self-service-totp-enrollment`, no confirmado del todo): el enroll se dispara por un link de invitación por email de 24h (`zuul.xindeler.com/enroll?token=...`) que el propio Zuul manda al agregar un operador nuevo (exige email verificado en xindeler-auth); `/login` con código vacío ya no devuelve ningún secret, solo `{"status":"enrollment_required"}`; nuevo endpoint sin autenticar `POST /enroll/begin` con el token de la URL es lo que devuelve el QR/secret; `POST /admin/operators` cambia de `204` a `200 {added, invite_email_sent}`. Falta el endpoint de reenvío de invitación. Este ítem queda `⬜` hasta que la sesión de Zuul confirme el contrato final y se reconstruya la pantalla contra eso — probablemente una implementación bastante distinta (lee un token de query string en vez de participar del flujo normal de login). | ⬜ (revertido; esperando contrato final de ZG-73) |
| OC-77 (ronda 2) | Reconstrucción completa contra el contrato final de `ZG-73` (email-invite-token) — PR #111 de Zuul, no mergeado todavía pero confirmado como definitivo por esa sesión | Implementado 2026-08-29 contra el contrato final que la sesión de Zuul mandó (dos revisiones de seguridad independientes, sin hallazgos bloqueantes): `POST /login` con `totp_code:""` ahora devuelve SOLO `{"status":"enrollment_required"}` (nunca un secret — `LoginEnrollmentRequiredSchema` ya no tiene campos); `AuthContext.checkLoginStatus()` ya no guarda ningún `pendingEnrollment` (no hay nada que guardar), muestra un mensaje inline en `login.tsx` en vez de navegar a ningún lado. Pantalla `/enroll` reescrita como página completamente standalone: lee `token` de la query string (`useLocalSearchParams`, no depende de `AuthContext` en absoluto salvo `useApi()`), llama a `POST /enroll/begin` (nuevo, sin autenticar) para conseguir el QR/secret, y el formulario de confirmación (usuario/contraseña/código, re-ingresados ahí mismo) llama directo a `enrollConfirm()` — sin pasar por ningún estado de login pendiente. `POST /admin/operators` (`OperatorsScreen.tsx`) actualizado: `addOperator()` ahora devuelve `{added, invite_email_sent}` en vez de `204`, con mensaje de éxito distinto si el mail falló; nuevo botón "Reenviar invitación" por fila (visible solo si `totp_status !== 'confirmed'`) llamando al nuevo `resendEnrollmentInvite()`, gateado por el mismo step-up que add/remove — usa un `ref` (no state) para el uuid objetivo, mismo patrón que `pendingCredentials` en `AuthContext`, porque acá no hay `ConfirmByTypingSheet` de por medio que garantice un render entre "elegir el target" y "ejecutar". Mock actualizado con el contrato completo: segunda operadora seedeada `maat` (sin TOTP, con `_inviteToken` fijo para probar sin pasos previos), `MOCK_UNVERIFIED_EMAIL_UUID` para ejercitar el nuevo rechazo `400` de email no verificado, `/enroll/begin` y `/admin/operators/:uuid/resend-enrollment-invite` nuevos. Verificado con `tsc`/lint/format limpios y de punta a punta en el navegador contra el mock: link de invitación → QR → confirmar → vuelve a login → login normal completo; agregar operador con UUID de email no verificado (rechazado, mensaje visible) y con uno válido (éxito, aparece "Reenviar invitación"); reenvío de invitación con step-up (mensaje de confirmación visible). Falta verificación real contra Zuul una vez que el PR #111 de ese repo se mergee y deployee. <br><br>**Actualización 2026-08-29**: PR #89 → PR #90 → tag `v0.3.1`, deployado. `ZG-73` (Zuul PR #111) también mergeado y deployado (`v1.11.0`) el mismo día. Falta la verificación real de punta a punta con Maat en producción (pendiente de que alguien con acceso corra "Reenviar invitación" desde el admin real). | ✅ (deployado; falta verificación real de punta a punta con Maat) |
| OC-78 | 🔴 El entorno seleccionado ("Público"/"Mock"/"WireGuard") se perdía en cada carga fresca de la app — un link de invitación por email, o simplemente reabrir la app, podían arrancar en Mock aunque el operador ya hubiera elegido Público antes | Encontrado 2026-08-29, reportado en vivo por Matías: al abrir el link de invitación de TOTP de Maat, la app arrancó en Mock en vez de Público; reproducido después en el simulador de iPad de forma independiente (reinicio del simulador → la app volvió a Mock). Causa raíz en `EnvironmentContext.tsx`: el efecto que restaura el entorno guardado en `AsyncStorage` tenía `if (stored === 'mock' || stored === 'wireguard')` — una lista de dos valores escrita a mano que **nunca incluyó `'public'`**, agregado después. Cualquier carga fresca (una pestaña nueva, un link abierto desde un cliente de mail, cualquier reload que no sea navegación interna de la SPA) volvía a ejecutar este efecto, y una elección explícita de "Público" quedaba descartada en silencio, cayendo siempre a `DEFAULT_ENVIRONMENT_ID`. Ese default era, además, incondicionalmente `'mock'` — un resabio de antes de que existiera un gateway de producción real (`environments.ts`'s propio comentario viejo: "OC-13 hasn't built the real mock-gateway server yet") — así que un dispositivo/navegador sin nada guardado todavía (el caso más realista para un operador nuevo abriendo su primer link de invitación) caía en Mock igual, sin ni siquiera pasar por el bug de restauración. <br><br>**Fix**: la restauración ahora chequea membership contra `ENVIRONMENTS` (`stored in ENVIRONMENTS`) en vez de una lista enumerada a mano — un futuro entorno nuevo no puede reintroducir esta misma clase de bug. `DEFAULT_ENVIRONMENT_ID` pasa a ser `__DEV__ ? 'mock' : 'public'` (el global estándar de Metro/Expo, `true` en un build de desarrollo, `false` en un export de producción como el que `deploy-web.yml` despliega) — un checkout de desarrollo local sigue arrancando en Mock como siempre, pero un build de producción real ya no puede aterrizar silenciosamente en un endpoint mock que ni siquiera existe desde la máquina del operador. Verificado con `tsc`/lint/format limpios. | ✅ |
| OC-79 | `/enroll` mostraba el texto crudo del servidor ("invalid credentials") en vez del mensaje pensado para un link inválido/expirado, contradiciendo la guía explícita del handoff de `ZG-73` | Encontrado 2026-08-29, reportado en vivo por Matías al probar un link de invitación real: la pantalla mostró literalmente "invalid credentials" en vez de "Este link de invitación no es válido o ya expiró...". Causa: `enroll.tsx` solo usaba el texto fijo cuando el error NO era un `ApiError` — para cualquier `ApiError` real (incluido el `401 invalid credentials` genérico que `POST /enroll/begin` devuelve a propósito para "token desconocido/expirado/ya usado/enrollment ya confirmado", según el propio handoff de la sesión de Zuul: "don't try to distinguish reasons in the UI, just show this link is invalid or expired") mostraba `zuulErrorMessage(...)`, que devuelve el `error.message` crudo del servidor sin más. Fix: solo se usa el mensaje crudo (+ el hint de VPN si aplica) cuando `error.status === 0` (una falla de red/timeout real, que nunca llegó a Zuul) — cualquier respuesta HTTP real, incluido ese `401` genérico, muestra siempre el texto fijo de este screen. Verificado con `tsc`/lint/format limpios. <br><br>**Sin confirmar todavía si el link que probó Matías era genuinamente inválido/expirado (una reinvitación posterior lo habría reemplazado) o si hay un bug real del lado de `enroll_begin`/`enrollment_invites` — pendiente de que confirme si era un link recién reenviado o uno más viejo.** | ✅ (mensaje corregido; causa del rechazo del link real sin confirmar) |

---

## Known blockers & dependencies

| Blocker | Blocks | Owner |
|---|---|---|
| `xindeler-ops-gateway` repo does not exist | Everything past OC-13 against a real backend | Matías / a separate session |
| The additive engine PRs (NH-75 §3.2) | Phase 2 and Phase 3 | `xindeler-new-horizon` |

Xcode and the Android SDK (the old OC-1/OC-2 blockers) are resolved — see `.claude/skills/ops-run/SKILL.md`
§0 for current toolchain state. **The six Phase-0 design decisions are resolved** too — see
"Decisions resolved" above, not the §9 worksheet in the spec (that worksheet is now historical; the
answers live in this file). **Phase 0 is fully complete** — OC-1 through OC-9 are all ✅, including
signed builds live on both TestFlight and Google Play internal testing.
